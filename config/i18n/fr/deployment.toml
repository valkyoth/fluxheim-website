locale_id = "fr-FR"
html_lang = "fr-FR"

[[phrase]]
from = "Fluxheim ships with a hardened systemd unit, RPM packaging, and rootless Podman container images for Wolfi, Alpine, SUSE Micro, and Debian."
to = "Fluxheim est livré avec une unité systemd durcie, des paquets RPM et des images conteneur Podman rootless pour Wolfi, Alpine, SUSE Micro et Debian."

[[phrase]]
from = "The tarball includes a pre-configured hardened systemd unit. The unit runs Fluxheim with"
to = "Le tarball inclut une unité systemd durcie préconfigurée. L'unité exécute Fluxheim avec"

[[phrase]]
from = "(to allow ports 80/443 without root) and a dedicated system user."
to = "(pour autoriser les ports 80/443 sans root) et un utilisateur système dédié."

[[phrase]]
from = "Setup"
to = "Configuration"

[[phrase]]
from = "bash — systemd setup"
to = "bash - configuration systemd"

[[phrase]]
from = "Config Reload"
to = "Rechargement de configuration"

[[phrase]]
from = "Fluxheim supports live config reload without dropping connections. Send"
to = "Fluxheim prend en charge le rechargement live de configuration sans couper les connexions. Envoyez"

[[phrase]]
from = "or use the admin API."
to = "ou utilisez l'API admin."

[[phrase]]
from = "Rootless Podman Containers"
to = "Conteneurs Podman rootless"

[[phrase]]
from = "Fluxheim's official container images are designed for rootless Podman operation. They use internal ports 8080 (HTTP) and 8443 (HTTPS) by default — no root required. The same release image set is published to GHCR and"
to = "Les images conteneur officielles de Fluxheim sont conçues pour Podman rootless. Elles utilisent par défaut les ports internes 8080 (HTTP) et 8443 (HTTPS) - aucun root requis. Le même jeu d'images de release est publié sur GHCR et"

[[phrase]]
from = "Quay"
to = "Quay"

[[phrase]]
from = "Image tag"
to = "Tag d'image"

[[phrase]]
from = "Base"
to = "Base"

[[phrase]]
from = "Profile"
to = "Profil"

[[phrase]]
from = "Full (recommended)"
to = "Complet (recommandé)"

[[phrase]]
from = "Load balancer edge"
to = "Edge load-balancer"

[[phrase]]
from = "Cache edge"
to = "Edge de cache"

[[phrase]]
from = "Proxy edge"
to = "Edge proxy"

[[phrase]]
from = "Managed PHP-FPM"
to = "PHP-FPM géré"

[[phrase]]
from = "Full mirror"
to = "Miroir complet"

[[phrase]]
from = "Load balancer mirror"
to = "Miroir load-balancer"

[[phrase]]
from = "Cache mirror"
to = "Miroir cache"

[[phrase]]
from = "Proxy mirror"
to = "Miroir proxy"

[[phrase]]
from = "PHP mirror"
to = "Miroir PHP"

[[phrase]]
from = "bash — rootless Podman run"
to = "bash - exécution Podman rootless"

[[phrase]]
from = "bash — image variants"
to = "bash - variantes d'image"

[[phrase]]
from = "Podman Quadlet (systemd integration)"
to = "Podman Quadlet (intégration systemd)"

[[phrase]]
from = "Use Podman Quadlet to manage container lifecycle with systemd on the host:"
to = "Utilisez Podman Quadlet pour gérer le cycle de vie du conteneur avec systemd sur l'hôte :"

[[phrase]]
from = "Production Readiness Checklist"
to = "Checklist de préparation production"

[[phrase]]
from = "<li>Run <code>fluxheim --check-config</code> before every restart or reload</li>"
to = "<li>Exécutez <code>fluxheim --check-config</code> avant chaque redémarrage ou rechargement</li>"

[[phrase]]
from = "<li>Run <code>scripts/release_checks.sh</code> before publishing security-sensitive changes</li>"
to = "<li>Exécutez <code>scripts/release_checks.sh</code> avant de publier des changements sensibles à la sécurité</li>"

[[phrase]]
from = "Pin to a specific image digest in production, not"
to = "Épinglez un digest d'image précis en production, pas"

[[phrase]]
from = "Mount config and site content as read-only ("
to = "Montez la configuration et le contenu du site en lecture seule ("

[[phrase]]
from = "Mount ACME state directory as read-write ("
to = "Montez le répertoire d'état ACME en lecture-écriture ("

[[phrase]]
from = ") separately"
to = ") séparément"

[[phrase]]
from = "Use the admin API over loopback only, or configure a trusted TLS terminator"
to = "Utilisez l'API admin uniquement via loopback, ou configurez un terminateur TLS de confiance"

[[phrase]]
from = "<li>Set <code>[server.host_routing] strict = true</code> for multi-tenant hardened deployments</li>"
to = "<li>Définissez <code>[server.host_routing] strict = true</code> pour les déploiements multi-tenant durcis</li>"

[[phrase]]
from = "<li>Review <code>deny_dotfiles = true</code> is set on all <code>[vhosts.web]</code> sections</li>"
to = "<li>Vérifiez que <code>deny_dotfiles = true</code> est défini sur toutes les sections <code>[vhosts.web]</code></li>"

[[phrase]]
from = "Verify HSTS is only set when TLS is confirmed working"
to = "Vérifiez que HSTS n'est défini que lorsque TLS fonctionne de façon confirmée"

[[phrase]]
from = "The native systemd unit (at"
to = "L'unité systemd native (dans"

[[phrase]]
from = ". Container configs (at"
to = ". Les configurations conteneur (dans"

[[phrase]]
from = ") use internal ports 8080 and 8443 for rootless compatibility."
to = ") utilisent les ports internes 8080 et 8443 pour la compatibilité rootless."

[[phrase]]
from = "Systemd & Containers"
to = "Systemd & conteneurs"

[[phrase]]
from = "Wolfi + PHP"
to = "Wolfi + PHP"

[[phrase]]
from = "before every restart or reload"
to = "avant chaque redémarrage ou rechargement"

[[phrase]]
from = "for multi-tenant hardened deployments"
to = "pour les déploiements multi-tenant durcis"
