locale_id = "fr-FR"
html_lang = "fr-FR"

[[phrase]]
from = "Cache Backends — Fluxheim Source Docs"
to = "Backends de cache - Documentation source Fluxheim"

[[phrase]]
from = "Cache Backends"
to = "Backends de cache"

[[phrase]]
from = "Fluxheim's cache configuration is intentionally byte-budgeted even when a backend crate is count-based. Operators should be able to say \"use 1 GiB of RAM\" or \"use this 10 GiB disk directory\" globally or per vhost without knowing the internal cache implementation."
to = "La configuration de cache de Fluxheim est volontairement exprimee en budgets d'octets, meme lorsqu'une crate backend raisonne par nombre d'elements. Les operateurs doivent pouvoir dire globalement ou par vhost \"utiliser 1 Gio de RAM\" ou \"utiliser ce repertoire disque de 10 Gio\" sans connaitre l'implementation interne du cache."

[[phrase]]
from = "Current Implementation"
to = "Implementation actuelle"

[[phrase]]
from = "Global and per-vhost cache policies share the same typed model."
to = "Les politiques de cache globales et par vhost partagent le meme modele type."

[[phrase]]
from = "is converted into a conservative object-slot plan by dividing the memory budget by"
to = "est converti en plan conservateur de slots d'objets en divisant le budget memoire par"

[[phrase]]
from = "Enabled memory tiers create a byte-weighted in-process"
to = "Les niveaux memoire actives creent un cache in-process pondere en octets"

[[phrase]]
from = "cache per runtime vhost policy."
to = "par politique de vhost runtime."

[[phrase]]
from = "are retained in the storage plan after config-path resolution."
to = "sont conserves dans le plan de stockage apres resolution des chemins de configuration."

[[phrase]]
from = "The disk tier currently stores one object per filesystem-managed cache file. This keeps the implementation portable, inspectable, and easy to recover. A future advanced backend may add slab/bin storage with pre-allocated data files to reduce filesystem overhead and fragmentation on very large, high-churn caches."
to = "Le niveau disque stocke actuellement un objet par fichier de cache gere par le systeme de fichiers. Cela garde l'implementation portable, inspectable et facile a recuperer. Un futur backend avance pourra ajouter un stockage slab/bin avec fichiers de donnees prealloues afin de reduire le surcout du systeme de fichiers et la fragmentation sur de tres grands caches a fort renouvellement."

[[phrase]]
from = "Enabled tier budgets must be at least as large as"
to = "Les budgets des niveaux actives doivent etre au moins aussi grands que"

[[phrase]]
from = "requires at least one storage tier."
to = "exige au moins un niveau de stockage."

[[phrase]]
from = "The proxy emits vhost-aware Pingora cache keys and enables Pingora"
to = "Le proxy emet des cles de cache Pingora conscientes du vhost et active l'admission Pingora"

[[phrase]]
from = "admission for eligible image requests with a configured memory or disk tier."
to = "pour les requetes image eligibles avec un niveau memoire ou disque configure."

[[phrase]]
from = "Pingora cache locks collapse concurrent misses for the same cache key to prevent cache stampedes when many clients request the same uncached or expired object at once. One request receives the writer permit and fetches from the origin; matching readers wait for that writer up to the configured timeout instead of all hitting the origin together."
to = "Les verrous de cache Pingora regroupent les misses concurrents pour la meme cle de cache afin d'eviter les cache stampedes lorsque de nombreux clients demandent simultanement le meme objet non cache ou expire. Une requete recoit le permis d'ecriture et recupere depuis l'origine; les lecteurs correspondants attendent ce writer jusqu'au timeout configure au lieu de frapper tous l'origine en meme temps."

[[phrase]]
from = "configure whether request collapsing is enabled and how long writer age and reader wait timeouts last. Defaults preserve the original 30 second writer age timeout and 30 second waiter timeout."
to = "configurent l'activation du regroupement de requetes ainsi que les durees de timeout d'age writer et d'attente reader. Les valeurs par defaut conservent le timeout writer age de 30 secondes et le timeout waiter de 30 secondes."

[[phrase]]
from = "The predictor keeps a bounded LRU of primary keys that recently produced origin-level uncacheable outcomes, allowing Fluxheim to bypass cache lookup and cache locking early for those keys."
to = "Le predicteur conserve un LRU borne de cles primaires ayant recemment produit des resultats non cacheables au niveau origine, ce qui permet a Fluxheim d'eviter tot le lookup cache et le verrouillage cache pour ces cles."

[[phrase]]
from = "Fluxheim-specific custom policy reasons are skipped so explicit Fluxheim controls such as"
to = "Les raisons de politique personnalisees propres a Fluxheim sont ignorees afin que les controles explicites Fluxheim tels que"

[[phrase]]
from = "optionally emit a cache debug header such as"
to = "emettent optionnellement un header de debug cache tel que"

[[phrase]]
from = "for requests that participate in the proxy cache."
to = "pour les requetes qui participent au proxy cache."

[[phrase]]
from = "remove explicitly configured upstream response headers before cache admission and downstream delivery. This is intended for tightly scoped static-asset routes where operators know a header such as"
to = "retirent les headers de reponse upstream explicitement configures avant l'admission en cache et la livraison downstream. C'est prevu pour des routes d'assets statiques tres delimitees ou les operateurs savent qu'un header tel que"

[[phrase]]
from = "is not part of the cache identity."
to = "ne fait pas partie de l'identite de cache."

[[phrase]]
from = "control which origin response headers are trusted as cache-tag sources for indexed tag purge. Defaults are"
to = "controlent quels headers de reponse origine sont approuves comme sources de cache-tags pour la purge indexee par tag. Les valeurs par defaut sont"

[[phrase]]
from = "set an empty list to disable tag indexing for a cache policy."
to = "definissez une liste vide pour desactiver l'indexation des tags pour une politique de cache."

[[phrase]]
from = "bypass cache lookup and storage when any listed request header is present. Use this for route policies where headers such as"
to = "contournent le lookup cache et le stockage lorsqu'un header de requete liste est present. Utilisez cela pour les politiques de route ou des headers tels que"

[[phrase]]
from = "make the upstream response request-specific."
to = "rendent la reponse upstream specifique a la requete."

[[phrase]]
from = "provide narrower bypass controls for preview flags, session cookies, and private query modes while keeping unrelated public requests cacheable. Query bypass matching checks both raw and percent-decoded query parameter names and values so encoded equivalents cannot bypass a configured no-cache switch."
to = "fournissent des controles de bypass plus fins pour les flags de preview, cookies de session et modes de requete prives tout en gardant cacheables les requetes publiques sans lien. Le matching de bypass de query verifie les noms et valeurs de parametres bruts et decodes en pourcentage afin que des equivalents encodes ne puissent pas contourner un commutateur no-cache configure."

[[phrase]]
from = "remains a full request bypass because the client forbids storage."
to = "reste un bypass complet de requete parce que le client interdit le stockage."

[[phrase]]
from = "allows deliberate negative caching for configured statuses, such as a bounded 404 TTL for immutable asset paths."
to = "permet un negative caching volontaire pour des statuts configures, par exemple une TTL 404 bornee pour des chemins d'assets immuables."

[[phrase]]
from = "add safe request headers to the Pingora cache variance key even when the origin does not emit a matching"
to = "ajoutent des headers de requete surs a la cle de variance du cache Pingora meme lorsque l'origine n'emet pas de"

[[phrase]]
from = "for request-specific responses. The configured list is capped at 16 headers, matching the runtime Vary field cap."
to = "pour les reponses specifiques a la requete. La liste configuree est limitee a 16 headers, comme le plafond runtime du champ Vary."

[[phrase]]
from = "add an operator-controlled namespace component to the primary cache key. Bump this value to isolate new objects from older route-cache contents without changing URLs."
to = "ajoutent a la cle de cache primaire un composant de namespace controle par l'operateur. Augmentez cette valeur pour isoler les nouveaux objets des anciens contenus de cache de route sans modifier les URL."

[[phrase]]
from = "provide a constrained cache-key template made from safe request fields:"
to = "fournissent un template de cle de cache contraint, construit a partir de champs de requete surs:"

[[phrase]]
from = "can temporarily pass repeated uncacheable cache keys around cache lookup and storage. The feature is disabled by default and uses a bounded, short-lived in-memory table so dynamic one-off responses do not turn into unbounded state."
to = "peuvent temporairement faire passer des cles de cache non cacheables repetees autour du lookup cache et du stockage. La fonctionnalite est desactivee par defaut et utilise une table en memoire bornee et de courte duree afin que des reponses dynamiques ponctuelles ne deviennent pas un etat non borne."

[[phrase]]
from = "preloads explicit paths through a running local Fluxheim HTTP listener. It uses normal"
to = "precharge des chemins explicites via un listener HTTP Fluxheim local en cours d'execution. Il utilise des requetes"

[[phrase]]
from = "requests with the selected Host header, so vhost routing, route matching, cache keys, and admission rules are identical to real traffic."
to = "normales avec le header Host selectionne, afin que le routage vhost, le matching de route, les cles de cache et les regles d'admission soient identiques au trafic reel."

[[phrase]]
from = "The command prints bounded summary counts for response statuses, observed cache-status values, and failure reasons so release jobs can distinguish upstream errors, unexpected response statuses, and unexpected cache behavior without parsing every per-target line."
to = "La commande imprime des compteurs de synthese bornes pour les statuts de reponse, les valeurs cache-status observees et les raisons d'echec, afin que les jobs de release distinguent les erreurs upstream, les statuts de reponse inattendus et les comportements cache inattendus sans analyser chaque ligne par cible."

[[phrase]]
from = "define explicit positive TTLs by response status. Matching cache-participating origin responses have their freshness headers normalized to"
to = "definissent des TTL positives explicites par statut de reponse. Les reponses origine participantes au cache et correspondantes ont leurs headers de fraicheur normalises en"

[[phrase]]
from = "add an explicit stale-if-error window to cache-participating responses. Pingora can then serve an expired stored object during upstream errors while the stale window is still valid."
to = "ajoutent une fenetre stale-if-error explicite aux reponses participantes au cache. Pingora peut alors servir un objet stocke expire pendant des erreurs upstream tant que la fenetre stale reste valide."

[[phrase]]
from = "add an explicit stale-while-revalidate window to cache-participating responses. Pingora can then serve an expired stored object while revalidating it with the upstream."
to = "ajoutent une fenetre stale-while-revalidate explicite aux reponses participantes au cache. Pingora peut alors servir un objet stocke expire pendant qu'il le revalide avec l'upstream."

[[phrase]]
from = "control whether the request query string participates in the cache key."
to = "controlent si la query string de la requete participe a la cle de cache."

[[phrase]]
from = "The first Pingora memory adapter stores complete objects only; it buffers up to"
to = "Le premier adaptateur memoire Pingora ne stocke que des objets complets; il met en tampon jusqu'a"

[[phrase]]
from = "and refuses anything larger."
to = "et refuse tout objet plus grand."

[[phrase]]
from = "The first Pingora disk adapter stores complete objects below"
to = "Le premier adaptateur disque Pingora stocke les objets complets sous"

[[phrase]]
from = "Disk-cache reads canonicalize existing object paths, open cache objects without following symlinks on Linux, verify the opened handle is a regular file, and refuse encoded files larger than the configured object budget plus bounded metadata overhead."
to = "Les lectures du cache disque canonicalisent les chemins d'objets existants, ouvrent les objets de cache sans suivre les symlinks sous Linux, verifient que le handle ouvert est un fichier regulier et refusent les fichiers encodes plus grands que le budget objet configure plus le surcout de metadata borne."

[[phrase]]
from = "Startup creates the manifest atomically, reuses it on later starts, and rejects a storage-bin root if the manifest no longer matches the configured bin size, total byte budget, preallocation mode, or open-bin cap."
to = "Le demarrage cree le manifeste atomiquement, le reutilise aux demarrages suivants et rejette une racine storage-bin si le manifeste ne correspond plus a la taille de bin configuree, au budget total en octets, au mode de preallocation ou au plafond de bins ouverts."

[[phrase]]
from = "The allocator model uses first-fit free-range reuse within bounded bins and refuses allocations once the configured disk-cache byte budget is exhausted."
to = "Le modele d'allocateur reutilise les plages libres en first-fit dans des bins bornes et refuse les allocations une fois le budget d'octets configure du cache disque epuise."

[[phrase]]
from = "Storage-bin stats report allocated bin bytes, reusable free bytes, free range count, largest free range, and bin file count through the admin cache JSON and Prometheus aggregate gauges."
to = "Les statistiques storage-bin indiquent les octets de bins alloues, les octets libres reutilisables, le nombre de plages libres, la plus grande plage libre et le nombre de fichiers bin via le JSON admin de cache et les gauges Prometheus agregees."

[[phrase]]
from = "Optional cache encryption at rest is part of the"
to = "Le chiffrement optionnel du cache au repos fait partie de la ligne"

[[phrase]]
from = "distributed-cache line starts with a safe"
to = "distributed-cache commence avec un contrat sur"

[[phrase]]
from = "policy contract. Peer fill is disabled by default, requires an enabled cache policy, bounds peers/timeouts/object size/concurrency, and validates peer origins."
to = "sur. Peer fill est desactive par defaut, exige une politique de cache activee, borne les peers, timeouts, tailles d'objet et la concurrence, et valide les origines peer."

[[phrase]]
from = "New disk cache objects use the v5 object header, which stores the combined cache key, primary key, user tag, cache tags, and path-index metadata."
to = "Les nouveaux objets de cache disque utilisent le header d'objet v5, qui stocke la cle de cache combinee, la cle primaire, le user tag, les cache-tags et la metadata d'index de chemin."

[[phrase]]
from = "Disk eviction maintains an ordered LRU view inside the runtime disk-object index."
to = "L'eviction disque maintient une vue LRU ordonnee dans l'index runtime des objets disque."

[[phrase]]
from = "Disk-only cache admission streams response chunks into a bounded temporary file under the cache root before the final atomic object write."
to = "L'admission de cache disque seul streame les chunks de reponse dans un fichier temporaire borne sous la racine du cache avant l'ecriture atomique finale de l'objet."

[[phrase]]
from = "Cache-header semantics are partially implemented and remain a cache-pack hardening requirement before cache is considered complete."
to = "La semantique des headers de cache est partiellement implementee et reste une exigence de durcissement du cache-pack avant que le cache soit considere complet."

[[phrase]]
from = "When both memory and disk tiers are enabled on a vhost, Fluxheim uses a tiered Pingora storage adapter: memory is L1, disk is L2, misses are written to both tiers, disk hits are promoted back into memory when they fit, and purge invalidates both tiers."
to = "Lorsque les niveaux memoire et disque sont actives sur un vhost, Fluxheim utilise un adaptateur de stockage Pingora hierarchise: la memoire est L1, le disque est L2, les misses sont ecrits dans les deux niveaux, les hits disque sont promus en memoire lorsqu'ils tiennent, et la purge invalide les deux niveaux."

[[phrase]]
from = "reports aggregate, per-vhost, and per-route cache enablement, tiering, memory counters, disk counters, request-collapsing lock coverage, and cache activity counters for hits, misses, stores, refused stores, disk evictions, and purges."
to = "rapporte l'activation de cache agregee, par vhost et par route, le tiering, les compteurs memoire, les compteurs disque, la couverture des verrous de regroupement de requetes et les compteurs d'activite cache pour hits, misses, stores, stores refuses, evictions disque et purges."

[[phrase]]
from = "Example admin cache invalidation requests:"
to = "Exemples de requetes admin d'invalidation de cache:"

[[phrase]]
from = "when the cache policy lives on a route instead of the whole vhost:"
to = "lorsque la politique de cache vit sur une route plutot que sur tout le vhost:"

[[phrase]]
from = "The same route can be supplied through"
to = "La meme route peut etre fournie via"

[[phrase]]
from = "for automation that keeps control parameters in headers instead of URLs."
to = "pour une automatisation qui garde les parametres de controle dans les headers plutot que dans les URL."

[[phrase]]
from = "Example cache warm after a release deploy:"
to = "Exemple de cache warm apres un deploiement de release:"

[[phrase]]
from = "Example cache-key preview during a production incident:"
to = "Exemple de preview de cle de cache pendant un incident de production:"

[[phrase]]
from = "The preview and lookup commands validate the effective config, select the same vhost and route cache policy as a live request, and print the selected namespace, primary cache-key material, compact hashes, user tag, cache-lock state, cache-lock wait timeout, cacheability predictor state, selected memory/disk tier availability, and ineligibility reason when the request is not admitted."
to = "Les commandes de preview et de lookup valident la configuration effective, selectionnent la meme politique de cache vhost et route qu'une requete live, puis impriment le namespace selectionne, le materiau de cle de cache primaire, les hashes compacts, le user tag, l'etat du cache-lock, le timeout d'attente du cache-lock, l'etat du predicteur de cacheabilite, la disponibilite du niveau memoire/disque selectionne et la raison d'ineligibilite lorsque la requete n'est pas admise."

[[phrase]]
from = "Memory Cache Evaluation"
to = "Evaluation du cache memoire"

[[phrase]]
from = "Checked on 2026-05-04:"
to = "Verifie le 2026-05-04:"

[[phrase]]
from = "Checked on 2026-05-05:"
to = "Verifie le 2026-05-05:"

[[phrase]]
from = "API shape: generic in-memory cache with stampede protection."
to = "Forme d'API: cache generique en memoire avec protection contre le stampede."

[[phrase]]
from = "Capacity model: item count."
to = "Modele de capacite: nombre d'elements."

[[phrase]]
from = "Pingora HTTP cache compatibility: not a drop-in backend for"
to = "Compatibilite cache HTTP Pingora: pas un backend drop-in pour"

[[phrase]]
from = "Capacity model: weighted capacity with a caller-provided weigher."
to = "Modele de capacite: capacite ponderee avec un weigher fourni par l'appelant."

[[phrase]]
from = "Fluxheim use: current byte-weighted memory tier."
to = "Usage Fluxheim: niveau memoire actuel pondere en octets."

[[phrase]]
from = "Fluxheim use: fixed-length disk cache object paths."
to = "Usage Fluxheim: chemins d'objets de cache disque de longueur fixe."

[[phrase]]
from = "Fluxheim owns the cache implementation."
to = "Fluxheim possede l'implementation du cache."

[[phrase]]
from = "Request collapsing remains integrated with Pingora cache locks while the HTTP proxy path is still Pingora-backed."
to = "Le regroupement de requetes reste integre aux verrous de cache Pingora tant que le chemin proxy HTTP s'appuie encore sur Pingora."

[[phrase]]
from = "Additional Pingora cache primitives worth exposing as Fluxheim matures:"
to = "Primitives de cache Pingora supplementaires a exposer a mesure que Fluxheim murit:"

[[phrase]]
from = "Adapter Requirements"
to = "Exigences d'adaptateur"

[[phrase]]
from = "A production adapter must:"
to = "Un adaptateur de production doit:"

[[phrase]]
from = "Enforce byte budgets, not only item counts."
to = "Appliquer les budgets d'octets, pas seulement les nombres d'elements."

[[phrase]]
from = "Preserve HTTP cache metadata, including status, headers, validators, freshness metadata, combined cache keys, primary keys, and user tags for index rebuilds."
to = "Preserver la metadata de cache HTTP, notamment statut, headers, validateurs, metadata de fraicheur, cles de cache combinees, cles primaires et user tags pour les reconstructions d'index."

[[phrase]]
from = "Keep CDN/browser cache headers configurable through header policy and examples instead of hardcoded provider-specific defaults."
to = "Garder les headers de cache CDN/navigateur configurables via la politique de headers et des exemples plutot que via des valeurs par defaut provider hardcodees."

[[phrase]]
from = "Have focused tests for hit, miss, oversized object, purge, and vhost key isolation behavior."
to = "Avoir des tests cibles pour les comportements de hit, miss, objet trop volumineux, purge et isolation de cle vhost."

[[phrase]]
from = "is disabled by default so unauthenticated clients cannot force origin revalidation with"
to = "est desactive par defaut afin que les clients non authentifies ne puissent pas forcer la revalidation origine avec"

[[phrase]]
from = "lines. Input files are capped at 1 MiB, and the parsed target count is still bounded by"
to = "lignes. Les fichiers d'entree sont plafonnes a 1 Mio, et le nombre de cibles parse reste borne par"

[[phrase]]
from = "to verify an expected transition, such as first-fill"
to = "pour verifier une transition attendue, par exemple premier remplissage"

[[phrase]]
from = "options to warm negotiated variants for safe request headers such as"
to = "options pour warmer des variantes negociees pour des headers de requete surs comme"

[[phrase]]
from = "can narrow that behavior to selected upstream error classes such as"
to = "peut restreindre ce comportement a des classes d'erreurs upstream selectionnees comme"

[[phrase]]
from = "can narrow HTTP-status stale-if-error serving to selected 5xx origin statuses. An empty list means all upstream 5xx statuses that Pingora marks stale-if-error eligible."
to = "peut restreindre le service stale-if-error par statut HTTP a des statuts origine 5xx selectionnes. Une liste vide signifie tous les statuts upstream 5xx que Pingora marque eligibles stale-if-error."

[[phrase]]
from = "allow exact media types and subtype wildcards such as"
to = "autorisent les types media exacts et les wildcards de sous-type comme"

[[phrase]]
from = "key is accepted as the user-facing alias for the request-path extension allow-list, while"
to = "est acceptee comme alias expose a l'utilisateur pour l'allow-list des extensions de chemin de requete, tandis que"

[[phrase]]
from = "using SHA-256-derived shard paths, same-directory temporary files, and atomic rename. It refuses objects above"
to = "avec des chemins de shards derives de SHA-256, des fichiers temporaires dans le meme repertoire et un renommage atomique. Il refuse les objets au-dessus de"

[[phrase]]
from = "object so excess files cannot become untracked eviction orphans. Runtime stats and eviction use the maintained disk-object index instead of repeated filesystem scans. Purge, invalid-object cleanup, and eviction re-check the target immediately before deletion and only remove regular"
to = "objet afin que les fichiers en surplus ne deviennent pas des orphelins d'eviction non suivis. Les stats runtime et l'eviction utilisent l'index maintenu des objets disque au lieu de scans repetes du systeme de fichiers. Purge, nettoyage d'objets invalides et eviction reverifient la cible immediatement avant suppression et ne retirent que des"

[[phrase]]
from = "The storage-bin layout reserves a root-local"
to = "Le layout storage-bin reserve un manifeste local a la racine"

[[phrase]]
from = "data files, with 16-digit hexadecimal bin ids. Object metadata will point to"
to = "fichiers de donnees, avec des ids de bins hexadecimaux sur 16 chiffres. La metadata d'objet pointera vers"

[[phrase]]
from = "The storage-bin backend can encode, allocate, store, read, purge, and release objects through the manifest/bin/free-map primitives. Restart recovery, eviction parity, the Pingora"
to = "Le backend storage-bin peut encoder, allouer, stocker, lire, purger et liberer les objets via les primitives manifest/bin/free-map. La recuperation au redemarrage, la parite d'eviction, l'implementation Pingora"

[[phrase]]
from = "is preserved during admission, so a peer-filled object stores only its remaining freshness instead of extending the origin TTL. Peer-filled responses with"
to = "est preserve pendant l'admission, donc un objet rempli par peer ne stocke que sa fraicheur restante au lieu d'etendre la TTL origine. Les reponses remplies par peer avec"

[[phrase]]
from = "are stored under the matching variant key, so later local hits preserve negotiated variants. Peer-fill requests carry"
to = "sont stockees sous la cle de variante correspondante, afin que les hits locaux ulterieurs preservent les variantes negociees. Les requetes peer-fill portent"

[[phrase]]
from = "instead of recursing. Metrics builds expose aggregate peer-fill policy, peer-count, and concurrency gauges for rollout checks."
to = "au lieu de recursiver. Les builds avec metriques exposent des gauges agregees de politique peer-fill, nombre de peers et concurrence pour les controles de rollout."

[[phrase]]
from = "headers are normalized, request variant headers are hashed into the variant key, and unsafe or identity-sensitive"
to = "headers sont normalises, les headers de variante de requete sont hashes dans la cle de variante, et les headers"

[[phrase]]
from = "fields so operators can read hit-rate, miss-rate, admission health, and eviction pressure without external JSON post-processing. Totals and per-vhost status include"
to = "afin que les operateurs puissent lire taux de hit, taux de miss, sante d'admission et pression d'eviction sans post-traitement JSON externe. Les totaux et statuts par vhost incluent"

[[phrase]]
from = "records bounded per-tick duration so operators can distinguish an idle cleanup loop from one falling behind or blocked on slow storage."
to = "enregistre une duree bornee par tick afin que les operateurs distinguent une boucle de nettoyage inactive d'une boucle en retard ou bloquee sur un stockage lent."

[[phrase]]
from = "so tier-specific cleanup is visible without parsing each result. Purge identities are bounded before key derivation: hosts, methods, paths, queries, and bulk path count have explicit limits; paths must start with"
to = "afin que le nettoyage propre a chaque niveau soit visible sans analyser chaque resultat. Les identites de purge sont bornees avant derivation de cle: hosts, methodes, chemins, queries et nombre de chemins bulk ont des limites explicites; les chemins doivent commencer par"

[[phrase]]
from = "invalidates indexed entries for a vhost or route whose normalized request path starts with"
to = "invalide les entrees indexees pour un vhost ou une route dont le chemin de requete normalise commence par"

[[phrase]]
from = "to count stale objects without deleting them; dry-run responses include"
to = "pour compter les objets stale sans les supprimer; les reponses dry-run incluent"

[[phrase]]
from = "invalidates indexed entries by absolute path pattern using"
to = "invalide les entrees indexees par motif de chemin absolu avec"

[[phrase]]
from = "means the configured batch limit was reached while more indexed entries may remain. The index is bounded in memory, mirrors disk-tier writes, and is designed for operational invalidation rather than as a complete filesystem scan."
to = "signifie que la limite de batch configuree a ete atteinte alors que d'autres entrees indexees peuvent rester. L'index est borne en memoire, reflete les ecritures du niveau disque et est concu pour l'invalidation operationnelle plutot que comme scan complet du systeme de fichiers."

[[phrase]]
from = "flags fail when no matching object has the exact expected stored response header value, which lets smoke tests verify revalidation updated validators and freshness metadata without printing every stored header. Repeated"
to = "flags echouent lorsqu'aucun objet correspondant n'a la valeur exacte attendue du header de reponse stocke, ce qui permet aux smoke tests de verifier que la revalidation a mis a jour les validateurs et la metadata de fraicheur sans imprimer chaque header stocke. Les flags repetes"
[[phrase]]
from = "configured bypasses, and response-header refusal policies remain observable through the existing policy counters."
to = "bypasses configures et policies de refus de headers de reponse restent observables via les compteurs de policy existants."
[[phrase]]
from = "Enable it only on narrowly scoped routes that intentionally expose browser-style refresh semantics."
to = "Activez-le seulement sur des routes etroitement scopees qui exposent volontairement une semantique de refresh type navigateur."
[[phrase]]
from = "header. Sensitive headers such as"
to = "header. Les headers sensibles comme"
[[phrase]]
from = "are rejected here; use"
to = "sont rejetes ici; utilisez"
[[phrase]]
from = "is required, duplicates are rejected, and"
to = "est requis, les doublons sont rejetes, et"
[[phrase]]
from = "values or an input file containing"
to = "valeurs ou un fichier d'entree contenant"
[[phrase]]
from = "remove upstream"
to = "suppriment les"
[[phrase]]
from = "The default includes all classes for compatibility with the stale-if-error window."
to = "Le defaut inclut toutes les classes pour compatibilite avec la fenetre stale-if-error."
[[phrase]]
from = "maintains a runtime disk-object index for stats and least-recently-used eviction, and refuses admission only when the incoming object still cannot fit after eviction."
to = "maintient un index runtime d'objets disque pour les stats et l'eviction least-recently-used, et refuse l'admission seulement lorsque l'objet entrant ne rentre toujours pas apres eviction."
[[phrase]]
from = "backend enables the focused"
to = "backend active la ligne focalisee"
[[phrase]]
from = "slab/bin storage line."
to = "de stockage slab/bin."
[[phrase]]
from = "manifest and deterministic"
to = "manifest et les"
[[phrase]]
from = "expands new bin files to the configured bin size before object bytes are committed."
to = "etend les nouveaux fichiers bin a la taille de bin configuree avant que les octets objet soient commit."
[[phrase]]
from = "releases their bin ranges, removes purge-index metadata, and persists the updated storage-bin index."
to = "libere leurs ranges de bin, supprime les metadonnees purge-index et persiste l'index storage-bin mis a jour."
[[phrase]]
from = "peer hits are stored locally and peer requests do not forward the client"
to = "les hits peer sont stockes localement et les requetes peer ne forwardent pas le"
[[phrase]]
from = "header. Peer requests use the authority from the configured peer"
to = "client. Les requetes peer utilisent l'autority du peer configure"
[[phrase]]
from = "plus safe negotiation headers rather than client credentials."
to = "plus des headers de negotiation surs plutot que les credentials client."
[[phrase]]
from = "runs a local multi-node smoke that proves node-to-node"
to = "execute un smoke multi-node local qui prouve le node-to-node"
[[phrase]]
from = "no extra origin fetch, local post-fill"
to = "aucun fetch origin supplementaire, le post-fill local"
[[phrase]]
from = "and a request carrying that inbound marker cannot launch another outbound peer-fill fetch; cyclic peer topologies therefore fall back or fail closed according to"
to = "et une requete portant ce marqueur inbound ne peut pas lancer un autre fetch peer-fill outbound; les topologies peer cycliques fallback donc ou fail-closed selon"
[[phrase]]
from = "when available,"
to = "lorsque disponible,"
[[phrase]]
from = "Header policy lets operators set, append, and unset browser/CDN-facing headers such as"
to = "La header policy permet aux operateurs de definir, append et unset des headers destines navigateur/CDN comme"
[[phrase]]
from = "values still reject"
to = "valeurs rejettent toujours"
