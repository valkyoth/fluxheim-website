locale_id = "fr-FR"
html_lang = "fr-FR"

[[phrase]]
from = "Release Runbook — Fluxheim Source Docs"
to = "Runbook de publication - documentation source Fluxheim"

[[phrase]]
from = "Release Runbook"
to = "Runbook de publication"

[[phrase]]
from = "This is the maintainer procedure for publishing a Fluxheim release. It is the step-by-step operational companion to the broader release checklist."
to = "Voici la procedure mainteneur pour publier une version Fluxheim. C'est le compagnon operationnel etape par etape de la checklist de publication plus large."

[[phrase]]
from = "Use this from a clean"
to = "Utilisez ceci depuis un checkout"

[[phrase]]
from = "checkout. Set the release variables once, then reuse them through the commands below:"
to = "propre. Definissez les variables de publication une fois, puis reutilisez-les dans les commandes ci-dessous :"

[[phrase]]
from = "1. Preflight"
to = "1. Preflight"

[[phrase]]
from = "Confirm you are on the release commit and the worktree is clean:"
to = "Confirmez que vous etes sur le commit de publication et que le worktree est propre :"

[[phrase]]
from = "Run the local release checks that match the release scope:"
to = "Executez les controles de publication locaux correspondant au perimetre de la publication :"

[[phrase]]
from = "For PHP-FPM releases, also run:"
to = "Pour les publications PHP-FPM, executez aussi :"

[[phrase]]
from = "When collecting release evidence on a host that cannot build every FIPS backend, use the per-backend evidence skips and attach the missing backend evidence from its supported builder. For example, rolling distro compilers may be too new for"
to = "Lors de la collecte des preuves de publication sur un hote qui ne peut pas construire tous les backends FIPS, utilisez les skips de preuve par backend et joignez la preuve du backend manquant depuis son builder supporte. Par exemple, les compilateurs de distributions rolling peuvent etre trop recents pour"

[[phrase]]
from = "For stable or release-candidate builds, prefer the stable gate:"
to = "Pour les builds stables ou release candidates, preferez le stable gate :"

[[phrase]]
from = "mode this stable gate is intentionally tag-blocking for container images: it runs the root image smoke plus representative Debian and Alpine variant image smokes before a tag should be pushed. Use"
to = "ce stable gate bloque intentionnellement le tag pour les images conteneur : il execute le smoke de l'image racine ainsi que des smokes representatifs des variantes Debian et Alpine avant qu'un tag soit pousse. Utilisez"

[[phrase]]
from = "when the release needs full local variant evidence. Use"
to = "lorsque la publication a besoin d'une preuve locale complete des variantes. Utilisez"

[[phrase]]
from = "only when equivalent image evidence has already been collected on another builder. Set"
to = "uniquement lorsque des preuves d'image equivalentes ont deja ete collectees sur un autre builder. Definissez"

[[phrase]]
from = "to also build and run the focused load-balancer image against two local origins before tagging."
to = "pour aussi construire et executer l'image load-balancer ciblee contre deux origins locaux avant le tag."

[[phrase]]
from = "and later lines this stable gate includes the proxy cache and local observability smoke suites, plus compile and packaged-config checks for the published image profiles. That keeps cache, Prometheus/OpenTelemetry basics, and focused image feature wiring covered by the same command used for release evidence."
to = "et les lignes ulterieures, ce stable gate inclut les suites de smoke du cache proxy et de l'observabilite locale, ainsi que les controles de compilation et de configuration packagee pour les profils d'image publies. Cela garde le cache, les bases Prometheus/OpenTelemetry et le cablage des fonctionnalites d'image ciblees couverts par la meme commande que celle utilisee pour les preuves de publication."

[[phrase]]
from = "reports a known upstream advisory that cannot be fixed in this repository yet, record it explicitly in the release notes with the package, advisory ID, impact, and removal condition."
to = "signale un avis upstream connu qui ne peut pas encore etre corrige dans ce repository, consignez-le explicitement dans les notes de publication avec le paquet, l'ID d'avis, l'impact et la condition de retrait."

[[phrase]]
from = "2. Commit The Release Prep"
to = "2. Committer la preparation de publication"

[[phrase]]
from = "Commit any release-note, README, packaging, or metadata changes:"
to = "Committez toute modification des notes de publication, du README, du packaging ou des metadonnees :"

[[phrase]]
from = "If Git reports"
to = "Si Git indique"

[[phrase]]
from = ", continue from the current"
to = ", continuez depuis le"

[[phrase]]
from = "Record the commit:"
to = "Consignez le commit :"

[[phrase]]
from = "3. Create And Push The Signed Tag"
to = "3. Creer et pousser le tag signe"

[[phrase]]
from = "Create a signed tag:"
to = "Creez un tag signe :"

[[phrase]]
from = "Pushing the tag starts the container image workflow."
to = "Pousser le tag demarre le workflow d'image conteneur."

[[phrase]]
from = "4. Build The Binary Release Assets"
to = "4. Construire les assets binaires de publication"

[[phrase]]
from = "Build Linux runtime and config-tester release assets with the helper script. It names artifacts by normalized platform label instead of raw Rust target triple:"
to = "Construisez les assets de publication runtime Linux et config-tester avec le script helper. Il nomme les artefacts par label de plateforme normalise plutot que par triple cible Rust brut :"

[[phrase]]
from = "Rust target"
to = "Cible Rust"

[[phrase]]
from = "Artifact label"
to = "Label d'artefact"

[[phrase]]
from = "Build the current Linux host target:"
to = "Construisez la cible de l'hote Linux courant :"

[[phrase]]
from = "Build Linux ARM64 on an ARM64 Linux builder or configured cross-builder:"
to = "Construisez Linux ARM64 sur un builder Linux ARM64 ou un cross-builder configure :"

[[phrase]]
from = "This produces, for each Linux target, the full/cache/proxy/load-balancer/php runtime archives and the config-tester archive:"
to = "Cela produit, pour chaque cible Linux, les archives runtime full/cache/proxy/load-balancer/php et l'archive config-tester :"

[[phrase]]
from = "For Level 1 macOS developer artifacts, build on the matching Mac host. These artifacts are developer conveniences, not production packages:"
to = "Pour les artefacts developpeur macOS niveau 1, construisez sur l'hote Mac correspondant. Ces artefacts sont des facilites developpeur, pas des paquets de production :"

[[phrase]]
from = "Apple Silicon Macs produce"
to = "Les Mac Apple Silicon produisent"

[[phrase]]
from = ". Intel Macs produce"
to = ". Les Mac Intel produisent"

[[phrase]]
from = "Record all runtime and config-tester binary checksums."
to = "Consignez toutes les sommes de controle des binaires runtime et config-tester."

[[phrase]]
from = "Generate SBOMs for the tagged source tree:"
to = "Generez les SBOM pour l'arbre source tague :"

[[phrase]]
from = "Upload both SBOM files as release assets, and record their checksums in the release notes."
to = "Televersez les deux fichiers SBOM comme assets de publication et consignez leurs sommes de controle dans les notes de publication."

[[phrase]]
from = "Verify that the local release builder can reproduce the release binary from two separate target directories:"
to = "Verifiez que le builder de publication local peut reproduire le binaire de publication depuis deux repertoires target separes :"

[[phrase]]
from = "Record the reported binary hash as reproducible-build evidence."
to = "Consignez le hash binaire rapporte comme preuve de build reproductible."

[[phrase]]
from = "Do not commit"
to = "Ne committez pas"

[[phrase]]
from = "; it is local release output."
to = " ; c'est une sortie locale de publication."

[[phrase]]
from = "Optional FIPS-Capable Evidence"
to = "Preuve optionnelle compatible FIPS"

[[phrase]]
from = "For releases that changed FIPS-capable TLS code or docs, capture the local backend evidence when the release builder has the selected provider/toolchain installed:"
to = "Pour les publications qui modifient du code ou de la documentation TLS compatible FIPS, capturez la preuve locale du backend lorsque le builder de publication a le fournisseur ou la toolchain selectionne installe :"

[[phrase]]
from = "Record the command output, package/provider version, provider or build config, and the selected module Security Policy reference. The rustls/AWS-LC validation requires the"
to = "Consignez la sortie de commande, la version du paquet/fournisseur, la configuration fournisseur ou build, et la reference de Security Policy du module selectionne. La validation rustls/AWS-LC exige la"

[[phrase]]
from = "build toolchain, including CMake, Go, and a C compiler. If the local builder can collect OpenSSL FIPS evidence but not rustls/AWS-LC evidence, use"
to = "toolchain de build, incluant CMake, Go et un compilateur C. Si le builder local peut collecter la preuve OpenSSL FIPS mais pas la preuve rustls/AWS-LC, utilisez"

[[phrase]]
from = "locally and attach rustls/AWS-LC evidence from the supported builder separately. If the release builder does not have a FIPS provider/toolchain installed, record the expected fail-closed output instead. OpenSSL"
to = "localement et joignez separement la preuve rustls/AWS-LC du builder supporte. Si le builder de publication n'a pas de fournisseur ou toolchain FIPS installe, consignez plutot la sortie fail-closed attendue. OpenSSL"

[[phrase]]
from = "mode fails closed by default; set"
to = "echoue fail-closed par defaut ; definissez"

[[phrase]]
from = "only for explicit stub-only validation environments where provider evidence is intentionally not being collected."
to = "uniquement pour des environnements de validation explicitement stub-only ou la preuve fournisseur n'est volontairement pas collectee."

[[phrase]]
from = "Optional Common Criteria-Aligned Evidence"
to = "Preuve optionnelle alignee Common Criteria"

[[phrase]]
from = "For releases that changed security-enforcing behavior, complete the relevant sections of"
to = "Pour les publications qui modifient un comportement d'enforcement de securite, completez les sections pertinentes de"

[[phrase]]
from = "and record any relevant notes from"
to = "et consignez les notes pertinentes de"

[[phrase]]
from = "TOE boundary assumptions affected by the release."
to = "Hypotheses de frontiere TOE affectees par la publication."

[[phrase]]
from = "Security Target-style draft notes: security problem, objectives, and security-relevant interfaces affected by the release."
to = "Notes de brouillon de style Security Target : probleme de securite, objectifs et interfaces pertinentes pour la securite affectees par la publication."

[[phrase]]
from = "Security-relevant interfaces changed by the release."
to = "Interfaces pertinentes pour la securite modifiees par la publication."

[[phrase]]
from = "Validation scripts or pentest regressions that provide evidence."
to = "Scripts de validation ou regressions de pentest fournissant une preuve."

[[phrase]]
from = "External dependencies and operational-environment assumptions."
to = "Dependances externes et hypotheses d'environnement operationnel."

[[phrase]]
from = "Vulnerability-analysis records for pentest, CodeQL, audit, or internal findings, including the fixed/accepted/false-positive/deferred decision and remediation commit."
to = "Enregistrements d'analyse de vulnerabilite pour pentest, CodeQL, audit ou findings internes, incluant la decision fixed/accepted/false-positive/deferred et le commit de remediation."

[[phrase]]
from = "This is evidence organization only. Do not describe the release as Common Criteria certified, Protection Profile compliant, or EAL compliant."
to = "Il s'agit seulement d'organisation de preuves. Ne decrivez pas la publication comme certifiee Common Criteria, conforme Protection Profile ou conforme EAL."

[[phrase]]
from = "OWASP Baseline Evidence"
to = "Preuve de baseline OWASP"

[[phrase]]
from = "For releases that changed request parsing, TLS, authentication-adjacent controls, PHP-FPM handling, config validation, or observability, capture the mapped in-repo OWASP Top 10 2025 baseline:"
to = "Pour les publications qui modifient le parsing de requetes, TLS, les controles proches de l'authentification, la gestion PHP-FPM, la validation de configuration ou l'observabilite, capturez la baseline OWASP Top 10 2025 mappee dans le repository :"

[[phrase]]
from = "Record the script output as release evidence. This is an engineering baseline for Fluxheim-owned controls, not an OWASP compliance claim for applications served behind Fluxheim."
to = "Consignez la sortie du script comme preuve de publication. C'est une baseline d'ingenierie pour les controles possedes par Fluxheim, pas une revendication de conformite OWASP pour les applications servies derriere Fluxheim."

[[phrase]]
from = "5. Draft The GitHub Release"
to = "5. Rediger la publication GitHub"

[[phrase]]
from = "On GitHub:"
to = "Sur GitHub :"

[[phrase]]
from = "Open Releases."
to = "Ouvrez Releases."

[[phrase]]
from = "Draft a new release."
to = "Redigez une nouvelle release."

[[phrase]]
from = "Select the tag from"
to = "Selectionnez le tag depuis"

[[phrase]]
from = "as the release title."
to = "comme titre de publication."

[[phrase]]
from = "Paste the contents of"
to = "Collez le contenu de"

[[phrase]]
from = "Upload every runtime profile archive built in step 4:"
to = "Televersez chaque archive de profil runtime construite a l'etape 4 :"

[[phrase]]
from = "Upload the unified config-tester archive built in step 4:"
to = "Televersez l'archive config-tester unifiee construite a l'etape 4 :"

[[phrase]]
from = "If the release includes Level 1 macOS developer artifacts, upload"
to = "Si la publication inclut des artefacts developpeur macOS niveau 1, televersez"

[[phrase]]
from = "for the Mac targets that were actually built."
to = "pour les cibles Mac effectivement construites."

[[phrase]]
from = "Publish the release."
to = "Publiez la release."

[[phrase]]
from = "It is normal to publish before every evidence field is filled. Source archives and container digests are available only after the tag/release and image workflow exist."
to = "Il est normal de publier avant que chaque champ de preuve soit rempli. Les archives source et les digests de conteneur ne sont disponibles qu'apres l'existence du tag/de la release et du workflow d'image."

[[phrase]]
from = "6. Record Source Archive Checksums"
to = "6. Consigner les sommes de controle des archives source"

[[phrase]]
from = "After the tag is visible on GitHub, download GitHub's generated source archives and hash them:"
to = "Une fois le tag visible sur GitHub, telechargez les archives source generees par GitHub et hashez-les :"

[[phrase]]
from = "Edit the GitHub release notes and add these checksums."
to = "Modifiez les notes de publication GitHub et ajoutez ces sommes de controle."

[[phrase]]
from = "After the tag and image workflows are available, the maintainer helper can collect the release evidence block:"
to = "Une fois le tag et les workflows d'image disponibles, le helper mainteneur peut collecter le bloc de preuves de publication :"

[[phrase]]
from = "The helper includes OpenSSL and rustls/AWS-LC FIPS-capable evidence by running"
to = "Le helper inclut les preuves compatibles FIPS OpenSSL et rustls/AWS-LC en executant"

[[phrase]]
from = "when that backend's evidence is collected on another builder. Use"
to = "lorsque la preuve de ce backend est collectee sur un autre builder. Utilisez"

[[phrase]]
from = "only for release lines where that evidence is not relevant."
to = "uniquement pour les lignes de publication ou cette preuve n'est pas pertinente."

[[phrase]]
from = "7. Publish And Verify Container Images"
to = "7. Publier et verifier les images conteneur"

[[phrase]]
from = "The image workflow publishes the configured image variants after the tag push. Wait for the workflow to finish before collecting digests."
to = "Le workflow d'image publie les variantes d'image configurees apres le push du tag. Attendez la fin du workflow avant de collecter les digests."

[[phrase]]
from = "For GHCR, the package must be public if anonymous users should pull it:"
to = "Pour GHCR, le paquet doit etre public si des utilisateurs anonymes doivent pouvoir le pull :"

[[phrase]]
from = "Open the Fluxheim container package on GitHub."
to = "Ouvrez le paquet conteneur Fluxheim sur GitHub."

[[phrase]]
from = "Open Package settings."
to = "Ouvrez Package settings."

[[phrase]]
from = "Use Danger Zone -> Change visibility -> Public."
to = "Utilisez Danger Zone -> Change visibility -> Public."

[[phrase]]
from = "Then collect immutable digests:"
to = "Collectez ensuite les digests immuables :"

[[phrase]]
from = "If Docker Hub publishing is enabled, repeat the same pull/inspect process for the Docker Hub tags. If Quay publishing is enabled, repeat it for the Quay release tags as well:"
to = "Si la publication Docker Hub est activee, repetez le meme processus pull/inspect pour les tags Docker Hub. Si la publication Quay est activee, repetez-le aussi pour les tags de release Quay :"

[[phrase]]
from = "and newer tags, also collect the"
to = "et les tags plus recents, collectez aussi les"

[[phrase]]
from = "image profile digests, for example"
to = "digests de profil d'image, par exemple"

[[phrase]]
from = "Edit the GitHub release notes and add one digest per image variant."
to = "Modifiez les notes de publication GitHub et ajoutez un digest par variante d'image."

[[phrase]]
from = "8. Final Release Evidence Format"
to = "8. Format final des preuves de publication"

[[phrase]]
from = "The release notes should end with concrete evidence, not placeholders:"
to = "Les notes de publication doivent se terminer par des preuves concretes, pas des placeholders :"

[[phrase]]
from = "9. Post-Release Smoke"
to = "9. Smoke post-publication"

[[phrase]]
from = "Pull one published image and confirm the packaged default site starts:"
to = "Pullez une image publiee et confirmez que le site par defaut package demarre :"

[[phrase]]
from = "Expected result:"
to = "Resultat attendu :"

[[phrase]]
from = "HTTP status is"
to = "Le statut HTTP est"

[[phrase]]
from = "The response includes"
to = "La reponse inclut"

[[phrase]]
from = "Logs do not show startup errors."
to = "Les logs ne montrent pas d'erreurs de demarrage."

[[phrase]]
from = "10. Local Cleanup"
to = "10. Nettoyage local"

[[phrase]]
from = "Remove local release artifacts when no longer needed:"
to = "Supprimez les artefacts de publication locaux lorsqu'ils ne sont plus necessaires :"

[[phrase]]
from = "Keep the signed tag and GitHub release immutable unless a serious release mistake requires a documented replacement release."
to = "Gardez le tag signe et la release GitHub immuables, sauf si une erreur de publication grave exige une publication de remplacement documentee."

[[phrase]]
from = "OWASP Top 10 2025 baseline evidence by running"
to = "les preuves de baseline OWASP Top 10 2025 en executant"

[[phrase]]
from = "<p>Upload both SBOM files as release assets, and record their checksums in the release notes.</p>"
to = "<p>Televerser les deux fichiers SBOM comme assets de release et consigner leurs checksums dans les release notes.</p>"

[[phrase]]
from = "<li>Upload every runtime profile archive built in step 4: <code>dist/fluxheim-${RELEASE_VERSION}-{full,cache,proxy,load-balancer,php}-{x86_64,aarch64}-linux.tar.gz</code>.</li>"
to = "<li>Televerser chaque archive de profil runtime construite a l'etape 4 : <code>dist/fluxheim-${RELEASE_VERSION}-{full,cache,proxy,load-balancer,php}-{x86_64,aarch64}-linux.tar.gz</code>.</li>"

[[phrase]]
from = "<li>Upload the unified config-tester archive built in step 4: <code>dist/fluxheim-${RELEASE_VERSION}-config-tester-{x86_64,aarch64}-linux.tar.gz</code>.</li>"
to = "<li>Televerser l'archive config-tester unifiee construite a l'etape 4 : <code>dist/fluxheim-${RELEASE_VERSION}-config-tester-{x86_64,aarch64}-linux.tar.gz</code>.</li>"

[[phrase]]
from = "<li>Upload <code>target/release-evidence/fluxheim.spdx.json</code>.</li>"
to = "<li>Televerser <code>target/release-evidence/fluxheim.spdx.json</code>.</li>"

[[phrase]]
from = "<li>Upload <code>target/release-evidence/fluxheim.cyclonedx.json</code>.</li>"
to = "<li>Televerser <code>target/release-evidence/fluxheim.cyclonedx.json</code>.</li>"
