locale_id = "fr-FR"
html_lang = "fr-FR"

[[phrase]]
from = "Fluxheim pins Rust 1.96.0 in <code>rust-toolchain.toml</code> and <code>Cargo.toml</code>. The local toolchain and the container builder should stay on the same stable release."
to = "Fluxheim epingle Rust 1.96.0 dans <code>rust-toolchain.toml</code> et <code>Cargo.toml</code>. La toolchain locale et le builder conteneur doivent rester sur la meme release stable."

[[phrase]]
from = "Local Builds"
to = "Builds locaux"

[[phrase]]
from = "Native builds are the best option when the binary should be optimized for the current CPU:"
to = "Les builds natifs sont la meilleure option lorsque le binaire doit etre optimise pour le CPU courant :"

[[phrase]]
from = "Use <code>target-cpu=native</code> only for binaries that will run on the same CPU family they were built on. For portable release artifacts, omit the flag."
to = "Utilisez <code>target-cpu=native</code> uniquement pour les binaires qui tourneront sur la meme famille de CPU que celle du build. Pour des artefacts de release portables, omettez ce flag."

[[phrase]]
from = "Feature-reduced builds keep the binary small and reduce dependency surface:"
to = "Les builds a fonctionnalites reduites gardent le binaire petit et reduisent la surface de dependances :"

[[phrase]]
from = "TLS backends are mutually exclusive. Select exactly one of"
to = "Les backends TLS sont mutuellement exclusifs. Selectionnez exactement l'un de"

[[phrase]]
from = "System Build Dependencies"
to = "Dependances systeme de build"

[[phrase]]
from = "Fluxheim builds with Rust 1.96.0. The default feature set uses Pingora, Rustls, cache support, and static file serving. Native builds need a normal C/C++ toolchain plus a few build helpers for transitive native code."
to = "Fluxheim se build avec Rust 1.96.0. Le set de fonctionnalites par defaut utilise Pingora, Rustls, le support cache et le service de fichiers statiques. Les builds natifs necessitent une toolchain C/C++ normale plus quelques helpers de build pour le code natif transitif."

[[phrase]]
from = "Required for the default build:"
to = "Requis pour le build par defaut :"

[[phrase]]
from = "C and C++ compiler toolchain;"
to = "toolchain de compilation C et C++ ;"

[[phrase]]
from = "Package examples:"
to = "Exemples de paquets :"

[[phrase]]
from = "Optional backend-specific packages:"
to = "Paquets optionnels specifiques au backend :"

[[phrase]]
from = "Container builds install the same requirements in the builder stage. The runtime images only need CA certificates plus the Fluxheim binary and config."
to = "Les builds conteneur installent les memes exigences dans l'etape builder. Les images runtime n'ont besoin que des certificats CA, du binaire Fluxheim et de la configuration."

[[phrase]]
from = "Container Variants"
to = "Variantes de conteneurs"

[[phrase]]
from = "Fluxheim ships multiple runtime Containerfiles so operators can choose the base OS that fits their security and operations model."
to = "Fluxheim livre plusieurs Containerfiles runtime afin que les operateurs puissent choisir l'OS de base adapte a leur modele de securite et d'exploitation."

[[phrase]]
from = "<th>Variant</th>"
to = "<th>Variante</th>"

[[phrase]]
from = "Runtime base"
to = "Base runtime"

[[phrase]]
from = "Notes"
to = "Notes"

[[phrase]]
from = "Recommended minimal security-focused runtime."
to = "Runtime minimal recommande, oriente securite."

[[phrase]]
from = "Small musl-based runtime with broad availability."
to = "Petit runtime base sur musl avec large disponibilite."

[[phrase]]
from = "SUSE Micro runtime base aligned with Leap Micro-style deployments."
to = "Base runtime SUSE Micro alignee avec les deploiements de style Leap Micro."

[[phrase]]
from = "Conservative glibc runtime for broad compatibility."
to = "Runtime glibc conservateur pour une large compatibilite."

[[phrase]]
from = "This default works under both rootless and rootful container engines. Running a rootful engine does not require running Fluxheim as root inside the container."
to = "Ce defaut fonctionne avec les moteurs conteneur rootless et rootful. Executer un moteur rootful n'exige pas d'executer Fluxheim en root dans le conteneur."

[[phrase]]
from = "Build the default Debian image:"
to = "Builder l'image Debian par defaut :"

[[phrase]]
from = "Build a specific runtime variant:"
to = "Builder une variante runtime specifique :"

[[phrase]]
from = "Build the cache-focused profile locally:"
to = "Builder localement le profil oriente cache :"

[[phrase]]
from = "Build the proxy-focused profile locally:"
to = "Builder localement le profil oriente proxy :"

[[phrase]]
from = "Build a PHP-FPM-enabled web profile locally:"
to = "Builder localement un profil web avec PHP-FPM :"

[[phrase]]
from = "Build the load-balancer profile locally:"
to = "Builder localement le profil load balancer :"

[[phrase]]
from = "Build a smaller proxy-only binary:"
to = "Builder un binaire proxy-only plus petit :"

[[phrase]]
from = "Validate the bundled example config:"
to = "Valider la configuration d'exemple fournie :"

[[phrase]]
from = "Run the complete local smoke:"
to = "Executer le smoke local complet :"

[[phrase]]
from = "The smoke script builds the image, validates the packaged config, and confirms the runtime user is <code>65532</code>."
to = "Le script smoke build l'image, valide la config packagee et confirme que l'utilisateur runtime est <code>65532</code>."

[[phrase]]
from = "FreeBSD support should be treated as a native build target instead:"
to = "Le support FreeBSD doit plutot etre traite comme une cible de build native :"

[[phrase]]
from = "Publishing Images"
to = "Publication des images"

[[phrase]]
from = "Optional Docker Hub repository secrets:"
to = "Secrets optionnels du repository Docker Hub :"

[[phrase]]
from = "Optional Quay repository secrets and variables:"
to = "Secrets et variables optionnels du repository Quay :"

[[phrase]]
from = "Config Tester Release Asset"
to = "Asset de release Config Tester"

[[phrase]]
from = "The tester is built as a release asset and is not installed into normal RPMs or runtime images by default."
to = "Le tester est build comme asset de release et n'est pas installe par defaut dans les RPMs normaux ni les images runtime."

[[phrase]]
from = "Volume Mapping"
to = "Mapping des volumes"

[[phrase]]
from = "Fluxheim containers use a small set of stable paths. Mount host directories to these paths instead of writing inside the image layer."
to = "Les conteneurs Fluxheim utilisent un petit ensemble de chemins stables. Montez les repertoires hote sur ces chemins au lieu d'ecrire dans la couche de l'image."

[[phrase]]
from = "Container path"
to = "Chemin conteneur"

[[phrase]]
from = "Purpose"
to = "But"

[[phrase]]
from = "Mount mode"
to = "Mode de montage"

[[phrase]]
from = "Main config file."
to = "Fichier de configuration principal."

[[phrase]]
from = "Optional config directory."
to = "Repertoire de configuration optionnel."

[[phrase]]
from = "Static certificate chains and private keys."
to = "Chaines de certificats statiques et cles privees."

[[phrase]]
from = "Runtime state: ACME storage and future snapshots."
to = "Etat runtime : stockage ACME et futurs snapshots."

[[phrase]]
from = "Disk cache root."
to = "Racine du cache disque."

[[phrase]]
from = "Podman run example:"
to = "Exemple podman run :"

[[phrase]]
from = "Compose example:"
to = "Exemple Compose :"

[[phrase]]
from = "Container ACME First Issuance"
to = "Premiere emission ACME en conteneur"

[[phrase]]
from = "Native systemd"
to = "systemd natif"

[[phrase]]
from = "Codex And Rootless Podman"
to = "Codex et Podman rootless"

[[phrase]]
from = "When running Codex in a sandbox, include the rootless Podman runtime directories as writable roots and point Podman at the user socket:"
to = "Lorsque Codex tourne dans une sandbox, incluez les repertoires runtime Podman rootless comme racines accessibles en ecriture et pointez Podman vers le socket utilisateur :"

[[phrase]]
from = "Run rootless on an unprivileged port:"
to = "Executer rootless sur un port non privilegie :"

[[phrase]]
from = "For TLS, mount certificate files read-only and keep private keys owner-only on the host. Use the storage check before starting:"
to = "Pour TLS, montez les fichiers de certificats en lecture seule et gardez les cles privees accessibles seulement au proprietaire sur l'hote. Utilisez le controle de stockage avant le demarrage :"

[[phrase]]
from = "Privileged ports such as <code>80</code> and <code>443</code> require host-level setup for rootless containers. Prefer host port forwarding to container ports <code>8080</code> and <code>8443</code> unless the deployment environment already grants low-port binding safely."
to = "Les ports privilegies comme <code>80</code> et <code>443</code> exigent une configuration au niveau hote pour les conteneurs rootless. Preferez le forwarding de ports hote vers les ports conteneur <code>8080</code> et <code>8443</code>, sauf si l'environnement de deploiement autorise deja le binding de bas ports en securite."

[[phrase]]
from = "Fluxheim pins Rust 1.96.0 in"
to = "Fluxheim fige Rust 1.96.0 dans"

[[phrase]]
from = "Fluxheim&#x27;s production RPM intentionally compiles"
to = "Le RPM de production Fluxheim compile volontairement"

[[phrase]]
from = "Fluxheim's production RPM intentionally compiles"
to = "Le RPM de production Fluxheim compile volontairement"

[[phrase]]
from = "You can also override the user at runtime with the container engine&#x27;s"
to = "Vous pouvez aussi remplacer l'utilisateur au runtime avec l'option"

[[phrase]]
from = "binary target for diagnosing configs outside the runtime container. It is intended for cases where a Podman or systemd deployment cannot start and the operator still needs Fluxheim&#x27;s real config parser, feature/profile checks, TLS storage checks, ACME target preview, and upstream DNS checks."
to = "binaire pour diagnostiquer les configurations hors du conteneur runtime. Il est destine aux cas ou un deploiement Podman ou systemd ne peut pas demarrer et ou l'operateur a encore besoin du vrai parseur de config Fluxheim, des checks feature/profile, des checks de stockage TLS, de la preview des targets ACME et des checks DNS upstream."

[[phrase]]
from = "gives the bind mount a private container label. Add"
to = "donne au bind mount un label conteneur prive. Ajoutez"

[[phrase]]
from = "The default image user is"
to = "L'utilisateur image par defaut est"

[[phrase]]
from = "For multi-site setups, prefer"
to = "Pour les setups multi-site, preferez"

[[phrase]]
from = "with one vhost per file."
to = "avec un vhost par fichier."

[[phrase]]
from = "For managed ACME in containers, use the same bind mounts for the gateway and the one-shot renewal container. A complete example with an external"
to = "Pour ACME gere en conteneurs, utilisez les memes bind mounts pour le gateway et le conteneur de renouvellement one-shot. Un exemple complet avec un service externe"

[[phrase]]
from = "in this mode so the long-running gateway does not also run the background renewal loop."
to = "dans ce mode afin que le gateway longue duree n'execute pas aussi la boucle de renouvellement en arriere-plan."

[[phrase]]
from = "For container secret files, mount the files read-only. If the host tree is already labeled with"
to = "Pour les fichiers secrets de conteneur, montez les fichiers en lecture seule. Si l'arborescence hote est deja etiquetee avec"

[[phrase]]
from = "Validate the mounted config directly with"
to = "Validez la configuration montee directement avec"

[[phrase]]
from = ". This avoids compose-provider differences around one-shot commands:"
to = ". Cela evite les differences entre providers Compose autour des commandes one-shot :"

[[phrase]]
from = "Then start Fluxheim, replacing any existing gateway on the published ports. If you kept"
to = "Demarrez ensuite Fluxheim en remplacant tout gateway existant sur les ports publies. Si vous avez garde"

[[phrase]]
from = "Run due-only renewal from a second container. Missing certificate files are due targets, so first issuance does not require"
to = "Executez le renouvellement due-only depuis un second conteneur. Les fichiers de certificat manquants sont des targets dues, donc la premiere emission ne necessite pas"

[[phrase]]
from = "disabled for the first run, enable HTTPS in the main config after every configured ACME target has renewed:"
to = "desactive pour le premier run, activez HTTPS dans la configuration principale apres le renouvellement de chaque target ACME configuree :"

[[phrase]]
from = ". Avoid nested mountpoints below read-only image paths such as"
to = ". Evitez les mountpoints imbriques sous des chemins d'image en lecture seule comme"

[[phrase]]
from = "For services running on the host, use Podman&#x27;s host gateway name when available:"
to = "Pour les services tournant sur l'hote, utilisez le nom de gateway hote de Podman lorsqu'il est disponible :"

[[phrase]]
from = "The container config sets"
to = "La configuration conteneur definit"

[[phrase]]
from = "; keep the Podman stop timeout higher than the sum of those values so normal shutdown does not fall back to"
to = "; gardez le timeout d'arret Podman superieur a la somme de ces valeurs afin que l'arret normal ne retombe pas sur"

[[phrase]]
from = "builds use the package name"
to = "utilisent le nom de paquet"

[[phrase]]
from = "This helper is intended for installation testing on RPM-based hosts. The release-grade RPM source of truth remains"
to = "Ce helper est destine aux tests d'installation sur des hotes bases RPM. La source de verite RPM release-grade reste"

[[phrase]]
from = "Official Podman documentation describes the rootless API socket default as"
to = "La documentation officielle Podman decrit le socket API rootless par defaut comme"

[[phrase]]
from = "has precedence over configured service destinations for Podman remote connections."
to = "a priorite sur les destinations de service configurees pour les connexions Podman distantes."

[[phrase]]
from = "You can also override the user at runtime with the container engine's"
to = "Vous pouvez aussi remplacer l'utilisateur au runtime avec l'option"

[[phrase]]
from = "binary target for diagnosing configs outside the runtime container. It is intended for cases where a Podman or systemd deployment cannot start and the operator still needs Fluxheim's real config parser, feature/profile checks, TLS storage checks, ACME target preview, and upstream DNS checks."
to = "binaire pour diagnostiquer les configurations hors du conteneur runtime. Il est destine aux cas ou un deploiement Podman ou systemd ne peut pas demarrer et ou l'operateur a encore besoin du vrai parseur de config Fluxheim, des checks feature/profile, des checks de stockage TLS, de la preview des targets ACME et des checks DNS upstream."

[[phrase]]
from = "For services running on the host, use Podman's host gateway name when available:"
to = "Pour les services tournant sur l'hote, utilisez le nom de gateway hote de Podman lorsqu'il est disponible :"

[[phrase]]
from = "The workflow defaults to"
to = "Le workflow utilise par defaut"

[[phrase]]
from = ", once every selected runtime base has been verified for those architectures."
to = ", une fois que chaque base runtime selectionnee a ete verifiee pour ces architectures."

[[phrase]]
from = "starts a vhost, and each following"
to = "demarre un vhost, et chaque table suivante"

[[phrase]]
from = "after the main file. When Fluxheim starts from"
to = "apres le fichier principal. Lorsque Fluxheim demarre depuis"

[[phrase]]
from = ", it loads top-level TOML files first and then"
to = ", il charge d'abord les fichiers TOML de premier niveau puis"

[[phrase]]
from = "relabel suffix to individual secret-file mounts:"
to = "suffixe de relabel aux montages individuels de fichiers secrets :"

[[phrase]]
from = "for the first issuance step:"
to = "pour l'etape de premiere emission :"

[[phrase]]
from = "Those commands produce names shaped like"
to = "Ces commandes produisent des noms de la forme"

[[phrase]]
from = "package because both install the same binary, service, config, and content paths."
to = "car les deux installent les memes chemins de binaire, service, config et contenu."

[[phrase]]
from = "Privileged ports such as"
to = "Les ports privilegies comme"

[[phrase]]
from = "require host-level setup for rootless containers. Prefer host port forwarding to container ports"
to = "exigent une configuration au niveau hote pour les conteneurs rootless. Preferez le forwarding de ports hote vers les ports conteneur"

[[phrase]]
from = "unless the deployment environment already grants low-port binding safely."
to = "sauf si l'environnement de deploiement autorise deja le binding de bas ports en securite."

[[phrase]]
from = "</code> plus <code>"
to = "</code> et <code>"

[[phrase]]
from = "<h2 id=\"freebsd\">FreeBSD</h2>"
to = "<h2 id=\"freebsd\">FreeBSD</h2>"

[[phrase]]
from = "<p>Untagged <code>latest</code> builds use the package name <code>fluxheim-unstable</code> and a date version so they are clearly separated from stable release packages:</p>"
to = "<p>Les builds <code>latest</code> non tagges utilisent le nom de paquet <code>fluxheim-unstable</code> et une version datee afin d'etre clairement separes des paquets de release stables :</p>"
