locale_id = "fr-FR"
html_lang = "fr-FR"

[[phrase]]
from = "Feature Matrix — Fluxheim Source Docs"
to = "Matrice des features - Documentation source Fluxheim"

[[phrase]]
from = "Feature Matrix"
to = "Matrice des features"

[[phrase]]
from = "Fluxheim uses Cargo features for compile-time module selection. The default binary is intentionally useful but conservative:"
to = "Fluxheim utilise les features Cargo pour la selection de modules a la compilation. Le binaire par defaut est volontairement utile mais conservateur:"

[[phrase]]
from = "The validator expands profile aliases and rejects unsupported combinations before Cargo starts compiling Pingora."
to = "Le validateur etend les alias de profil et refuse les combinaisons non prises en charge avant que Cargo ne commence a compiler Pingora."

[[phrase]]
from = "Stable Core Features"
to = "Features core stables"

[[phrase]]
from = "Optional Implemented Features"
to = "Features optionnelles implementees"

[[phrase]]
from = "TLS Backends"
to = "Backends TLS"

[[phrase]]
from = "Cache Encryption Key Utility"
to = "Utilitaire de cle de chiffrement du cache"

[[phrase]]
from = "ACME Client Wiring"
to = "Cablage du client ACME"

[[phrase]]
from = "PHP Runtime Modules"
to = "Modules runtime PHP"

[[phrase]]
from = "Profile Aliases"
to = "Alias de profil"

[[phrase]]
from = "FIPS-Capable TLS Features"
to = "Features TLS compatibles FIPS"

[[phrase]]
from = "Incompatible Combinations"
to = "Combinaisons incompatibles"

[[phrase]]
from = "Planned Future Features"
to = "Features futures prevues"

[[phrase]]
from = "Pingora proxy runtime and upstream forwarding."
to = "Runtime proxy Pingora et forwarding upstream."

[[phrase]]
from = "Static file resolver and static response planning."
to = "Resolver de fichiers statiques et planification des reponses statiques."

[[phrase]]
from = "Image cache module. Runtime caching still requires config."
to = "Module de cache image. Le cache runtime exige toujours une configuration."

[[phrase]]
from = "Shared Pingora/Tokio ingress primitives used by proxy, TLS, and ACME-capable focused builds."
to = "Primitives d'ingress Pingora/Tokio partagees par les builds cibles proxy, TLS et compatibles ACME."

[[phrase]]
from = "rustls TLS backend."
to = "Backend TLS rustls."

[[phrase]]
from = "rustls/AWS-LC FIPS-capable TLS backend candidate."
to = "Candidat de backend TLS rustls/AWS-LC compatible FIPS."

[[phrase]]
from = "Compile-time security profile marker plus release hardening checks. Runtime enforcement lives in the concrete config, TLS, filesystem, admin, and request-handling modules."
to = "Marqueur de profil securite a la compilation plus controles de durcissement de release. L'application runtime vit dans les modules concrets de config, TLS, systeme de fichiers, admin et traitement des requetes."

[[phrase]]
from = "Fluxheim load-balancing support, health checks, and runtime pool policy."
to = "Support load-balancing Fluxheim, health checks et policy de pool runtime."

[[phrase]]
from = "Prometheus metrics listener."
to = "Listener de metriques Prometheus."

[[phrase]]
from = "Optional OTLP/HTTP JSON metrics export to a local Prometheus OTLP receiver or collector."
to = "Export optionnel des metriques OTLP/HTTP JSON vers un receiver OTLP Prometheus local ou un collector."

[[phrase]]
from = "Optional OTLP/HTTP JSON trace export to a local collector or Jaeger endpoint."
to = "Export optionnel des traces OTLP/HTTP JSON vers un collector local ou un endpoint Jaeger."

[[phrase]]
from = "ACME config, renewal planning, managed certificate/account paths, local HTTP-01 and rustls TLS-ALPN-01 challenge serving, and the renewal executor contract."
to = "Configuration ACME, planification du renouvellement, chemins geres de certificats/comptes, service local des challenges HTTP-01 et rustls TLS-ALPN-01, et contrat de l'executant de renouvellement."

[[phrase]]
from = "Live ACME account/order HTTP client and background renewal service."
to = "Client HTTP live pour comptes/orders ACME et service de renouvellement en arriere-plan."

[[phrase]]
from = "Shared response compression config and body-filter integration."
to = "Configuration partagee de compression de reponse et integration body-filter."

[[phrase]]
from = "Brotli response compression for eligible known-length responses."
to = "Compression de reponse Brotli pour les reponses eligibles a longueur connue."

[[phrase]]
from = "gzip response compression for eligible known-length responses."
to = "Compression de reponse gzip pour les reponses eligibles a longueur connue."

[[phrase]]
from = "Zstandard response compression for eligible known-length responses."
to = "Compression de reponse Zstandard pour les reponses eligibles a longueur connue."

[[phrase]]
from = "Local MMDB Geo-Context lookup for MaxMind GeoIP2/GeoLite2 and CIRCL Geo Open datasets, exposed to bounded access policy and structured logs."
to = "Lookup local de Geo-Context MMDB pour les jeux MaxMind GeoIP2/GeoLite2 et CIRCL Geo Open, expose a une policy d'acces bornee et aux logs structures."

[[phrase]]
from = "Raw L4 TCP stream proxy service with separate stream routes, source IP/CIDR allow/deny policy, DNS-rebinding guards for hostname upstreams, bounded idle/lifetime/byte caps, route-local PROXY protocol receive/send, stream upstream TLS/mTLS controls, and weighted/drain/backup policy."
to = "Service proxy de stream TCP L4 brut avec routes de stream separees, policy allow/deny IP/CIDR source, gardes DNS-rebinding pour upstreams par nom d'hote, limites idle/lifetime/octet bornees, reception/envoi PROXY protocol local a la route, controles TLS/mTLS upstream de stream et policy weighted/drain/backup."

[[phrase]]
from = "Zero-retention static/proxy build profile."
to = "Profil de build statique/proxy zero-retention."

[[phrase]]
from = "Internal marker for TLS-aware code; select a concrete backend for serving."
to = "Marqueur interne pour le code conscient de TLS; selectionnez un backend concret pour servir."

[[phrase]]
from = "Select at most one:"
to = "Selectionnez au plus un:"

[[phrase]]
from = "Default and recommended."
to = "Par defaut et recommande."

[[phrase]]
from = "Optional rustls/AWS-LC FIPS-capable candidate."
to = "Candidat optionnel rustls/AWS-LC compatible FIPS."

[[phrase]]
from = "ISO/IEC 19790 terminology alias for "
to = "Alias terminologique ISO/IEC 19790 pour "

[[phrase]]
from = "Optional OpenSSL backend."
to = "Backend OpenSSL optionnel."

[[phrase]]
from = "Cargo features are additive, and Fluxheim supports exactly one TLS backend per build. The feature validator catches conflicting TLS backend selections before build."
to = "Les features Cargo sont additives, et Fluxheim prend en charge exactement un backend TLS par build. Le validateur de features detecte les selections de backends TLS conflictuelles avant le build."

[[phrase]]
from = "BoringSSL and s2n-tls are no longer supported Fluxheim backends because their integrations did not provide the same complete policy, SNI, upstream TLS, and client-auth coverage as rustls and OpenSSL."
to = "BoringSSL et s2n-tls ne sont plus des backends Fluxheim pris en charge, car leurs integrations ne fournissaient pas la meme couverture complete de policy, SNI, TLS upstream et client-auth que rustls et OpenSSL."

[[phrase]]
from = " to validate the supported TLS backends on the current machine."
to = " pour valider les backends TLS pris en charge sur la machine actuelle."

[[phrase]]
from = " is available in every build profile. It prints one random 256-bit lowercase hex key suitable for "
to = " est disponible dans chaque profil de build. Il imprime une cle hex aleatoire de 256 bits en minuscules adaptee a "

[[phrase]]
from = ". The command does not write files itself; pipe it into the operator's preferred secret manager, systemd credential source, or container secret."
to = ". La commande n'ecrit pas de fichiers elle-meme; redirigez-la vers le gestionnaire de secrets prefere de l'operateur, une source de credentials systemd ou un secret de conteneur."

[[phrase]]
from = " contains the config, storage, challenge, certificate observation, and renewal-executor pieces. "
to = " contient les pieces de config, stockage, challenge, observation de certificat et renewal-executor. "

[[phrase]]
from = " adds the live ACME HTTP client stack used to load or create issuer accounts and complete HTTP-01 or rustls TLS-ALPN-01 orders through "
to = " ajoute la stack client HTTP ACME live utilisee pour charger ou creer des comptes issuer et completer les orders HTTP-01 ou rustls TLS-ALPN-01 via "

[[phrase]]
from = ", plus the runtime background renewal service. Keep "
to = ", plus le service de renouvellement runtime en arriere-plan. Gardez "

[[phrase]]
from = " enabled only in builds that perform certificate issuance or renewal."
to = " active seulement dans les builds qui effectuent l'emission ou le renouvellement de certificats."

[[phrase]]
from = " adds managed php-fpm process supervision as a runtime config mode inside this same "
to = " ajoute la supervision geree du processus php-fpm comme mode de config runtime dans cette meme feature "

[[phrase]]
from = " feature, not as a separate Cargo feature. It still uses the FastCGI bridge; only php-fpm process lifecycle changes from operator-managed to Fluxheim-supervised."
to = ", et non comme feature Cargo separee. Il utilise toujours le bridge FastCGI; seul le cycle de vie du processus php-fpm passe de gere par l'operateur a supervise par Fluxheim."

[[phrase]]
from = "Fluxheim no longer reserves a pure-Rust PHP/phprs runtime for "
to = "Fluxheim ne reserve plus de runtime PHP/phprs pur Rust pour "

[[phrase]]
from = ". Managed php-fpm is the supported zero-admin PHP direction."
to = ". php-fpm gere est la direction PHP zero-admin prise en charge."

[[phrase]]
from = "Cargo does not have a separate "
to = "Cargo n'a pas de flag "

[[phrase]]
from = " flag. Fluxheim provides normal Cargo feature aliases for common deployment shapes."
to = " separe. Fluxheim fournit des alias de features Cargo normaux pour les formes de deploiement courantes."

[[phrase]]
from = "Same intent as default."
to = "Meme intention que le defaut."

[[phrase]]
from = "Static sites without Fluxheim cache."
to = "Sites statiques sans cache Fluxheim."

[[phrase]]
from = "Reverse proxy without static hosting/cache."
to = "Reverse proxy sans hebergement statique ni cache."

[[phrase]]
from = "Static/proxy server with cache enabled."
to = "Serveur statique/proxy avec cache active."

[[phrase]]
from = "Edge server with Fluxheim load balancing and all compression codecs compiled in."
to = "Serveur edge avec load balancing Fluxheim et tous les codecs de compression compiles."

[[phrase]]
from = "Core server with Prometheus metrics, optional local OTLP metrics export, trace context propagation, and optional local OTLP trace export."
to = "Serveur core avec metriques Prometheus, export local optionnel des metriques OTLP, propagation du contexte de trace et export local optionnel des traces OTLP."

[[phrase]]
from = "All stable production modules, including GeoIP, traffic mirroring, and the current stream/load-balancer runtime lines."
to = "Tous les modules de production stables, y compris GeoIP, traffic mirroring et les lignes runtime stream/load-balancer actuelles."

[[phrase]]
from = "Broad development build with all compatible production modules."
to = "Build de developpement large avec tous les modules de production compatibles."

[[phrase]]
from = "Static webserver profile while serving still uses the shared proxy runtime."
to = "Profil webserver statique tandis que le service utilise encore la runtime proxy partagee."

[[phrase]]
from = "Cache edge without local static web serving."
to = "Edge de cache sans service web statique local."

[[phrase]]
from = "Focused reverse proxy edge."
to = "Edge reverse proxy cible."

[[phrase]]
from = "Load-balancer edge without cache or static web serving."
to = "Edge load-balancer sans cache ni service web statique."

[[phrase]]
from = "FIPS-capable OpenSSL proxy build that links to the operator-selected OpenSSL provider."
to = "Build proxy OpenSSL compatible FIPS lie au provider OpenSSL selectionne par l'operateur."

[[phrase]]
from = "FIPS-capable rustls/AWS-LC candidate build."
to = "Build candidat rustls/AWS-LC compatible FIPS."

[[phrase]]
from = "The remaining Pingora compatibility runtime is selected through "
to = "La runtime de compatibilite Pingora restante est selectionnee via "

[[phrase]]
from = "Examples that match the official release artifacts:"
to = "Exemples qui correspondent aux artefacts de release officiels:"

[[phrase]]
from = "The raw profile aliases do not force "
to = "Les alias de profil bruts ne forcent pas "

[[phrase]]
from = "; that is intentional for offline or static-certificate custom builds."
to = "; c'est intentionnel pour les builds custom offline ou a certificats statiques."

[[phrase]]
from = "Official RPMs, images, and release tarballs add "
to = "Les RPM, images et tarballs de release officiels ajoutent "

[[phrase]]
from = " to the full, cache, proxy, load-balancer, and PHP profiles by default."
to = " par defaut aux profils full, cache, proxy, load-balancer et PHP."

[[phrase]]
from = "Those profile aliases are deliberately narrow proof builds."
to = "Ces alias de profil sont volontairement des builds de preuve etroits."

[[phrase]]
from = "These combinations make Fluxheim's TLS boundary FIPS/ISO-capable when the operator provides the selected backend's validated provider evidence."
to = "Ces combinaisons rendent la frontiere TLS de Fluxheim compatible FIPS/ISO lorsque l'operateur fournit la preuve de provider validee du backend selectionne."

[[phrase]]
from = "They do not automatically make the whole deployment FIPS compliant:"
to = "Elles ne rendent pas automatiquement tout le deploiement conforme FIPS:"

[[phrase]]
from = "For the cleanest regulated boundary, prefer local/static certificates that were issued and renewed outside Fluxheim through an approved process."
to = "Pour la frontiere regulee la plus propre, preferez des certificats locaux/statiques emis et renouveles hors de Fluxheim via un processus approuve."

[[phrase]]
from = "Focused image profile status:"
to = "Statut des profils d'image cibles:"

[[phrase]]
from = "TLS and ACME are shared ingress capabilities, not implicit static webserver capabilities."
to = "TLS et ACME sont des capacites ingress partagees, pas des capacites implicites de webserver statique."

[[phrase]]
from = "Compatibility aliases may keep the older broad behavior, but published focused images are "
to = "Les alias de compatibilite peuvent conserver l'ancien comportement large, mais les images ciblees publiees sont "

[[phrase]]
from = "Pingora exposes one TLS backend at a time."
to = "Pingora expose un backend TLS a la fois."

[[phrase]]
from = "Zero-retention builds must not compile request/response cache code."
to = "Les builds zero-retention ne doivent pas compiler le code de cache requete/reponse."

[[phrase]]
from = "Zero-retention builds must not compile request metrics."
to = "Les builds zero-retention ne doivent pas compiler les metriques de requete."

[[phrase]]
from = "Zero-retention builds must not compile metrics export."
to = "Les builds zero-retention ne doivent pas compiler l'export de metriques."

[[phrase]]
from = "Zero-retention builds must not compile trace context propagation."
to = "Les builds zero-retention ne doivent pas compiler la propagation de contexte de trace."

[[phrase]]
from = "Zero-retention builds must not compile trace export."
to = "Les builds zero-retention ne doivent pas compiler l'export de traces."

[[phrase]]
from = "These are documented architecture tracks, not enabled Cargo features yet:"
to = "Ce sont des pistes d'architecture documentees, pas encore des features Cargo activees:"

[[phrase]]
from = "Programmable media edge"
to = "Edge media programmable"

[[phrase]]
from = "OpenTelemetry OTLP export"
to = "Export OTLP OpenTelemetry"

[[phrase]]
from = "WASM extensibility"
to = "Extensibilite WASM"

[[phrase]]
from = "Crypto RPC edge"
to = "Edge RPC crypto"

[[phrase]]
from = "PHP runtimes"
to = "Runtimes PHP"

[[phrase]]
from = "Perl CGI"
to = "Perl CGI"

[[phrase]]
from = "WireGuard smart load balancing"
to = "Load balancing intelligent WireGuard"

[[phrase]]
from = "propagation and access-log trace ID correlation."
to = "propagation et correlation d'ID de trace dans l'access-log."

[[phrase]]
from = "flag. Fluxheim provides normal Cargo feature aliases for common deployment shapes."
to = "flag. Fluxheim fournit des alias de features Cargo normaux pour les formes de deploiement courantes."

[[phrase]]
from = "adds release-gate dependency and compile proofs for"
to = "ajoute une dependance de release-gate et des preuves de compilation pour"

[[phrase]]
from = "; those builds do not compile Pingora crates, but they are not yet the production static-site runtime profile."
to = "; ces builds ne compilent pas les crates Pingora, mais ne sont pas encore le profil runtime static-site de production."

[[phrase]]
from = "is an opt-in OpenSSL 3 FIPS-provider proof path. It expands to"
to = "est un chemin de preuve opt-in de provider FIPS OpenSSL 3. Il s'etend a"

[[phrase]]
from = "and adds OpenSSL provider diagnostics plus default FIPS property enforcement for configurations that set"
to = "et ajoute les diagnostics du provider OpenSSL ainsi que l'application des proprietes FIPS par defaut pour les configurations qui definissent"

[[phrase]]
from = "feature, uses"
to = "feature, utilise"

[[phrase]]
from = "and requires CMake, Go, and a C compiler."
to = "et exige CMake, Go et un compilateur C."

[[phrase]]
from = "into a FIPS-capable TLS build for non-required configs, but"
to = "dans un build TLS compatible FIPS pour les configs non requises, mais"

[[phrase]]
from = "to see whether the binary can load or report the selected FIPS-capable provider in the current environment. The"
to = "pour voir si le binaire peut charger ou rapporter le provider compatible FIPS selectionne dans l'environnement courant. Les"

[[phrase]]
from = "before using this for regulated systems."
to = "avant d'utiliser cela pour des systemes regules."

[[phrase]]
from = "Compatibility aliases may keep the older broad behavior, but published focused images are"
to = "Les alias de compatibilite peuvent conserver l'ancien comportement large, mais les images ciblees publiees sont"

[[phrase]]
from = "is part of the default build, privacy builds must use"
to = "fait partie du build par defaut, les builds privacy doivent utiliser"

[[phrase]]
from = "The remaining Pingora compatibility runtime is selected through"
to = "Le runtime de compatibilite Pingora restant est selectionne via"

[[phrase]]
from = "The raw profile aliases do not force"
to = "Les alias de profil bruts n'imposent pas"

[[phrase]]
from = "profiles validate the same guard with"
to = "profils valident le meme garde avec"

[[phrase]]
from = "Future feature family"
to = "Famille de features future"

[[phrase]]
from = "<tr><td><code>ingress</code></td><td>Yes, through <code>proxy</code>/TLS profiles</td><td>Shared Pingora/Tokio ingress primitives used by proxy, TLS, and ACME-capable focused builds.</td></tr>"
to = "<tr><td><code>ingress</code></td><td>Oui, via les profils <code>proxy</code>/TLS</td><td>Primitives d'ingress Pingora/Tokio partagees utilisees par les builds cibles proxy, TLS et compatibles ACME.</td></tr>"

[[phrase]]
from = "<p><code>php-fpm</code> enables the <code>1.3.1</code> FastCGI bridge for PHP applications. It implies <code>proxy</code> and <code>web</code>, uses <code>fastcgi-client</code>, and must be selected explicitly:</p>"
to = "<p><code>php-fpm</code> active le bridge FastCGI <code>1.3.1</code> pour les applications PHP. Il implique <code>proxy</code> et <code>web</code>, utilise <code>fastcgi-client</code> et doit etre selectionne explicitement :</p>"

[[phrase]]
from = "<tr><td>Multiple <code>tls-*</code> backends</td><td>Pingora exposes one TLS backend at a time.</td></tr>"
to = "<tr><td>Plusieurs backends <code>tls-*</code></td><td>Pingora expose un seul backend TLS a la fois.</td></tr>"

[[phrase]]
from = "<p>Because <code>cache</code> is part of the default build, privacy builds must use <code>--no-default-features</code>.</p>"
to = "<p>Comme <code>cache</code> fait partie du build par defaut, les builds privacy doivent utiliser <code>--no-default-features</code>.</p>"

[[phrase]]
from = "<thead><tr><th>Future feature family</th><th>Document</th></tr></thead>"
to = "<thead><tr><th>Famille de features future</th><th>Document</th></tr></thead>"

[[phrase]]
from = "<tr><td><code>proxy</code></td><td>Yes</td><td>Pingora proxy runtime and upstream forwarding.</td></tr>"
to = "<tr><td><code>proxy</code></td><td>Oui</td><td>Runtime proxy Pingora et forwarding upstream.</td></tr>"

[[phrase]]
from = "<tr><td><code>web</code></td><td>Yes</td><td>Static file resolver and static response planning.</td></tr>"
to = "<tr><td><code>web</code></td><td>Oui</td><td>Resolver de fichiers statiques et planification de reponses statiques.</td></tr>"

[[phrase]]
from = "<tr><td><code>cache</code></td><td>Yes</td><td>Image cache module. Runtime caching still requires config.</td></tr>"
to = "<tr><td><code>cache</code></td><td>Oui</td><td>Module de cache d'images. Le caching runtime exige toujours une config.</td></tr>"

[[phrase]]
from = "<tr><td><code>tls-rustls</code></td><td>Yes</td><td>rustls TLS backend.</td></tr>"
to = "<tr><td><code>tls-rustls</code></td><td>Oui</td><td>Backend TLS rustls.</td></tr>"

[[phrase]]
from = "<tr><td><code>security</code></td><td>Yes</td><td>Compile-time security profile marker plus release hardening checks. Runtime enforcement lives in the concrete config, TLS, filesystem, admin, and request-handling modules.</td></tr>"
to = "<tr><td><code>security</code></td><td>Oui</td><td>Marqueur de profil securite compile-time et checks de durcissement de release. L'application runtime vit dans les modules concrets de config, TLS, filesystem, admin et traitement des requetes.</td></tr>"
