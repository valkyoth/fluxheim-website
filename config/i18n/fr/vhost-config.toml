locale_id = "fr-FR"
html_lang = "fr-FR"

[[phrase]]
from = "Fluxheim uses TOML array-of-tables syntax for virtual hosts:"
to = "Fluxheim utilise la syntaxe TOML array-of-tables pour les hôtes virtuels :"

[[phrase]]
from = "The important rule is:"
to = "La règle importante est :"

[[phrase]]
from = "starts a new vhost."
to = "démarre un nouveau vhost."

[[phrase]]
from = "starts the next vhost."
to = "démarre le vhost suivant."

[[phrase]]
from = "So in the example above,"
to = "Ainsi, dans l'exemple ci-dessus,"

[[phrase]]
from = "because it appears after the second"
to = "parce qu'il apparaît après le second"

[[phrase]]
from = "Recommended Layout"
to = "Disposition recommandée"

[[phrase]]
from = "For readable production configs, prefer split config directories and keep one vhost per file:"
to = "Pour des configurations de production lisibles, préférer des répertoires de configuration séparés et garder un vhost par fichier :"

[[phrase]]
from = "This avoids long files where it is hard to see which"
to = "Cela évite les longs fichiers où il est difficile de voir quelle"

[[phrase]]
from = "Common Mistakes"
to = "Erreurs fréquentes"

[[phrase]]
from = ". There is no current vhost yet."
to = ". Il n'y a pas encore de vhost courant."

[[phrase]]
from = "for the same vhost in the same file. TOML table headers are definitions, not append operations."
to = "pour le même vhost dans le même fichier. Les en-têtes de tables TOML sont des définitions, pas des opérations d'ajout."

[[phrase]]
from = "are normal nested tables inside one vhost, not arrays."
to = "sont des tables imbriquées normales dans un vhost, pas des arrays."

[[phrase]]
from = "for a vhost certificate. A vhost has one direct certificate pair under"
to = "pour un certificat de vhost. Un vhost possède une paire de certificats directe sous"

[[phrase]]
from = "Use global"
to = "Utiliser les entrées globales"

[[phrase]]
from = "entries when you need to configure additional listener certificates outside a specific vhost block. Use"
to = "lorsqu'il faut configurer des certificats de listener supplémentaires hors d'un bloc vhost spécifique. Utiliser"

[[phrase]]
from = "when Fluxheim should manage the vhost certificate."
to = "lorsque Fluxheim doit gérer le certificat du vhost."

[[phrase]]
from = "Proxy error pages are arrays because one proxy can define multiple fallback statuses. Define the array entry first, then its nested web root:"
to = "Les pages d'erreur proxy sont des arrays, car un proxy peut définir plusieurs statuts de fallback. Définir d'abord l'entrée d'array, puis son web root imbriqué :"

[[phrase]]
from = "The same rule applies inside route proxy blocks:"
to = "La même règle s'applique dans les blocs proxy de route :"

[[phrase]]
from = "When a vhost has both static files and a proxy, Fluxheim can use both policies: static files are served from"
to = "Lorsqu'un vhost a à la fois des fichiers statiques et un proxy, Fluxheim peut utiliser les deux politiques : les fichiers statiques sont servis depuis"

[[phrase]]
from = "when they match, and other requests can continue through"
to = "lorsqu'ils correspondent, et les autres requêtes peuvent continuer via"

[[phrase]]
from = "Routes Inside A Vhost"
to = "Routes dans un vhost"

[[phrase]]
from = "For gateway-style configs, add"
to = "Pour les configurations de type gateway, ajouter"

[[phrase]]
from = "under the current vhost. Routes are matched in this order:"
to = "sous le vhost courant. Les routes sont comparées dans cet ordre :"

[[phrase]]
from = "exact path;"
to = "chemin exact ;"

[[phrase]]
from = "longest path prefix;"
to = "plus long préfixe de chemin ;"

[[phrase]]
from = "one optional fallback route."
to = "une route de fallback optionnelle."

[[phrase]]
from = "Each route must define exactly one action by adding one nested table:"
to = "Chaque route doit définir exactement une action en ajoutant une table imbriquée :"

[[phrase]]
from = "block. There is no current route yet. Also do not use array syntax such as"
to = "bloc. Il n'y a pas encore de route courante. Ne pas utiliser non plus de syntaxe d'array comme"

[[phrase]]
from = "are normal nested tables inside one route, not arrays."
to = "sont des tables imbriquées normales dans une route, pas des arrays."

[[phrase]]
from = "rewrites the path for the selected route only. For example,"
to = "réécrit le chemin uniquement pour la route sélectionnée. Par exemple,"

[[phrase]]
from = "before it is sent to the route backend. When"
to = "avant son envoi au backend de route. Lorsque"

[[phrase]]
from = "is also set, the stripped suffix is attached to that upstream prefix instead, so"
to = "est également défini, le suffixe retiré est plutôt attaché à ce préfixe upstream, donc"

[[phrase]]
from = "must be paired with"
to = "doit être associé à"

[[phrase]]
from = "and must be an absolute safe path."
to = "et doit être un chemin absolu sûr."

[[phrase]]
from = "can be set on a vhost for a host-wide upload limit, or on a route to override both the vhost and global request body limit for the selected route. Route proxy actions can also set"
to = "peut être défini sur un vhost pour une limite d'upload à l'échelle de l'hôte, ou sur une route pour remplacer à la fois la limite du vhost et la limite globale de corps de requête pour la route sélectionnée. Les actions proxy de route peuvent aussi définir"

[[phrase]]
from = "for long-lived uploads or websocket paths without changing every backend on the vhost."
to = "pour les uploads longue durée ou les chemins websocket sans modifier chaque backend du vhost."

[[phrase]]
from = "Fluxheim preserves downstream"
to = "Fluxheim préserve les en-têtes de requête downstream"

[[phrase]]
from = "request headers by default, so websocket-style upgrade routes usually only need prefix matching, optional"
to = "par défaut, de sorte que les routes d'upgrade de type websocket n'ont généralement besoin que d'un matching par préfixe,"

[[phrase]]
from = ", and longer read/send timeouts. Header policy can still remove or replace those headers when an operator intentionally wants to disable upgrade tunneling for a route."
to = "et de timeouts de lecture/envoi plus longs. La politique d'en-têtes peut toujours supprimer ou remplacer ces en-têtes lorsqu'un opérateur veut désactiver volontairement le tunneling d'upgrade pour une route."

[[phrase]]
from = "When global HTTP-to-HTTPS redirect is enabled, proxy and static routes are also redirected by default on cleartext requests. Use the vhost ACME challenge helper for normal HTTP-01 challenge forwarding:"
to = "Lorsque la redirection globale HTTP vers HTTPS est activée, les routes proxy et statiques sont aussi redirigées par défaut sur les requêtes en clair. Utiliser l'aide de challenge ACME du vhost pour le forwarding HTTP-01 normal :"

[[phrase]]
from = "That helper creates the standard"
to = "Cette aide crée la route standard"

[[phrase]]
from = "route and marks only that route as cleartext-exempt. Advanced configs can still set"
to = "et marque seulement cette route comme exemptée du clair. Les configurations avancées peuvent toujours définir"

[[phrase]]
from = "on an explicit route for other rare exceptions."
to = "sur une route explicite pour d'autres rares exceptions."

[[phrase]]
from = "Static route actions can serve repository-style aliases with optional directory listing. Directory listing is disabled by default, index files are preferred when present, dotfiles remain hidden when"
to = "Les actions de route statique peuvent servir des alias de type dépôt avec listing de répertoire optionnel. Le listing de répertoire est désactivé par défaut, les fichiers d'index sont préférés lorsqu'ils existent, les dotfiles restent cachés lorsque"

[[phrase]]
from = ", and symlink entries are skipped."
to = ", et les entrées de symlink sont ignorées."

[[phrase]]
from = "Route-specific header policy is nested under the route:"
to = "La politique d'en-têtes propre à la route est imbriquée sous la route :"

[[phrase]]
from = "Route request headers support the same safe dynamic values as global request headers, including"
to = "Les en-têtes de requête de route prennent en charge les mêmes valeurs dynamiques sûres que les en-têtes globaux, notamment"

[[phrase]]
from = ". Unknown variables are config errors."
to = ". Les variables inconnues sont des erreurs de configuration."

[[phrase]]
from = "Use the normal user-friendly header operations when possible:"
to = "Utiliser les opérations d'en-têtes normales et conviviales lorsque c'est possible :"

[[phrase]]
from = "<li>Every <code>[vhosts.*]</code> table after it belongs to that current vhost.</li>"
to = "<li>Chaque table <code>[vhosts.*]</code> qui suit appartient a ce vhost courant.</li>"

[[phrase]]
from = "<p>Do not write <code>[vhosts.proxy]</code> before the first <code>[[vhosts]]</code>. There is no current vhost yet.</p>"
to = "<p>Ne pas ecrire <code>[vhosts.proxy]</code> avant le premier <code>[[vhosts]]</code>. Il n'y a pas encore de vhost courant.</p>"

[[phrase]]
from = "<p>Do not repeat <code>[vhosts.proxy]</code> for the same vhost in the same file. TOML table headers are definitions, not append operations.</p>"
to = "<p>Ne pas repeter <code>[vhosts.proxy]</code> pour le meme vhost dans le meme fichier. Les en-tetes de table TOML sont des definitions, pas des operations d'ajout.</p>"

[[phrase]]
from = "<p>Do not use <code>[[vhosts.proxy]]</code>. <code>proxy</code>, <code>web</code>, <code>cache</code>, <code>tls</code>, and <code>headers</code> are normal nested tables inside one vhost, not arrays.</p>"
to = "<p>Ne pas utiliser <code>[[vhosts.proxy]]</code>. <code>proxy</code>, <code>web</code>, <code>cache</code>, <code>tls</code> et <code>headers</code> sont des tables imbriquees normales dans un vhost, pas des arrays.</p>"

[[phrase]]
from = "<p>Do not use <code>[[vhosts.tls.certificates]]</code> for a vhost certificate. A vhost has one direct certificate pair under <code>[vhosts.tls.certificate]</code>:</p>"
to = "<p>Ne pas utiliser <code>[[vhosts.tls.certificates]]</code> pour un certificat de vhost. Un vhost a une paire de certificats directe sous <code>[vhosts.tls.certificate]</code> :</p>"

[[phrase]]
from = "</code> becomes <code>"
to = "</code> devient <code>"
