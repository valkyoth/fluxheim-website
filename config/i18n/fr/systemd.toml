locale_id = "fr-FR"
html_lang = "fr-FR"

[[phrase]]
from = "systemd Deployment"
to = "Déploiement systemd"

[[phrase]]
from = "Fluxheim can run as a native systemd service when you manually compile the binary or install an RPM package."
to = "Fluxheim peut tourner comme service systemd natif lorsque vous compilez manuellement le binaire ou installez un paquet RPM."

[[phrase]]
from = "The packaged unit is intentionally conservative:"
to = "L'unit packagee est volontairement conservatrice :"

[[phrase]]
from = "runs as the"
to = "tourne comme"

[[phrase]]
from = "user and group;"
to = "utilisateur et groupe ;"

[[phrase]]
from = "validates the config before starting;"
to = "valide la config avant le demarrage ;"

[[phrase]]
from = "uses <code>/run/fluxheim</code>, <code>/var/lib/fluxheim</code>, <code>/var/cache/fluxheim</code>, and <code>/var/log/fluxheim</code> as writable service paths;"
to = "utilise <code>/run/fluxheim</code>, <code>/var/lib/fluxheim</code>, <code>/var/cache/fluxheim</code> et <code>/var/log/fluxheim</code> comme chemins service writable ;"

[[phrase]]
from = "keeps <code>/etc/fluxheim</code> and <code>/srv/fluxheim</code> readable but not writable by the service;"
to = "garde <code>/etc/fluxheim</code> et <code>/srv/fluxheim</code> lisibles mais non writable par le service ;"

[[phrase]]
from = "runs with"
to = "tourne avec"

[[phrase]]
from = "and grants only"
to = "et n'accorde que"

[[phrase]]
from = ", allowing the unprivileged"
to = ", permettant a l'utilisateur non privilegie"

[[phrase]]
from = "user to bind production ports"
to = "de binder les ports de production"

[[phrase]]
from = "without running the service as root;"
to = "sans executer le service en root ;"

[[phrase]]
from = "uses strict system path protection, private temporary and device namespaces, kernel/control-group write protection, namespace restrictions, native syscall architecture filtering, and a conservative system-service/network syscall allow-list;"
to = "utilise une protection stricte des chemins systeme, des namespaces temporaires et device prives, une protection d'ecriture kernel/control-group, des restrictions de namespace, un filtrage d'architecture syscall native et une allow-list conservative de syscalls system-service/reseau ;"

[[phrase]]
from = "limits address families to IPv4, IPv6, and Unix domain sockets;"
to = "limite les familles d'adresses a IPv4, IPv6 et sockets Unix domain ;"

[[phrase]]
from = "stops with"
to = "s'arrete avec"

[[phrase]]
from = "and lets Fluxheim/Pingora shut down gracefully."
to = "et laisse Fluxheim/Pingora s'arreter proprement."

[[phrase]]
from = "These systemd controls are the supported"
to = "Ces controles systemd sont la sandbox hote"

[[phrase]]
from = "host sandbox. They are deployment-level controls and do not require a special Fluxheim binary."
to = "supportee. Ce sont des controles de niveau deploiement et ils ne requierent pas de binaire Fluxheim special."

[[phrase]]
from = "The packaged native config listens on"
to = "La config native packagee ecoute sur"

[[phrase]]
from = "by default. HTTPS uses"
to = "par defaut. HTTPS utilise"

[[phrase]]
from = "once <code>server.tls_listen</code> and certificate paths are enabled."
to = "une fois que <code>server.tls_listen</code> et les chemins de certificats sont actives."

[[phrase]]
from = "This matches normal bare-metal web server expectations while keeping the process unprivileged."
to = "Cela correspond aux attentes normales d'un serveur web bare-metal tout en gardant le processus non privilegie."

[[phrase]]
from = "Manual Binary Install"
to = "Installation binaire manuelle"

[[phrase]]
from = "Build Fluxheim:"
to = "Builder Fluxheim :"

[[phrase]]
from = "Install the binary where the provided unit expects it:"
to = "Installer le binaire la ou l'unit fournie l'attend :"

[[phrase]]
from = "Install the service user, runtime directories, default config, and default static page:"
to = "Installer l'utilisateur service, les repertoires runtime, la config par defaut et la page statique par defaut :"

[[phrase]]
from = "The prepare script is intentionally path-restricted. Any path override must be absolute, must not pass through a symlinked existing directory, and must stay below one of Fluxheim&#x27;s standard native roots:"
to = "Le script prepare est volontairement restreint par chemins. Tout override de chemin doit etre absolu, ne doit pas passer par un repertoire existant symlinked, et doit rester sous l'une des racines natives standard de Fluxheim :"

[[phrase]]
from = "The prepare script is intentionally path-restricted. Any path override must be absolute, must not pass through a symlinked existing directory, and must stay below one of Fluxheim's standard native roots:"
to = "Le script prepare est volontairement restreint par chemins. Tout override de chemin doit etre absolu, ne doit pas passer par un repertoire existant symlinked, et doit rester sous l'une des racines natives standard de Fluxheim :"

[[phrase]]
from = "Install the systemd unit and optional environment file:"
to = "Installer l'unit systemd et le fichier d'environnement optionnel :"

[[phrase]]
from = "Validate before starting:"
to = "Valider avant demarrage :"

[[phrase]]
from = "Start and enable:"
to = "Demarrer et activer :"

[[phrase]]
from = "View logs:"
to = "Voir les logs :"

[[phrase]]
from = "Config Path Override"
to = "Override du chemin de config"

[[phrase]]
from = "The unit defaults to:"
to = "L'unit utilise par defaut :"

[[phrase]]
from = "To use another config, edit"
to = "Pour utiliser une autre config, editez"

[[phrase]]
from = "Then reload systemd and restart:"
to = "Puis rechargez systemd et redemarrez :"

[[phrase]]
from = "Reload And Restart"
to = "Reload et restart"

[[phrase]]
from = "treat native service changes as validate-then-restart unless a specific runtime reload path is documented for the setting you changed:"
to = "traitez les changements du service natif comme validate-then-restart sauf si un chemin de reload runtime specifique est documente pour le setting modifie :"

[[phrase]]
from = "Fluxheim exits on"
to = "Fluxheim quitte sur"

[[phrase]]
from = "; the unit uses"
to = "; l'unit utilise"

[[phrase]]
from = "so the process has time to drain and shut down cleanly before systemd escalates."
to = "afin que le processus ait le temps de drainer et s'arreter proprement avant escalation par systemd."

[[phrase]]
from = "ACME Timer"
to = "Timer ACME"

[[phrase]]
from = "RPM packages install a one-shot renewal unit and timer:"
to = "Les paquets RPM installent une unit et un timer de renouvellement one-shot :"

[[phrase]]
from = "The service runs"
to = "Le service execute"

[[phrase]]
from = "as the same"
to = "comme le meme"

[[phrase]]
from = "user, with the same runtime/state/cache/log directories as the web service. It does not bind ports and does not receive"
to = "utilisateur, avec les memes repertoires runtime/state/cache/log que le service web. Il ne bind pas de ports et ne recoit pas"

[[phrase]]
from = ". After successful renewal it requests live certificate activation through"
to = ". Apres renouvellement reussi, il demande l'activation live du certificat via"

[[phrase]]
from = "when using the timer so the main webserver does not also run the background renewal loop."
to = "lorsque vous utilisez le timer afin que le serveur web principal n'execute pas aussi la boucle de renouvellement en arriere-plan."

[[phrase]]
from = "For issuers with External Account Binding, install the ACME credential drop-in for the ACME unit and use credential names in the TOML:"
to = "Pour les issuers avec External Account Binding, installez le drop-in de credentials ACME pour l'unit ACME et utilisez les noms de credentials dans le TOML :"

[[phrase]]
from = "Enable scheduled renewal:"
to = "Activer le renouvellement planifie :"

[[phrase]]
from = "Sandbox Overrides"
to = "Overrides de sandbox"

[[phrase]]
from = "The default unit is strict enough for normal static/proxy deployments. If a deployment needs extra host access, prefer a local drop-in instead of editing the packaged unit:"
to = "L'unit par defaut est assez stricte pour les deploiements statiques/proxy normaux. Si un deploiement requiert un acces hote supplementaire, preferez un drop-in local au lieu de modifier l'unit packagee :"

[[phrase]]
from = "For example, add another read-only content root:"
to = "Par exemple, ajoutez une autre racine de contenu read-only :"

[[phrase]]
from = "Then validate and restart:"
to = "Puis validez et redemarrez :"

[[phrase]]
from = "TLS And Content Paths"
to = "Chemins TLS et contenu"

[[phrase]]
from = "The default native paths are:"
to = "Les chemins natifs par defaut sont :"

[[phrase]]
from = "<th>Path</th>"
to = "<th>Chemin</th>"

[[phrase]]
from = "<th>Purpose</th>"
to = "<th>But</th>"

[[phrase]]
from = "Main config."
to = "Config principale."

[[phrase]]
from = "Optional split config directory."
to = "Repertoire de config split optionnel."

[[phrase]]
from = "Static certificate chains and private keys."
to = "Chaines de certificats statiques et cles privees."

[[phrase]]
from = "Default static site root."
to = "Racine de site statique par defaut."

[[phrase]]
from = "State and future ACME/snapshot storage."
to = "Etat et futur stockage ACME/snapshots."

[[phrase]]
from = "Cache storage."
to = "Stockage cache."

[[phrase]]
from = "Optional file logs."
to = "Logs fichier optionnels."

[[phrase]]
from = "PID file, upgrade socket, and certificate reload socket."
to = "Fichier PID, socket d'upgrade et socket de reload certificat."

[[phrase]]
from = "Keep private keys mode"
to = "Gardez le mode des cles privees"

[[phrase]]
from = "or stricter and owned by the runtime user when Fluxheim reads them directly:"
to = "ou plus strict et avec ownership par l'utilisateur runtime lorsque Fluxheim les lit directement :"
