locale_id = "de-DE"
html_lang = "de-DE"

[[phrase]]
from = "as the core instrumentation API."
to = "als zentrale Instrumentierungs-API."

[[phrase]]
from = "Emit production logs as JSON through serde-backed formatting."
to = "Produktionslogs als JSON über serde-gestützte Formatierung ausgeben."

[[phrase]]
from = "Keep stdout available as the default sink for systemd, Podman, and local debugging."
to = "stdout als Standard-Sink für systemd, Podman und lokales Debugging verfügbar halten."

[[phrase]]
from = "Avoid disk or network writes on request worker hot paths."
to = "Festplatten- oder Netzwerk-Schreibvorgänge auf heißen Request-Worker-Pfaden vermeiden."

[[phrase]]
from = "Add remote logging only behind explicit config and optional compile features."
to = "Remote-Logging nur hinter expliziter Konfiguration und optionalen Compile-Features hinzufügen."

[[phrase]]
from = "Make overflow behavior explicit instead of pretending logs are always both non-blocking and lossless."
to = "Überlaufverhalten explizit machen, statt vorzugeben, Logs seien immer zugleich nicht blockierend und verlustfrei."

[[phrase]]
from = "Log Classes"
to = "Log-Klassen"

[[phrase]]
from = "Fluxheim should split logs by intent:"
to = "Fluxheim sollte Logs nach Absicht aufteilen:"

[[phrase]]
from = ": one event per completed request."
to = ": ein Ereignis pro abgeschlossenem Request."

[[phrase]]
from = ": internal failures and unexpected runtime errors."
to = ": interne Fehler und unerwartete Laufzeitfehler."

[[phrase]]
from = ": rejected requests, request-smuggling attempts, admin auth failures, path traversal, denied PHP/CGI execution, and TLS/cert permission failures."
to = ": abgelehnte Requests, Request-Smuggling-Versuche, Admin-Auth-Fehler, Path Traversal, verweigerte PHP/CGI-Ausführung und TLS-/Zertifikats-Berechtigungsfehler."

[[phrase]]
from = "Event Fields"
to = "Ereignisfelder"

[[phrase]]
from = "Access events should include:"
to = "Access-Ereignisse sollten enthalten:"

[[phrase]]
from = "target/module"
to = "Ziel/Modul"

[[phrase]]
from = "query_present"
to = "query_present"

[[phrase]]
from = "latency_ms"
to = "latency_ms"

[[phrase]]
from = "remote_addr"
to = "remote_addr"

[[phrase]]
from = "user_agent, length-capped"
to = "user_agent, längenbegrenzt"

[[phrase]]
from = "referer, length-capped"
to = "referer, längenbegrenzt"

[[phrase]]
from = "upstream, when proxied"
to = "Upstream, wenn proxied"

[[phrase]]
from = "Pipeline"
to = "Pipeline"

[[phrase]]
from = "The desired pipeline has three stages:"
to = "Die gewünschte Pipeline hat drei Stufen:"

[[phrase]]
from = "Capture: request path emits"
to = "Capture: Der Request-Pfad emittiert"

[[phrase]]
from = "events with structured fields."
to = "Ereignisse mit strukturierten Feldern."

[[phrase]]
from = "Buffer: events enter a bounded async queue."
to = "Buffer: Ereignisse gelangen in eine begrenzte asynchrone Queue."

[[phrase]]
from = "Dispatch: a background dispatcher writes to configured sinks."
to = "Dispatch: Ein Hintergrund-Dispatcher schreibt in konfigurierte Sinks."

[[phrase]]
from = "The request worker must never perform slow remote network logging directly."
to = "Der Request-Worker darf langsames Remote-Netzwerk-Logging niemals direkt ausführen."

[[phrase]]
from = "Overflow Policy"
to = "Überlauf-Policy"

[[phrase]]
from = "Fluxheim must expose an explicit overflow policy:"
to = "Fluxheim muss eine explizite Überlauf-Policy bereitstellen:"

[[phrase]]
from = ": preserves logs but can slow request handling."
to = ": bewahrt Logs, kann aber die Request-Verarbeitung verlangsamen."

[[phrase]]
from = ": writes to a bounded durable disk queue; best durability but more operational complexity."
to = ": schreibt in eine begrenzte dauerhafte Disk-Queue; beste Haltbarkeit, aber mehr Betriebsaufwand."

[[phrase]]
from = ", Fluxheim must not claim zero data loss. With"
to = ", darf Fluxheim keinen Datenverlust von null behaupten. Mit"

[[phrase]]
from = ", Fluxheim must not claim zero request latency impact."
to = ", darf Fluxheim keine Auswirkung von null auf die Request-Latenz behaupten."

[[phrase]]
from = "Sinks"
to = "Sinks"

[[phrase]]
from = "Initial sinks:"
to = "Initiale Sinks:"

[[phrase]]
from = ": default."
to = ": Standard."

[[phrase]]
from = ": optional structured local file."
to = ": optionale strukturierte lokale Datei."

[[phrase]]
from = ": future production sink for Vector, Logstash, or similar."
to = ": künftiger Produktions-Sink für Vector, Logstash oder Ähnliches."

[[phrase]]
from = "Remote sinks should be optional compile-time support, for example"
to = "Remote-Sinks sollten optionale Compile-Zeit-Unterstützung sein, zum Beispiel"

[[phrase]]
from = ". Durable disk queue support should be behind"
to = ". Unterstützung für dauerhafte Disk-Queues sollte hinter"

[[phrase]]
from = "Circuit Breaker"
to = "Circuit Breaker"

[[phrase]]
from = "Remote dispatch should use a circuit breaker:"
to = "Remote-Dispatch sollte einen Circuit Breaker verwenden:"

[[phrase]]
from = ": send to remote sink."
to = ": an den Remote-Sink senden."

[[phrase]]
from = ": remote failed; write to stdout or spool."
to = ": Remote fehlgeschlagen; nach stdout oder in den Spool schreiben."

[[phrase]]
from = ": attempt periodic reconnect/heartbeat."
to = ": periodischen Reconnect/Heartbeat versuchen."

[[phrase]]
from = ", admin bearer tokens, ACME EAB values, API keys, and configured sensitive headers/fields."
to = ", Admin-Bearer-Tokens, ACME-EAB-Werte, API-Schlüssel und konfigurierte sensible Header/Felder."

[[phrase]]
from = "Never log request bodies by default."
to = "Request-Bodies standardmäßig niemals loggen."

[[phrase]]
from = "Length-cap paths, headers, error messages, and upstream error text."
to = "Pfade, Header, Fehlermeldungen und Upstream-Fehlertext in der Länge begrenzen."

[[phrase]]
from = "Mark fallback source, for example"
to = "Fallback-Quelle markieren, zum Beispiel"

[[phrase]]
from = "Protect local log files and spool directories with owner-only permissions where supported."
to = "Lokale Logdateien und Spool-Verzeichnisse, wo unterstützt, mit Owner-only-Berechtigungen schützen."

[[phrase]]
from = "Config Shape"
to = "Konfigurationsform"

[[phrase]]
from = "Initial target:"
to = "Initiales Ziel:"

[[phrase]]
from = "Implemented baseline:"
to = "Implementierte Baseline:"

[[phrase]]
from = "controls a compact JSON access event emitted from Pingora's"
to = "steuert ein kompaktes JSON-Access-Ereignis, das aus Pingoras"

[[phrase]]
from = "or generates one and passes it upstream before normal header policy mutations run. This baseline uses the existing"
to = "oder erzeugt eine und gibt sie upstream weiter, bevor normale Header-Policy-Mutationen laufen. Diese Baseline verwendet den bestehenden"

[[phrase]]
from = "stack. stderr/stdout and a disabled-by-default"
to = "Stack. stderr/stdout und ein standardmäßig deaktivierter"

[[phrase]]
from = "sink are implemented; the async dispatcher and remote sinks remain future work."
to = "Sink sind implementiert; der asynchrone Dispatcher und Remote-Sinks bleiben künftige Arbeit."

[[phrase]]
from = "are applied at startup through"
to = "werden beim Start über"

[[phrase]]
from = "can still override the level for local debugging. Changing either setting after startup currently requires a process upgrade because the logger is initialized once. JSON format emits structured internal runtime records, while access events remain direct JSON access records."
to = "kann das Level für lokales Debugging weiterhin überschreiben. Eine Änderung einer der beiden Einstellungen nach dem Start erfordert derzeit ein Prozess-Upgrade, weil der Logger einmalig initialisiert wird. Das JSON-Format emittiert strukturierte interne Laufzeitdatensätze, während Access-Ereignisse direkte JSON-Access-Datensätze bleiben."

[[phrase]]
from = "Implementation Stages"
to = "Implementierungsstufen"

[[phrase]]
from = "Add typed access-log config, request IDs, and baseline JSON access events through"
to = "Typisierte Access-Log-Konfiguration, Request-IDs und Baseline-JSON-Access-Ereignisse hinzufügen über"

[[phrase]]
from = ", JSON stdout formatting, and bridge existing"
to = ", JSON-stdout-Formatierung hinzufügen und bestehende"

[[phrase]]
from = "Add bounded dispatcher queue with"
to = "Begrenzte Dispatcher-Queue hinzufügen mit"

[[phrase]]
from = "Add file sink and protected local spool directory."
to = "File-Sink und geschütztes lokales Spool-Verzeichnis hinzufügen."

[[phrase]]
from = "Add remote TCP/TLS sink with circuit breaker and fallback."
to = "Remote-TCP/TLS-Sink mit Circuit Breaker und Fallback hinzufügen."

[[phrase]]
from = "Expose log health through Prometheus/admin status: dropped logs, spooled logs, remote failures, current circuit state, and reconnect attempts."
to = "Log-Health über Prometheus/Admin-Status bereitstellen: verworfene Logs, gespulte Logs, Remote-Fehler, aktueller Circuit-Zustand und Reconnect-Versuche."

[[phrase]]
from = "Required tests:"
to = "Erforderliche Tests:"

[[phrase]]
from = "JSON output contains expected request fields."
to = "JSON-Ausgabe enthält die erwarteten Request-Felder."

[[phrase]]
from = "Secret fields are redacted."
to = "Geheime Felder werden redigiert."

[[phrase]]
from = "Long fields are capped."
to = "Lange Felder werden begrenzt."

[[phrase]]
from = "Queue overflow follows configured policy."
to = "Queue-Überlauf folgt der konfigurierten Policy."

[[phrase]]
from = "Remote failure falls back to stdout/spool."
to = "Remote-Fehler fallen auf stdout/spool zurück."

[[phrase]]
from = "Circuit breaker trips and recovers."
to = "Circuit Breaker löst aus und erholt sich."

[[phrase]]
from = "File/spool path validation rejects insecure paths where possible."
to = "File-/Spool-Pfadvalidierung lehnt unsichere Pfade ab, wo möglich."

[[phrase]]
from = "<li>timestamp</li>"
to = "<li>Zeitstempel</li>"

[[phrase]]
from = "<li>level</li>"
to = "<li>Level</li>"

[[phrase]]
from = "<li>request_id</li>"
to = "<li>Request-ID</li>"

[[phrase]]
from = "<li>vhost</li>"
to = "<li>Vhost</li>"

[[phrase]]
from = "<li>method</li>"
to = "<li>Methode</li>"

[[phrase]]
from = "<li>status</li>"
to = "<li>Status</li>"

[[phrase]]
from = "<li>Redact <code>Authorization</code>, <code>Cookie</code>, <code>Set-Cookie</code>, admin bearer tokens, ACME EAB values, API keys, and configured sensitive headers/fields.</li>"
to = "<li><code>Authorization</code>, <code>Cookie</code>, <code>Set-Cookie</code>, Admin-Bearer-Tokens, ACME-EAB-Werte, API-Keys und konfigurierte sensible Header/Felder redigieren.</li>"
