locale_id = "de-DE"
html_lang = "de-DE"

[[phrase]]
from = "Fluxheim is configured via a single TOML file. Use"
to = "Fluxheim wird über eine einzelne TOML-Datei konfiguriert. Nutze"

[[phrase]]
from = "to validate before restarting."
to = "zur Validierung vor dem Neustart."

[[phrase]]
from = "Key"
to = "Schlüssel"

[[phrase]]
from = "Type"
to = "Typ"

[[phrase]]
from = "Default"
to = "Standard"

[[phrase]]
from = "Description"
to = "Beschreibung"

[[phrase]]
from = "Cleartext listener addresses. Each entry is"
to = "Klartext-Listener-Adressen. Jeder Eintrag ist"

[[phrase]]
from = "Explicit TLS listener addresses. Fluxheim does not infer TLS from port numbers."
to = "Explizite TLS-Listener-Adressen. Fluxheim leitet TLS nicht aus Portnummern ab."

[[phrase]]
from = "Name of the vhost to use when no Host header matches. Required."
to = "Name des Vhosts, der verwendet wird, wenn kein Host-Header passt. Erforderlich."

[[phrase]]
from = "Trusted direct peers for recursive client-IP restoration."
to = "Vertrauenswürdige direkte Peers für rekursive Wiederherstellung der Client-IP."

[[phrase]]
from = "Enable global HTTP to HTTPS redirect with safe Host validation."
to = "Aktiviert globale HTTP-zu-HTTPS-Weiterleitung mit sicherer Host-Validierung."

[[phrase]]
from = "Reject missing, invalid, or unknown host identity instead of falling back to the default vhost."
to = "Lehnt fehlende, ungültige oder unbekannte Host-Identität ab, statt auf den Standard-Vhost zurückzufallen."

[[phrase]]
from = "Maximum request body size."
to = "Maximale Request-Body-Größe."

[[phrase]]
from = "Runtime worker thread count. Process settings require a process upgrade."
to = "Anzahl der Runtime-Worker-Threads. Prozess-Einstellungen erfordern ein Prozess-Upgrade."

[[phrase]]
from = "The admin control-plane provides health, status, purge, and config-reload endpoints. It requires bearer-token authentication on non-loopback listeners."
to = "Die Admin-Control-Plane stellt Health-, Status-, Purge- und Config-Reload-Endpunkte bereit. Auf Nicht-Loopback-Listenern ist Bearer-Token-Authentifizierung erforderlich."

[[phrase]]
from = "Enable the admin listener."
to = "Aktiviert den Admin-Listener."

[[phrase]]
from = "Admin listener address. Keep loopback where possible."
to = "Adresse des Admin-Listeners. Nutze wenn möglich Loopback."

[[phrase]]
from = "Require the admin listener to stay loopback-only."
to = "Erzwingt, dass der Admin-Listener nur auf Loopback bleibt."

[[phrase]]
from = "Environment variable containing the bearer token."
to = "Umgebungsvariable mit dem Bearer Token."

[[phrase]]
from = "File containing the bearer token. Prefer container or systemd secrets."
to = "Datei mit dem Bearer Token. Bevorzuge Container- oder systemd-Secrets."

[[phrase]]
from = "only when a trusted local sidecar terminates TLS/mTLS."
to = "nur wenn ein vertrauenswürdiger lokaler Sidecar TLS/mTLS terminiert."

[[phrase]]
from = "Enable the secure response header policy."
to = "Aktiviert die sichere Response-Header-Policy."

[[phrase]]
from = "Headers to remove. Use"
to = "Zu entfernende Header. Nutze"

[[phrase]]
from = "to suppress the default server banner."
to = "um das Standard-Server-Banner zu unterdrücken."

[[phrase]]
from = "Headers to add or set, such as"
to = "Header, die hinzugefügt oder gesetzt werden, wie"

[[phrase]]
from = "Only set when TLS is in use."
to = "Nur setzen, wenn TLS verwendet wird."

[[phrase]]
from = "When Fluxheim is built with the optional"
to = "Wenn Fluxheim mit dem optionalen"

[[phrase]]
from = "feature, it can load local MMDB databases and expose bounded country/ASN context to route policy and structured logs. GeoIP is local-only: Fluxheim does not download, poll, or refresh databases itself."
to = "Feature gebaut wurde, kann es lokale MMDB-Datenbanken laden und begrenzten Länder-/ASN-Kontext für Route-Policy und strukturierte Logs bereitstellen. GeoIP ist rein lokal: Fluxheim lädt, pollt oder aktualisiert Datenbanken nicht selbst."

[[phrase]]
from = "Enable local Geo-Context lookup when the binary includes"
to = "Aktiviert lokale Geo-Context-Lookups, wenn das Binary"

[[phrase]]
from = "Try later local MMDB files when the primary file has no usable country or ASN result."
to = "Versucht spätere lokale MMDB-Dateien, wenn die primäre Datei kein nutzbares Länder- oder ASN-Ergebnis liefert."

[[phrase]]
from = "Absolute path to a local MMDB file. Files are bounded at load time."
to = "Absoluter Pfad zu einer lokalen MMDB-Datei. Dateien werden beim Laden begrenzt."

[[phrase]]
from = "Route policy can use"
to = "Route-Policy kann"

[[phrase]]
from = "Allow lists fail closed when no Geo-Context is available; deny lists only deny on a known match."
to = "Allow-Listen schlagen geschlossen fehl, wenn kein Geo-Context verfügbar ist; Deny-Listen blockieren nur bei bekanntem Treffer."

[[phrase]]
from = "cannot be compiled together."
to = "können nicht gemeinsam kompiliert werden."

[[phrase]]
from = "string[]"
to = "string[]"

[[phrase]]
from = "string"
to = "string"

[[phrase]]
from = "bool"
to = "bool"

[[phrase]]
from = "integer"
to = "integer"

[[phrase]]
from = "table"
to = "table"

[[phrase]]
from = "true"
to = "true"

[[phrase]]
from = "false"
to = "false"

[[phrase]]
from = "listen"
to = "listen"

[[phrase]]
from = "tls_listen"
to = "tls_listen"

[[phrase]]
from = "default_vhost"
to = "default_vhost"

[[phrase]]
from = "trusted_proxies"
to = "trusted_proxies"

[[phrase]]
from = "enabled"
to = "enabled"

[[phrase]]
from = "token_env"
to = "token_env"

[[phrase]]
from = "token_file"
to = "token_file"

[[phrase]]
from = "fallback_enabled"
to = "fallback_enabled"

[[phrase]]
from = "When Fluxheim is built with the optional"
to = "Wenn Fluxheim mit dem optionalen"

[[phrase]]
from = "exposes raw L4 TCP services that stay separate from HTTP routing, headers, cache, compression, auth subrequests, PHP, and static web serving."
to = "stellt rohe L4-TCP-Dienste bereit, die von HTTP-Routing, Headern, Cache, Kompression, Auth-Subrequests, PHP und statischem Web-Serving getrennt bleiben."

[[phrase]]
from = "Enable dedicated TCP stream listeners when the binary includes"
to = "Aktiviert dedizierte TCP-Stream-Listener, wenn das Binary"

[[phrase]]
from = "TCP listeners. Each listener may belong to only one stream route."
to = "TCP-Listener. Jeder Listener darf nur zu einer Stream-Route gehören."

[[phrase]]
from = "upstreams. Multiple upstreams use weighted selection when weights are configured."
to = "Upstreams. Mehrere Upstreams verwenden gewichtete Auswahl, wenn Gewichte konfiguriert sind."

[[phrase]]
from = "Optional per-upstream weights for stream-local TCP selection."
to = "Optionale Per-Upstream-Gewichte für stream-lokale TCP-Auswahl."

[[phrase]]
from = "Optional safe labels for stream upstreams in logs and metrics."
to = "Optionale sichere Labels für Stream-Upstreams in Logs und Metrics."

[[phrase]]
from = "Backup upstreams attempted when primary stream upstream connection attempts fail."
to = "Backup-Upstreams, die versucht werden, wenn primäre Stream-Upstream-Verbindungsversuche fehlschlagen."

[[phrase]]
from = "Upstreams excluded from new stream selections while existing connections drain."
to = "Upstreams, die von neuen Stream-Auswahlen ausgeschlossen werden, während bestehende Verbindungen drainen."

[[phrase]]
from = "True idle timeout that resets when either direction transfers bytes."
to = "Echter Idle-Timeout, der zurückgesetzt wird, wenn eine Richtung Bytes überträgt."

[[phrase]]
from = "Optional wall-clock lifetime cap for one accepted stream connection."
to = "Optionale Wall-Clock-Lifetime-Grenze für eine angenommene Stream-Verbindung."

[[phrase]]
from = "Optional copied-byte cap per direction for a single stream connection."
to = "Optionale Copied-Byte-Grenze pro Richtung für eine einzelne Stream-Verbindung."

[[phrase]]
from = "Per-route concurrent connection cap. Zero means unlimited."
to = "Concurrent-Connection-Grenze pro Route. Null bedeutet unbegrenzt."

[[phrase]]
from = "receive for this stream route only. Requires route-local trusted proxies."
to = "nur für diese Stream-Route empfangen. Erfordert route-lokale vertrauenswürdige Proxies."

[[phrase]]
from = "send to upstream before forwarding stream bytes."
to = "an den Upstream senden, bevor Stream-Bytes weitergeleitet werden."

[[phrase]]
from = "Enable TLS for stream upstream connections. Cannot be combined with upstream PROXY protocol in this release."
to = "Aktiviert TLS für Stream-Upstream-Verbindungen. Kann in diesem Release nicht mit Upstream-PROXY-Protokoll kombiniert werden."

[[phrase]]
from = "Optional RFC 6066-safe SNI name for stream upstream TLS."
to = "Optionaler RFC-6066-sicherer SNI-Name für Stream-Upstream-TLS."

[[phrase]]
from = "Verify the upstream certificate chain when stream upstream TLS is enabled."
to = "Verifiziert die Upstream-Zertifikatskette, wenn Stream-Upstream-TLS aktiviert ist."

[[phrase]]
from = "Verify upstream TLS hostname identity using SNI or the upstream host."
to = "Verifiziert die Upstream-TLS-Hostname-Identität über SNI oder den Upstream-Host."

[[phrase]]
from = "One alternative certificate name accepted for stream upstream TLS."
to = "Ein alternativer Zertifikatsname, der für Stream-Upstream-TLS akzeptiert wird."

[[phrase]]
from = "Route-local CA bundle for stream upstream TLS verification."
to = "Route-lokales CA-Bundle für Stream-Upstream-TLS-Verifizierung."

[[phrase]]
from = "Client certificate chain for stream upstream mTLS."
to = "Client-Zertifikatskette für Stream-Upstream-mTLS."

[[phrase]]
from = "Client private key for stream upstream mTLS. Temporary key bytes are zeroized after parsing."
to = "Client-Private-Key für Stream-Upstream-mTLS. Temporäre Key-Bytes werden nach dem Parsen zeroized."

[[phrase]]
from = "compiled and enabled, stream proxying exports bounded connection outcome and per-direction byte counters."
to = "kompiliert und aktiviert ist, exportiert Stream-Proxying begrenzte Verbindungsresultate und Byte-Zähler pro Richtung."

[[phrase]]
from = "Stream routes do not support UDP, TLS passthrough SNI routing, HTTP policy, HTTP header/body mutation, or Wasm/Lua filters in this release."
to = "Stream-Routen unterstützen in diesem Release kein UDP, kein TLS-Passthrough-SNI-Routing, keine HTTP-Policy, keine HTTP-Header-/Body-Mutation und keine Wasm-/Lua-Filter."

[[phrase]]
from = "cannot be combined."
to = "können nicht kombiniert werden."

[[phrase]]
from = "table in TOML (array of tables). They are matched by the"
to = "Tabelle in TOML (Array von Tabellen). Sie werden über den"

[[phrase]]
from = "Unique identifier for this vhost. Used in logs and as the default_vhost reference."
to = "Eindeutiger Bezeichner für diesen Vhost. Wird in Logs und als default_vhost-Referenz verwendet."

[[phrase]]
from = "<tr><td class=\"key\">hosts</td><td class=\"type\">string[]</td><td class=\"desc\">List of <code class=\"section-ref\">Host</code> header values that route to this vhost. Wildcard hosts are accepted for matching; managed ACME derives exact domains only unless configured.</td></tr>"
to = "<tr><td class=\"key\">hosts</td><td class=\"type\">string[]</td><td class=\"desc\">Liste von <code class=\"section-ref\">Host</code>-Header-Werten, die zu diesem Vhost routen. Wildcard-Hosts werden beim Matching akzeptiert; verwaltetes ACME leitet exakte Domains nur ab, wenn es nicht anders konfiguriert ist.</td></tr>"

[[phrase]]
from = "Absolute path to the document root directory."
to = "Absoluter Pfad zum Document-Root-Verzeichnis."

[[phrase]]
from = "<tr><td class=\"key\">index_files</td><td class=\"type\">string[]</td><td class=\"desc\">List of filenames to serve as directory index. E.g. <code class=\"section-ref\">[\"index.html\"]</code>.</td></tr>"
to = "<tr><td class=\"key\">index_files</td><td class=\"type\">string[]</td><td class=\"desc\">Liste von Dateinamen, die als Directory-Index ausgeliefert werden. Z. B. <code class=\"section-ref\">[\"index.html\"]</code>.</td></tr>"

[[phrase]]
from = "Deny requests for files or directories starting with"
to = "Lehnt Requests für Dateien oder Verzeichnisse ab, die beginnen mit"

[[phrase]]
from = "response header."
to = "Response-Header."

[[phrase]]
from = "Enable managed ACME certificate issuance and renewal for this vhost."
to = "Aktiviert verwaltete ACME-Zertifikatsausstellung und -Erneuerung für diesen Vhost."

[[phrase]]
from = "Path to PEM certificate file for static/bought certs."
to = "Pfad zur PEM-Zertifikatsdatei für statische/gekaufte Zertifikate."

[[phrase]]
from = "Path to PEM private key file."
to = "Pfad zur PEM-Private-Key-Datei."

[[phrase]]
from = "Routes are matched in order. The first matching route wins."
to = "Routen werden der Reihe nach abgeglichen. Die erste passende Route gewinnt."

[[phrase]]
from = "for one or more origins."
to = "für einen oder mehrere Origins."

[[phrase]]
from = "Redirect action with"
to = "Redirect-Aktion mit"

[[phrase]]
from = "and optional"
to = "und optionalem"

[[phrase]]
from = "Route-scoped PHP action when built with a PHP runtime feature."
to = "Route-begrenzte PHP-Aktion, wenn mit einer PHP-Runtime-Feature gebaut."

[[phrase]]
from = "Per-status TTLs, e.g."
to = "Per-Status-TTLs, z. B."

[[phrase]]
from = "Multi-vhost with shared TLS"
to = "Multi-Vhost mit gemeinsamem TLS"

[[phrase]]
from = "WordPress with PHP-FPM"
to = "WordPress mit PHP-FPM"
