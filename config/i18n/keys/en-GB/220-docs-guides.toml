[docs_guides]
guides = "Guides"
start = "Start"
install = "Install"
static_sites = "Static Sites"
future_modules = "Future Modules"
full_reference = "Full Reference"
deep_reference = "Deep Reference"
good_first_path = "Good first path"
wasm_extensions = "WASM extensions"
common_builds = "Common builds"
two_upstreams = "Two upstreams"
what_it_gives_you = "What it gives you"
external_php_fpm_pool = "External PHP-FPM pool"
where_the_deep_docs_live = "Where the deep docs live"
production_notes = "Production notes"
rootless_podman = "Rootless Podman"
production_checklist = "Production checklist"
safe_habits = "Safe habits"
managed_certificates = "Managed certificates"
when_to_use_it = "When to use it"
example = "Example"
common_checks = "Common checks"
load_balancer = "Load Balancer"
get_fluxheim_running = "Get Fluxheim Running"
run_local_server_quickly = "Use this page when you want a local server quickly. It keeps the config small and checks it before serving traffic."
static_sites_intro = "Serve HTML, CSS, images, and downloads directly from disk. This is the simplest Fluxheim mode."
reverse_proxy_intro = "Put Fluxheim in front of an application server. Fluxheim handles TLS, headers, limits, and upstream selection."
keep_app_private = "Keep the application on loopback or a private network."
set_body_limits = "Set request body limits before exposing uploads."
tls_acme_intro = "Fluxheim can serve TLS with static certificates or manage ACME certificates for you."
php_fpm_intro = "Use PHP-FPM for PHP apps while Fluxheim serves static assets and forwards PHP requests safely."
wordpress_intro = "A practical PHP-FPM recipe for a normal WordPress front controller."
observability_intro = "Use Prometheus and OpenTelemetry to see aggregate traffic, errors, downloads, and page usage without tracking individual visitors."
features_intro = "Fluxheim is compiled with feature sets. Pick the smallest build that contains what your deployment needs."
configuration_intro = "Fluxheim uses TOML. Unknown fields are rejected, so spelling mistakes fail before restart."
deployment_intro = "Use systemd for native hosts or rootless containers for isolated deployments."
validate_before_restart = "Validate the TOML config before you restart the service."
minimal_static_site = "Minimal static site"
check_and_start = "Check and start"
static_sites_when = "Use static hosting for documentation, product pages, downloads, and sites that do not need application code on each request."
etag_reduces_traffic = "ETag and conditional requests reduce repeat traffic."
byte_ranges = "Byte ranges work for larger files."
dotfiles_denied = "Dotfiles should stay denied for public roots."
before_enabling_acme = "Before enabling ACME"
public_dns_points = "Make sure public DNS points to this server."
allow_acme_challenges = "Allow HTTP-01 or TLS-ALPN-01 challenge traffic."
mount_acme_state = "Mount ACME state as persistent writable storage."
cache_short_ttls = "Use purge or short TTLs when content changes often."
health_checks_rotation = "Health checks can keep broken backends out of rotation."
drain_force_down = "Drain and force-down operations help with maintenance windows."
keep_php_inside_root = "Keep PHP files inside the configured root."
serve_static_assets = "Serve static assets directly when possible."
never_expose_php_source = "Never expose PHP source when PHP execution fails."
wordpress_vhost = "WordPress vhost"
php_fpm_user_can_read = "Make sure the PHP-FPM user can read WordPress files."
uploads_writable_only = "Keep uploads writable only where WordPress needs writes."
prometheus_metrics = "Prometheus metrics"
otlp_export = "OTLP export"
low_cardinality_labels = "Keep labels low-cardinality: route group, language, status class, download artefact, and GitHub target are useful. Raw IP addresses and user identifiers are not."
use_it_for = "Use it for"
focused_balancing = "Focused upstream balancing and health checks."
static_php_wordpress = "Static web plus PHP-FPM applications such as WordPress."
cannot_go_together = "Things that cannot go together"
one_tls_backend = "Pick exactly one TLS backend."
focused_images_omit_modules = "Focused edge images intentionally omit modules outside their job."
main_shape = "Main shape"
run = "Run"
before_reloads = "before reloads."
one_vhost_per_site = "Use one vhost per site or application boundary."
keep_secrets_out = "Keep secrets in files, environment, or container secrets, not in public docs."
pin_versions = "Pin release versions or image digests."
mount_read_only = "Mount config and content read-only."
future_modules_intro = "Some modules are planned or experimental. Keep them separate from production basics until they are stable."
waf_future_page = "The WAF track is for rule-based request filtering, logging, and challenge/block responses. It should become its own clear page when it is production-ready."
github_deep_docs = "Use the GitHub repository when you need complete generated source documentation, release evidence, architecture tracks, and internal planning notes."
