[compression]
compression = "Compression"
cargo_features = "Cargo features:"
compression_remains_opt_in_default_builds_do_not_include_compression_code_and_privacy_mode_builds_re = "Compression remains opt-in. Default builds do not include compression code, and <code>privacy-mode</code> builds reject compression at compile time because response-body transforms can create side-channel and retention risks."
shared_config_and_response_filter_integration = ": shared config and response filter integration."
brotli_response_encoding_through = ": Brotli response encoding through"
gzip_response_encoding_through = ": gzip response encoding through"
zstandard_response_encoding = ": Zstandard response encoding."
goals = "Goals"
keep_gzip_as_a_conservative_compatibility_baseline = "Keep gzip as a conservative compatibility baseline."
keep_zstandard_and_brotli_behind_explicit_cargo_features_because_they_add_extra_codec_dependencies_a = "Keep Zstandard and Brotli behind explicit Cargo features because they add extra codec dependencies and operational behavior."
avoid_compressing_already_compressed_or_low_value_content = "Avoid compressing already-compressed or low-value content."
keep_request_workers_responsive_by_moving_expensive_compression_work_out_of_the_main_request_path = "Keep request workers responsive by moving expensive compression work out of the main request path."
validators_and_range_behavior_safely = ", validators, and range behavior safely."
negotiation = "Negotiation"
and_the_binary_is_built_with_at_least_one_codec_feature_gzip_is_available_through = "and the binary is built with at least one codec feature. Gzip is available through"
zstandard_through = ", Zstandard through"
and_brotli_through = ", and Brotli through"
identity_is_served_when_no_enabled_coding_is_accepted_by_the_client_the_response_is_already_encoded = "Identity is served when no enabled coding is accepted by the client, the response is already encoded, the response is too small or too large, the content length is unknown, or policy disables compression."
every_compressed_response_must_set_or_update = "Every compressed response must set or update:"
or_validator_behavior_according_to_the_selected_variant = "or validator behavior according to the selected variant;"
only_when_the_encoded_length_is_known = "only when the encoded length is known."
eligibility = "Eligibility"
do_not_compress_by_default = "Do not compress by default:"
jpeg_png_gif_webp_avif_mp4_webm_mp3_ogg_woff2_zip_gzip_brotli_zstandard_or_other_already_compressed = "JPEG, PNG, GIF, WebP, AVIF, MP4, WebM, MP3, OGG, WOFF2, ZIP, gzip, Brotli, Zstandard, or other already-compressed formats;"
responses_carrying_sensitive_per_user_content_unless_the_operator_explicitly_allows_it_and_cache_adm = "responses carrying sensitive per-user content unless the operator explicitly allows it and cache admission remains disabled;"
partial_range_responses_unless_a_future_range_aware_design_exists = "partial/range responses unless a future range-aware design exists;"
responses_above_configured_input_output_limits = "responses above configured input/output limits."
initial_positive_mime_types_should_be_conservative = "Initial positive MIME types should be conservative:"
execution_model = "Execution Model"
the_first_codec_implementation_is_intentionally_bounded = "The first codec implementation is intentionally bounded:"
only_responses_with_a_known = "only responses with a known"
input_must_fit_between = "input must fit between"
encoded_output_must_stay_within = "encoded output must stay within"
is_capped_at_64_mib_by_config_validation = "is capped at 64 MiB by config validation;"
is_capped_at_128_mib_by_config_validation = "is capped at 128 MiB by config validation;"
gzip_levels_are_restricted_to = "gzip levels are restricted to"
zstd_levels_are_restricted_to = "zstd levels are restricted to"
brotli_quality_is_restricted_to = "Brotli quality is restricted to"
fluxheim_removes = "Fluxheim removes"
a_later_implementation_may_add_bounded_compression_worker_pools_per_vhost_concurrency_and_precompres = "A later implementation may add bounded compression worker pools, per-vhost concurrency, and precompressed static asset variants."
cache_integration = "Cache Integration"
future_shared_cache_compression_variants_must_be_cache_isolated_by = "Future shared-cache compression variants must be cache-isolated by:"
source_cache_key = "source cache key;"
selected_encoding = "selected encoding;"
compression_policy_version = "compression policy version."
shared_cache_admission_must_still_reject_unsafe_personalized_responses_such_as_responses_with = "Shared cache admission must still reject unsafe personalized responses such as responses with"
precompressed_static_assets_may_be_supported_later_through_files_such_as = "Precompressed static assets may be supported later through files such as"
hardware_and_native_acceleration = "Hardware And Native Acceleration"
hardware_acceleration_and_cpu_specific_codecs_are_future_beta_work_any_qat_simd_or_platform_specific = "Hardware acceleration and CPU-specific codecs are future beta work. Any QAT, SIMD, or platform-specific backend must be selected through explicit feature flags or runtime capability detection with a safe fallback. Release artifacts must document whether they are generic or CPU-specific."
privacy_and_security = "Privacy And Security"
compression_can_create_side_channel_risk_when_secrets_and_attacker_controlled_input_share_the_same_c = "Compression can create side-channel risk when secrets and attacker-controlled input share the same compressed response. Safe defaults:"
do_not_compress_admin_metrics_auth_or_internal_control_responses = "do not compress admin, metrics, auth, or internal control responses;"
do_not_compress_responses_with_cookies_or_authorization_dependent_content_unless_explicitly_enabled = "do not compress responses with cookies or authorization-dependent content unless explicitly enabled for a selected vhost;"
do_not_log_compressed_bytes_or_response_bodies = "do not log compressed bytes or response bodies;"
reject_the_module_with = "reject the module with"
until_a_no_retention_no_side_channel_design_is_written_and_tested = "until a no-retention, no-side-channel design is written and tested."
the_global_block_is_the_default_for_every_vhost_a_vhost_can_override_it_with = "The global block is the default for every vhost. A vhost can override it with"
and_a_route_can_override_the_vhost_with = ", and a route can override the vhost with"
this_lets_one_site_enable_compression_while_another_site_keeps_identity_responses_or_lets_one_path_p = "This lets one site enable compression while another site keeps identity responses, or lets one path prefix opt in while the rest of the vhost stays uncompressed."
path_scoped_compression_can_be_modeled_as_a_route_override = "Path-scoped compression can be modeled as a route override:"
eligibility_checks_still_apply_inside_that_path_already_compressed_media_types_such_as_jpeg_png_webp = "Eligibility checks still apply inside that path: already-compressed media types such as JPEG, PNG, WebP, AVIF, and most archives are served as identity."
test_plan = "Test Plan"
and_identity_correctly_for_compiled_codecs = ", and identity correctly for compiled codecs."
does_not_compress_excluded_mime_types_or = "Does not compress excluded MIME types or"
does_not_compress_cookie_authorization = "Does not compress cookie, authorization,"
range_or_already_encoded_responses = ", range, or already encoded responses."
enforces_input_size_and_level_limits = "Enforces input size and level limits."
proves_compression_code_is_absent_from_default_and = "Proves compression code is absent from default and"
proves_compression_code_is_absent_from_default_and_privacy_mode_builds = "Proves compression code is absent from default and <code>privacy-mode</code> builds."
fluxheim_negotiates_response_compression_from_accept_encoding_when_compression_enabled_true_and_the = "<p>Fluxheim negotiates response compression from <code>Accept-Encoding</code> when <code>compression.enabled = true</code> and the binary is built with at least one codec feature. Gzip is available through <code>compression-gzip</code>, Zstandard through <code>compression-zstd</code>, and Brotli through <code>compression-brotli</code>. <code>q=0</code> is respected for each coding. When multiple accepted codings are enabled, Fluxheim prefers <code>br</code>, then <code>zstd</code>, then <code>gzip</code>.</p>"
negotiates_br_zstd_gzip_and_identity_correctly_for_compiled_codecs = "<li>Negotiates <code>br</code>, <code>zstd</code>, <code>gzip</code>, and identity correctly for compiled codecs.</li>"
