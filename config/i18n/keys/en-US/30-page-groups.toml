[release_updates]
fallback_pages_backed_by = "fallback pages backed by"
moves = "Moves"

[runtime_parity_fixtures]
runtime_parity_fixtures = "Runtime Parity Fixtures"
pingora_exit_baseline = "Pingora-exit baseline"
line_replaces_the_current_pingora_runtime_in_stages = "line replaces the current Pingora runtime in stages. Before any cutover, Fluxheim keeps an explicit inventory of fixtures and smoke scripts that define current behavior. This is not the whole test suite; it is the minimum release-gated parity surface that must stay visible while old and new runtime paths coexist."
the_machine_readable_inventory_is = "The machine-readable inventory is:"
the_inventory_is_validated_by = "The inventory is validated by:"
the_gate_checks_that_every_listed_script_config = "The gate checks that every listed script, config, directory, fixture, or document still exists. Script entries must remain executable. Later"
cutover_releases_should_add_more_entries_before_replacing = "cutover releases should add more entries before replacing a subsystem, not after discovering a parity gap."
how_to_use = "How To Use"
add_a_fixture_before_changing_a_runtime_boundary = "Add a fixture before changing a runtime boundary."
keep_fixtures_focused_on_observable_behavior_not_implementation = "Keep fixtures focused on observable behavior, not implementation details."
prefer_local_deterministic_scripts_over_external_services_unless = "Prefer local deterministic scripts over external services unless the feature specifically needs a containerized dependency."
keep_the_inventory_target_column_aligned_with_the = "Keep the inventory target column aligned with the"
release_where_the_fixture_matters_most = "release where the fixture matters most."
do_not_delete_or_rename_an_inventory_entry = "Do not delete or rename an inventory entry without updating the release notes and explaining what replaces the coverage."
relationship_to_runtime_baseline = "Relationship To Runtime Baseline"
records_build_and_dependency_evidence_this_document_records = "records build and dependency evidence. This document records behavior fixtures. Both are required: dependency removal without behavior parity is not a successful Pingora exit."

[geoip]
adds_a_bounded_optional = "adds a bounded optional"
feature_it_is_a_local_geo_context_foundation = "feature. It is a local Geo-Context foundation for access policy, not a dynamic downloader or programmable geo engine."
profile_full_includes_geoip_focused_builds_can_enable = "<code>profile-full</code> includes <code>geoip</code>. Focused builds can enable it directly:"
focused_builds_can_enable_it_directly = ". Focused builds can enable it directly:"
privacy_mode_and_geoip_are_mutually_exclusive_privacy = "<code>privacy-mode</code> and <code>geoip</code> are mutually exclusive. Privacy builds reject GeoIP lookup and Geo-Context metadata entirely."
are_mutually_exclusive_privacy_builds_reject_geoip_lookup = "are mutually exclusive. Privacy builds reject GeoIP lookup and Geo-Context metadata entirely."
local_databases = "Local Databases"
fluxheim_reads_local_mmdb_files_through_a_provider = "Fluxheim reads local MMDB files through a provider-agnostic layer. The initial providers are labels for compatible MMDB datasets:"
maxmind_geoip2_or_geolite2_mmdb_files = ": MaxMind GeoIP2 or GeoLite2 MMDB files."
european_circl_geo_open_mmdb_compatible_datasets = ": European CIRCL Geo Open MMDB-compatible datasets."
fluxheim_does_not_download_or_update_databases_in = "Fluxheim does not download or update databases in-process. Use an external systemd timer, package job, or sidecar to fetch and verify database files, then reload Fluxheim. Database files are opened as regular files and symlink leaf paths are rejected."
each_mmdb_file_is_capped_at_512_mib = "Each MMDB file is capped at 512 MiB at both metadata and read time. A single loaded GeoIP runtime is capped at 1 GiB of MMDB data, so keep the ordered fallback set small. During hot reload, the old and new runtimes can briefly coexist while in-flight requests finish, so size host/container memory for that temporary overlap."
databases_are_evaluated_in_order_when = "Databases are evaluated in order. When"
when_fallback_enabled_true_fluxheim_fills_missing_country = "When <code>fallback_enabled = true</code>, Fluxheim fills missing country or ASN fields from later databases when possible. When it is false, Fluxheim consults only the first configured database."
when_country_or_asn_access_rules_are_configured = "When country or ASN access rules are configured, Fluxheim checks the loaded MMDB database type strings and emits a security warning if no loaded database appears to provide the required record family. This is a diagnostic guard, not a substitute for testing policy behavior with known source IPs."
access_policy = "Access Policy"
geo_rules_live_on_the_existing_vhost_and = "Geo rules live on the existing vhost and route access policy. Country codes are ISO alpha-2 uppercase values. ASN values are numeric and must be greater than zero."
vhost_policy_runs_before_route_policy_geo_allow = "Vhost policy runs before route policy. Geo allow lists fail closed when no Geo-Context is available for the client IP. Geo deny lists only deny when the resolved country or ASN matches."
when_geoip_is_compiled_and_a_lookup_succeeds = "When <code>geoip</code> is compiled and a lookup succeeds, structured access logs include:"
is_compiled_and_a_lookup_succeeds_structured_access = "is compiled and a lookup succeeds, structured access logs include:"
geoip_metrics_and_trace_attributes_remain_bounded_follow = "GeoIP metrics and trace attributes remain bounded follow-up work; do not add high-cardinality labels from city, latitude/longitude, or raw database fields."

[load_balancer_ha]
load_balancer_ha_design_notes = "Load Balancer HA Design Notes"
ships_managed_affinity_cookies_for_one_process_this = "ships managed affinity cookies for one process. This document records the active-active HA design boundary for later"
work_so_operators_can_separate_shipped_behavior_from = "work so operators can separate shipped behavior from planned cluster behavior."
current_1_5_3_behavior = "Current 1.5.3 Behavior"
emits_a_signed_opaque_cookie_on_eligible_2xx = "emits a signed, opaque cookie on eligible 2xx/3xx backend responses."
cookie_values_map_to_the_local_bounded_persistence = "Cookie values map to the local bounded persistence table."
signing_keys_are_generated_locally_rotate_daily_and = "Signing keys are generated locally, rotate daily, and verify the current or previous generation."
persistence_tables_and_signing_keys_are_not_shared = "Persistence tables and signing keys are not shared between Fluxheim nodes."
process_restart_invalidates_unmanaged_local_state_and_starts = "Process restart invalidates unmanaged local state and starts a new signing key lineage."
future_active_active_shape = "Future Active-Active Shape"
the_planned_ha_track_should_replicate_only_bounded = "The planned HA track should replicate only bounded, operationally useful state:"
managed_cookie_key_to_backend_affinity_table_entries = "managed-cookie key-to-backend affinity table entries;"
application_cookie_or_header_persistence_decisions_when_explicitly = "application-cookie or header persistence decisions when explicitly opted in;"
administrative_member_overrides_such_as = "administrative member overrides such as"
optionally_passive_health_circuit_state_after_split_brain = "optionally passive-health/circuit state after split-brain and freshness rules are proven."
retry_budgets_queue_counters_high_cardinality_telemetry_and = "Retry budgets, queue counters, high-cardinality telemetry, and low-value per-request counters should stay node-local unless a later design proves that replication is bounded and useful."
peer_protocol_requirements = "Peer Protocol Requirements"
any_cookie_mirroring_implementation_must_include = "Any cookie-mirroring implementation must include:"
mutually_authenticated_peers = "mutually authenticated peers;"
bounded_message_size_and_bounded_table_sizes = "bounded message size and bounded table sizes;"
per_entry_ttl_and_table_eviction = "per-entry TTL and table eviction;"
replay_protection_through_monotonic_sequence_numbers_or_signed = "replay protection through monotonic sequence numbers or signed timestamps;"
explicit_fail_open_fail_closed_behavior_when_peers = "explicit fail-open/fail-closed behavior when peers are unavailable;"
audit_events_for_peer_joins_state_import_export = "audit events for peer joins, state import/export, and rejected updates;"
metrics_for_replication_lag_dropped_updates_rejected_updates = "metrics for replication lag, dropped updates, rejected updates, and peer health;"
key_rotation_that_supports_overlap_across_the_cluster = "key rotation that supports overlap across the cluster, not only one process."
non_goals_for_1_5_3 = "Non-Goals For 1.5.3"
code_1_5_3_code_does_not_implement = "<code>1.5.3</code> does not implement shared signing keys, restart-persistent state, cross-node persistence-table synchronization, or active-active cookie mirroring. Those remain future HA/control-plane work after the local managed-cookie path has passed release and pentest coverage."

[getting_started]
get_fluxheim_running_in_under_five_minutes_from = "Get Fluxheim running in under five minutes — from tarball, container, or source."
prerequisites = "Prerequisites"
macos_developer_builds_are_available_for_local_testing = "macOS developer builds are available for local testing only"
for_source_builds_rust_stable_toolchain_pinned_use = "For source builds: Rust stable toolchain (pinned — use"
for_containers_podman_4_rootless_or_docker = "For containers: Podman 4+ (rootless) or Docker"
move_binaries_to_your_path = "Move binaries to your path"
create_a_minimal_config_and_validate_it = "Create a minimal config and validate it"
run_it = "Run it"
option_2_container_rootless_podman = "Option 2 — Container (Rootless Podman)"
option_3_build_from_source = "Option 3 — Build from Source"
your_first_config = "Your First Config"
a_minimal_static_site_config_save_this_to = "A minimal static site config. Save this to"
adding_a_reverse_proxy_route = "Adding a Reverse Proxy Route"
build_profiles = "Build Profiles"
fluxheim_uses_cargo_feature_aliases_called = "Fluxheim uses Cargo feature aliases called"
for_grouped_builds_choose_the_profile_that_matches = "for grouped builds. Choose the profile that matches your deployment role."
profile = "Profile"
use_case = "Use case"
default_local_server_baseline = "Default local/server baseline"
cache_edge_without_local_static_web_serving = "Cache edge without local static web serving"
static_webserver_profile = "Static webserver profile"
profile_core = "profile-core"
profile_static_site = "profile-static-site"
profile_observability = "profile-observability"
profile_privacy = "profile-privacy"
profile_web_server = "profile-web-server"
profile_development = "profile-development"
all_stable_production_modules_including_traffic_mirroring = "All stable production modules, including traffic mirroring"
broad_development_build_with_compatible_production_modules = "Broad development build with compatible production modules"
bash_build_a_focused_profile = "bash — build a focused profile"
validate_custom_features_before_building = "Validate custom features before building"
checks_that_the_selected_feature_combination_is_coherent = "checks that the selected feature combination is coherent. TLS backends are mutually exclusive; pick exactly one."

[cache]
fluxheim_s_cache_system_supports_memory_disk_tiered = "Fluxheim's cache system supports memory, disk, tiered, and encrypted backends with route-scoped policies, cache locks, stale serving, distributed peer fill, and range caching."
enabling_cache = "Enabling Cache"
the_code_cache_code_cargo_feature_must_be = "The <code>cache</code> Cargo feature must be compiled in (default in pre-built binaries). Runtime cache is disabled until configured per-vhost."
fluxheim_toml_basic_memory_cache = "fluxheim.toml — basic memory cache"
cache_backends = "Cache Backends"
memory_cache = "Memory Cache"
fast_in_process_lru_cache_lost_on_restart = "Fast in-process LRU cache. Lost on restart. Best for frequently accessed small-to-medium objects."
disk_cache = "Disk Cache"
persistent_on_disk_cache_survives_restarts_the_storage = "Persistent on-disk cache. Survives restarts. The storage-bin backend (v1.2.2+) is optimised for larger, high-churn caches."
tiered_cache = "Tiered Cache"
hot_memory_tier_backed_by_disk_objects_are = "Hot memory tier backed by disk. Objects are promoted to memory on repeated access and written through to disk for persistence."
encrypted_disk_cache = "Encrypted Disk Cache"
available_since_v1_2_3_cache_objects_are = "Available since v1.2.3. Cache objects are encrypted at rest using a local key or an OpenBao Transit engine."
cache_policies = "Cache Policies"
cache_lock_stampede_prevention = "Cache Lock (Stampede Prevention)"
with_code_vhosts_cache_lock_enabled_true_code = "With <code>[vhosts.cache.lock].enabled = true</code>, only one upstream request is made per cache key when a miss occurs. Other requests wait for the fill to complete and are served from cache."
stale_while_revalidate = "Stale While Revalidate"
with_code_stale_while_revalidate_secs_code_expired = "With <code>stale_while_revalidate_secs</code>, expired cache entries are served immediately while a background request fills the cache with fresh content. Eliminates tail latency on cache expiry."
distributed_peer_fill = "Distributed Peer Fill"
available_since_v1_2_4_when_a_request = "Available since v1.2.4. When a request misses the local cache, Fluxheim can fetch from a cache peer before going to the upstream origin. Uses a safe"
semantics_if_the_peer_doesn_t_have_it = "semantics — if the peer doesn't have it, the request falls through to the origin."
range_caching = "Range Caching"
available_since_v1_2_5_opt_in_bounded = "Available since v1.2.5. Opt-in bounded range caching for large proxy-cache objects — useful for large media files and binary downloads."
fixed_slice_range_cache_v1_2_6 = "Fixed-Slice Range Cache (v1.2.6+)"
extended_range_cache_mode_that_supports_open_ended = "Extended range-cache mode that supports open-ended, suffix, and multipart byte-range responses by composing cached fixed-size slices."
cache_operations = "Cache Operations"
fluxheim_exposes_protected_cache_status_and_purge_endpoints = "Fluxheim exposes protected cache status and purge endpoints through the admin API. Warming and key inspection are CLI operations."
bash_admin_api_cache_operations = "bash — admin API cache operations"
local_static_file_caching = "Local Static-File Caching"
opt_in_since_v1_2_1_when_code = "Opt-in since v1.2.1. When <code>local_static = true</code> is enabled on the cache policy, Fluxheim caches static files from the local filesystem through the cache layer for hot-path performance."
p_strong_privacy_builds_strong_the_code_cache = "<p><strong>Privacy builds:</strong> The <code>cache</code> feature is compile-time incompatible with <code>privacy-mode</code>. Zero-retention deployments must use <code>--no-default-features</code> with <code>profile-privacy</code> to ensure no caching occurs.</p>"
opt_in_since_v1_2_1_when = "Opt-in since v1.2.1. When"
privacy_builds = "Privacy builds:"
feature_is_compile_time_incompatible_with = "feature is compile-time incompatible with"
zero_retention_deployments_must_use = ". Zero-retention deployments must use"
to_ensure_no_caching_occurs = "to ensure no caching occurs."

[extraction_dependency_graph]
extraction_dependency_graph_fluxheim_source_docs = "Extraction Dependency Graph — Fluxheim Source Docs"
extraction_dependency_graph = "Extraction Dependency Graph"
pingora_exit_planning_baseline = "Pingora-exit planning baseline"
fluxheim_should_split_large_modules_by_dependency_direction = "Fluxheim should split large modules by dependency direction, not by line count alone. The root crate should keep orchestration and adapters while pure domain logic moves into focused crates that can be tested without the current Pingora runtime."
dependency_direction = "Dependency Direction"
target_dependency_direction = "Target dependency direction:"
the_root_crate_may_temporarily_adapt_pingora_request = "The root crate may temporarily adapt Pingora request, response, listener, cache, and service types. Domain crates should not depend on those adapters."
extraction_order = "Extraction Order"
current_area = "Current area"
target_boundary = "Target boundary"
why_this_order = "Why this order"
mostly_filesystem_config_snapshot_policy_already_has_a = "Mostly filesystem/config snapshot policy; already has a crate and should keep shrinking root admin/runtime coupling."
pure_framing_plus_the_current_pingora_l4_connector = "Pure framing plus the current Pingora L4 connector adapter; split protocol bytes before stream/server replacement."
pure_parsing_export_formatting_can_move_before_http = "Pure parsing/export formatting can move before HTTP runtime changes."
security_sensitive_but_mostly_request_response_policy_extract = "Security-sensitive but mostly request/response policy; extract pure rules before replacing Pingora HTTP types."
listener_provider_policy = "listener/provider policy"
own_downstream_listener_planning_certificate_selection_sni_matching = "Own downstream listener planning, certificate selection, SNI matching, ALPN/cipher policy, and TLS provider/FIPS checks before replacing the Pingora listener adapter."
large_filesystem_tls_runtime_integration_surface_extract_after = "Large filesystem/TLS/runtime integration surface; extract after listener/TLS boundary traits exist."
own_task_shutdown_listener_tls_and_server_bootstrap = "Own task, shutdown, listener, TLS, and server bootstrap abstractions before removing Pingora server services."
continue_moving_admission_keys_headers_storage_plans_and = "Continue moving admission, keys, headers, storage plans, and purge logic; keep only Pingora cache adapters in root until HTTP proxy cutover."
last_large_http_cutover_depends_on_config_protocol = "Last large HTTP cutover; depends on config, protocol, headers, cache, PHP, observability, auth, mirror, compression, TLS, and server boundaries."
depends_on_nearly_every_domain_split_after_domain = "Depends on nearly every domain. Split after domain crates own status DTOs and mutation APIs."
1_6_rules = "1.6 Rules"
do_not_create_a_crate_that_depends_back = "Do not create a crate that depends back on the root"
prefer_pure_request_response_view_traits_over_concrete = "Prefer pure request/response view traits over concrete Pingora request headers in domain crates."
keep_temporary_pingora_adapters_narrow_and_named_as = "Keep temporary Pingora adapters narrow and named as adapters."
when_moving_code_move_tests_with_the_owned = "When moving code, move tests with the owned logic rather than leaving all private behavior tests in root modules."
if_a_moved_file_would_exceed_500_lines = "If a moved file would exceed 500 lines, split it during extraction instead of creating a new oversized exception."
review_use = "Review Use"
pentest_and_sast_reviews_should_use_this_graph = "Pentest and SAST reviews should use this graph to scope findings:"
pure_parser_or_policy_bug_fix_in_the = "pure parser or policy bug: fix in the domain crate;"
adapter_bug_fix_in_the_root_adapter_and = "adapter bug: fix in the root adapter and add a domain-crate regression where possible;"
runtime_lifecycle_bug_fix_through = "runtime lifecycle bug: fix through"
abstractions_before_touching_http_proxy_orchestration = "abstractions before touching HTTP proxy orchestration;"
broad_proxy_lifecycle_bug_keep_it_in_the = "broad proxy lifecycle bug: keep it in the"
http_cutover_queue_unless_it_is_a_release = "HTTP cutover queue unless it is a release-blocking security fix."

[runtime_baseline]
pingora_exit_baseline = "Pingora-exit baseline"
line_removes_pingora_from_the_normal_build_graph = "line removes Pingora from the normal build graph. That work must be measurable before it becomes behavioral. The runtime baseline records what the current implementation builds, depends on, and costs before the replacement runtime ships."
release_evidence = "Release Evidence"
release_gates_write_baseline_output_to = "Release gates write baseline output to:"
the_baseline_directory_is_generated_by = "The baseline directory is generated by:"
mode_captures_dependency_graphs_and_pingora_presence_for = "mode captures dependency graphs and Pingora presence for the official profiles without forcing a release build."
mode_also_builds_the_default_release_binary_and = "mode also builds the default release binary and records its size."
the_generated_files_are = "The generated files are:"
package_version_git_commit_dirty_flag_timestamp_host = "Package version, git commit, dirty flag, timestamp, host, rustc, and Cargo versions."
report_only_list_of_pingora_crates_still_present = "Report-only list of Pingora crates still present per profile."
milliseconds_from_process_spawn_to_first_successful_local = "Milliseconds from process spawn to first successful local HTTP response."
idle_rss_file_descriptor_count_sample_counts_and = "Idle RSS, file-descriptor count, sample counts, and TLS availability."
local_static_http_request_timing_samples = "Local static HTTP request timing samples."
local_round_robin_route_timing_samples_and_observed = "Local round-robin route timing samples and observed backend labels."
single_connection_http_keep_alive_throughput_samples = "Single-connection HTTP keep-alive throughput samples."
https_request_timing_samples_that_create_a_fresh = "HTTPS request timing samples that create a fresh TLS connection per sample when OpenSSL is available to generate the local certificate."
the_stricter_release_policy_is_tracked_in = "The stricter release policy is tracked in"
pingora_dependency_exceptions = "Pingora Dependency Exceptions"
that_gate_fails_when_a_pingora_crate_is = "That gate fails when a Pingora crate is present without an exception, when an exception is stale, or when the current Fluxheim version has reached the exception's"
which_checks_the_same_removal_targets_against = ", which checks the same removal targets against"
so_deadline_drift_is_visible_outside_the_release = "so deadline drift is visible outside the release gate. Maintainers can test an upcoming release bump before editing"
release_mode_invokes_the_performance_baseline_automatically_to = "Release mode invokes the performance baseline automatically. To skip it for a local emergency run, set:"
comparison_rules = "Comparison Rules"
baseline_is_not_a_pass_fail_performance_benchmark = "baseline is not a pass/fail performance benchmark. It is the reference point for later cutovers."
releases_should_compare_against_this_baseline_as_follows = "releases should compare against this baseline as follows:"
dependency_surface_once_a_release_claims_a_pingora = "dependency surface: once a release claims a Pingora crate is gone for a profile, that profile's"
check_must_fail_if_the_crate_returns = "check must fail if the crate returns;"
binary_size_default_and_focused_profile_binaries_should = "binary size: default and focused profile binaries should not grow without a release-note explanation;"
behavior_smoke_tests_and_fixture_parity_are_stronger = "behavior: smoke tests and fixture parity are stronger than raw speed numbers;"
environment_every_measurement_must_record_enough_host_toolchain = "environment: every measurement must record enough host/toolchain information to explain differences between local developer machines, CI runners, and release builders."
pingora_surface_policy = "Pingora Surface Policy"
deliberately_records_pingora_dependencies_instead_of_failing_on = "deliberately records Pingora dependencies instead of failing on them. The final"
proof_profiles_those_are_not_production_static_site = "proof profiles. Those are not production static-site profiles yet, but their"
dependency_trees_should_already_emit_no_rows_in = "dependency trees should already emit no rows in"
thead_tr_th_file_th_th_purpose_th = "<thead><tr><th>File</th><th>Purpose</th></tr></thead>"
p_that_gate_fails_when_a_pingora_crate = "<p>That gate fails when a Pingora crate is present without an exception, when an exception is stale, or when the current Fluxheim version has reached the exception&#x27;s <code>removal_target</code>. <code>cargo test</code> also runs <code>tests/pingora_dependency_policy.rs</code>, which checks the same removal targets against <code>Cargo.lock</code> so deadline drift is visible outside the release gate. Maintainers can test an upcoming release bump before editing <code>Cargo.toml</code> with:</p>"
p_later_code_1_6_x_code_releases = "<p>Later <code>1.6.x</code> releases should compare against this baseline as follows:</p>"

[modularity_policy]
modularity_policy_fluxheim_source_docs = "Modularity Policy — Fluxheim Source Docs"
modularity_policy = "Modularity Policy"
fluxheim_modularity_policy = "Fluxheim Modularity Policy"
status_1_6_planning_policy = "Status: 1.6 planning policy"
fluxheim_treats_large_files_and_unclear_crate_boundaries = "Fluxheim treats large files and unclear crate boundaries as security review risk, not only style debt. The 1.6 Pingora-exit line should move Fluxheim toward the same discipline used by smaller security-focused workspace projects: focused crates, small files, explicit adapters, and release-gated exceptions."
core_rule = "Core Rule"
crate_should_become_orchestration_glue_major_domains_belong = "crate should become orchestration glue. Major domains belong in focused workspace crates with one-way dependencies back to common types, never circular dependencies through admin, proxy, or runtime modules."
target_crates_include = "Target crates include:"
and_ecosystem_crates = ", and ecosystem crates"
file_size_rule = "File Size Rule"
new_or_newly_split_rust_implementation_files_should = "New or newly split Rust implementation files should follow:"
normal_target_300_lines_or_less = "normal target: 300 lines or less;"
hard_target_500_lines_or_less = "hard target: 500 lines or less;"
tests_should_be_split_by_behavior_before_they = "tests should be split by behavior before they make a production file hard to review;"
generated_vendored_or_machine_owned_files_must_be = "generated, vendored, or machine-owned files must be isolated and excluded from human-review line limits."
existing_large_files_are_legacy_debt_do_not = "Existing large files are legacy debt. Do not block 1.6.0 on the current file sizes, but do not let them grow without a documented reason."
1_6_migration_gate = "1.6 Migration Gate"
and_the_baseline_exception_inventory_in = "and the baseline exception inventory in"
the_gate_lists_non_generated = ". The gate lists non-generated"
files_over_500_lines_and_verifies_that_legacy = "files over 500 lines and verifies that legacy oversized files are tracked. The gate should fail only for:"
new_non_generated = "new non-generated"
files_over_500_lines = "files over 500 lines;"
files_that_exceed_500_lines_after_being_split = "files that exceed 500 lines after being split below the threshold;"
legacy_exception_files_that_grow_without_an_exception = "legacy exception files that grow without an exception update."
each_legacy_exception_should_record = "Each legacy exception should record:"
by_the_end_of_the_1_6_line = "By the end of the 1.6 line, the exception list should be materially smaller. After Pingora is gone and the root runtime is split, the gate can become a hard rule for all non-exempt files."
split_rules = "Split Rules"
split_by_reason_to_change_not_by_arbitrary = "Split by reason to change, not by arbitrary size alone."
prefer_modules_such_as = "Prefer modules such as:"
avoid_mixing_parser_code_validation_i_o_policy = "Avoid mixing parser code, validation, I/O, policy decisions, state mutation, telemetry, and tests in one file. Pure logic should move into crates that can be unit-tested without a running server."
security_rationale = "Security Rationale"
small_files_and_focused_crates_make_pentest_and = "Small files and focused crates make pentest and code review more reliable:"
reviewers_can_reason_about_one_trust_boundary_at = "reviewers can reason about one trust boundary at a time;"
duplicated_security_controls_are_easier_to_spot = "duplicated security controls are easier to spot;"
fuzz_and_fixture_coverage_can_target_pure_logic = "fuzz and fixture coverage can target pure logic;"
adapters_to_external_crates_stay_narrow = "adapters to external crates stay narrow;"
redaction_authority_checks_and_failure_modes_are_local = "redaction, authority checks, and failure modes are local;"
future_wasm_and_http_3_work_starts_from = "future Wasm and HTTP/3 work starts from clean domain APIs."
li_future_code_fluxheim_wasm_code_code_fluxheim = "<li>future <code>fluxheim-wasm</code>, <code>fluxheim-http3</code>, and ecosystem crates</li>"

[observability]
fluxheim_supports_prometheus_metrics_opentelemetry_metrics_and_traces = "Fluxheim supports Prometheus metrics, OpenTelemetry metrics and traces, and structured logging. All observability features are opt-in at compile time."
requires_code_class_text_gray_400_metrics_code = "Requires <code class=\"text-gray-400\">metrics</code> feature"
requires_code_class_text_gray_400_metrics_otlp = "Requires <code class=\"text-gray-400\">metrics-otlp</code>"
requires_code_class_text_gray_400_otel_tracing = "Requires <code class=\"text-gray-400\">otel-tracing</code> + <code class=\"text-gray-400\">otel-otlp</code>"
otlp_metrics = "OTLP Metrics"
otlp_tracing = "OTLP Tracing"
building_with_observability = "Building with Observability"
the_official_strong_full_release_binary_strong_includes = "The official <strong>full release binary</strong> includes all observability features. The cache and proxy edge binaries do not include metrics or tracing by default."
prometheus_metrics = "Prometheus Metrics"
enable_a_prometheus_compatible = "Enable a Prometheus-compatible"
scrape_endpoint_by_configuring_the_metrics_listener = "scrape endpoint by configuring the metrics listener."
scraping_with_prometheus = "Scraping with Prometheus"
available_metrics = "Available Metrics"
bounded_acme_lifecycle_outcomes = "— bounded ACME lifecycle outcomes"
aggregate_cache_hit_miss_store_purge_stale_and = "— aggregate cache hit, miss, store, purge, stale, and peer-fill activity"
configured_vhost_route_cache_activity_without_raw_host = "— configured vhost/route cache activity without raw host or path labels"
cache_lookup_and_request_collapsing_wait_durations = "— cache lookup and request-collapsing wait durations"
and_related_purger_metrics_cache_cleanup_health = "and related purger metrics — cache cleanup health"
otlp_metrics_exporter_health = "— OTLP metrics exporter health"
opentelemetry_metrics_otlp = "OpenTelemetry Metrics (OTLP)"
requires_the_code_metrics_otlp_code_cargo_feature = "Requires the <code>metrics-otlp</code> Cargo feature. Exports metrics to an OTLP-compatible endpoint (Grafana Alloy, OpenTelemetry Collector, etc.)."
opentelemetry_tracing = "OpenTelemetry Tracing"
requires_code_otel_tracing_code_for_trace_context = "Requires <code>otel-tracing</code> for trace context propagation and <code>otel-otlp</code> for exporting traces. Propagates W3C <code>traceparent</code> headers to upstream services."
what_gets_traced = "What gets traced"
inbound_w3c_code_traceparent_code_validation_and_normalized = "Inbound W3C <code>traceparent</code> validation and normalized propagation to upstreams"
structured_access_log_trace_ids_when_code_log = "Structured access-log trace IDs when <code>log_trace_id</code> is enabled"
bounded_request_spans_for_otlp_export_when_code = "Bounded request spans for OTLP export when <code>tracing.otlp.enabled</code> is true"
cache_attributes_for_cache_phase_timing_when_the = "Cache attributes for cache-phase timing when the binary includes the <code>cache</code> feature"
logging = "Logging"
fluxheim_emits_structured_logs_log_level_format_target = "Fluxheim emits structured logs. Log level, format, target, and access-log fields are controlled by the <code>[logging]</code> config."
profile_observability_build = "profile-observability Build"
the_code_profile_observability_code_alias_enables_the = "The <code>profile-observability</code> alias enables the full observability stack on top of <code>profile-core</code>: Prometheus metrics listener, OTLP metrics export, trace context propagation, and OTLP trace export."
the_official_full_release_binary_includes_all_observability = "The official full release binary includes all observability features. The cache and proxy edge binaries do not include metrics or tracing by default."
for_trace_context_propagation_and = "for trace context propagation and"
for_exporting_traces_propagates_w3c = "for exporting traces. Propagates W3C"
headers_to_upstream_services = "headers to upstream services."
inbound_w3c = "Inbound W3C"
validation_and_normalized_propagation_to_upstreams = "validation and normalized propagation to upstreams"
structured_access_log_trace_ids_when = "Structured access-log trace IDs when"
bounded_request_spans_for_otlp_export_when = "Bounded request spans for OTLP export when"
fluxheim_emits_structured_logs_log_level_format_target_2 = "Fluxheim emits structured logs. Log level, format, target, and access-log fields are controlled by the"

[release_notes_template]
release_notes_template_fluxheim_source_docs = "Release Notes Template — Fluxheim Source Docs"
release_notes_template = "Release Notes Template"
fluxheim_release_notes_template = "Fluxheim Release Notes Template"
use_this_template_for_stable_tags_keep_it = "Use this template for stable tags. Keep it factual and include the local release-gate report path or copied summary."
release_date = "Release date:"
git_tag = "Git tag:"
git_commit = "Git commit:"
license_eupl_1_2 = "License: EUPL-1.2"
stable_modules = "Stable modules:"
experimental_modules = "Experimental modules:"
default_cargo_features = "Default Cargo features:"
container_variants = "Container variants:"
security_and_stability_gate = "Security And Stability Gate"
gate_command = "Gate command:"
gate_report_directory = "Gate report directory:"
tls_scan_result = "TLS scan result:"
load_smoke_result = "Load smoke result:"
request_framing_smoke_result = "Request-framing smoke result:"
fuzz_target_compile_result = "Fuzz target compile result:"
podman_smoke_result = "Podman smoke result:"
sbom_generation_result = "SBOM generation result:"
reproducible_build_result = "Reproducible build result:"
reviewed_advisory_exceptions = "Reviewed Advisory Exceptions"
list_every_accepted_dependency_advisory_and_why_it = "List every accepted dependency advisory and why it is acceptable for this release. Include whether the dependency is direct or transitive."
breaking_changes = "Breaking Changes"
upgrade_notes = "Upgrade Notes"
known_limitations = "Known Limitations"
container_images = "Container Images"
github_container_registry = "GitHub Container Registry:"
docker_hub = "Docker Hub:"
runtime_user = "Runtime user:"
default_config_path = "Default config path:"
static_site_path = "Static site path:"
cache_path = "Cache path:"
state_path = "State path:"
checksums_and_signatures = "Checksums And Signatures"
source_archive_checksum = "Source archive checksum:"
binary_checksums = "Binary checksums:"
when_published = "when published"
sbom_checksums = "SBOM checksums:"
reproducible_build_hash = "Reproducible build hash:"
container_digests = "Container digests:"
tag_signature = "Tag signature:"

[macos_development]
macos_development_support_fluxheim_source_docs = "macOS Development Support — Fluxheim Source Docs"
macos_development_support = "macOS Development Support"
targets_level_1_macos_support_contributors_should_be = "targets Level 1 macOS support: contributors should be able to build and run local development profiles on Apple Silicon Macs. This is not a production macOS support claim."
linux_remains_the_production_baseline_for_native_packages_systemd = "Linux remains the production baseline for native packages, systemd units, containers, FIPS/ISO evidence, and release-gate parity. macOS support is for local development, site testing, and contributor workflows while Pingora macOS support remains experimental."
supported_scope = "Supported Scope"
primary_apple_silicon_developer_target_for_m_series = "Primary Apple Silicon developer target for M-series Macs."
intel_mac_developer_target_when_a_maintainer_can_test = "Intel Mac developer target when a maintainer can test it."
linux_arm64_release_target_released_as = "Linux ARM64 release target, released as"
main_linux_production_release_target_released_as = "Main Linux production release target, released as"
arm_is_not_one_release_artifact_the_rust_target = "ARM is not one release artifact. The Rust target triple defines the operating system ABI and CPU baseline. For normal server ARM64 hardware such as Apple Silicon, AWS Graviton, Ampere, and 64-bit Raspberry Pi operating systems, build the matching"
code_aarch64_code_target_do_not_use_code = "<code>aarch64-*</code> target. Do not use <code>RUSTFLAGS=&quot;-C target-cpu=native&quot;</code> for published release assets unless the artifact is intentionally tied to one machine class."
for_published_release_assets_unless_the_artifact_is_intentionally = "for published release assets unless the artifact is intentionally tied to one machine class."
install = "Install:"
xcode_command_line_tools = "Xcode Command Line Tools:"
rust_1_96_through_rustup = "Rust 1.96 through rustup"
cmake_if_selecting_native_c_dependencies_such_as_zstd = "CMake if selecting native C dependencies such as zstd/libz-ng/AWS-LC paths"
optional_homebrew_php_for_managed_php_fpm_development_tests = "Optional Homebrew PHP for managed PHP-FPM development tests"
check_the_host_target = "Check the host target:"
on_an_m_series_mac_this_should_print = "On an M-series Mac this should print"
build_checks = "Build Checks"
these_are_the_level_1_developer_checks = "These are the Level 1 developer checks:"
for_a_quick_runtime_check = "For a quick runtime check:"
the_smoke_script_writes_all_runtime_state_under = "The smoke script writes all runtime state under"
by_default_and_does_not_touch = "by default and does not touch"
launchd_or_system_service_locations = ", launchd, or system service locations."
local_runtime_paths = "Local Runtime Paths"
macos_development_configs_should_use_project_local_paths_or = "macOS development configs should use project-local paths, or a private per-user temporary directory whose parents are not group/world writable:"
runtime_data = "Runtime data"
recommended_macos_dev_path = "Recommended macOS dev path"
pid_file = "pid file"
upgrade_socket = "upgrade socket"
certificate_reload_socket = "certificate reload socket"
admin_snapshots = "admin snapshots"
acme_storage = "ACME storage"
disk_cache = "disk cache"
file_logs = "file logs"
managed_php_fpm_sockets = "managed PHP-FPM sockets"
do_not_copy_linux_service_examples_directly_to_macos = "Do not copy Linux service examples directly to macOS:"
are_packaging_defaults_for_linux_service_deployments_do_not = "are packaging defaults for Linux service deployments. Do not place Fluxheim runtime state directly under"
fluxheim_x27_s_filesystem_trust_checks_reject_world = "; Fluxheim&#x27;s filesystem trust checks reject world-writable parents."
release_assets = "Release Assets"
if_macos_developer_binaries_are_attached_to_a = "If macOS developer binaries are attached to a GitHub release, build them on macOS with the release helper:"
preferred_release_naming_examples = "Preferred release naming examples:"
universal_macos_binaries_are_optional_and_can_be_produced = "Universal macOS binaries are optional and can be produced with"
from_separate_intel_and_apple_silicon_builds_but = "from separate Intel and Apple Silicon builds, but per-target tarballs are simpler and clearer for Fluxheim's current support level."
from_separate_intel_and_apple_silicon_builds_but_2 = "from separate Intel and Apple Silicon builds, but per-target tarballs are simpler and clearer for Fluxheim&#x27;s current support level."
thead_tr_th_target_th_th_scope_th_tr_thead = "<thead><tr><th>Target</th><th>Scope</th></tr></thead>"

[gateway_recipes]
gateway_recipes_fluxheim_source_docs = "Gateway Recipes — Fluxheim Source Docs"
gateway_recipes = "Gateway Recipes"
this_page_shows_practical = "This page shows practical"
gateway_style_patterns_for_moving_common_static_and_proxy = "gateway-style patterns for moving common static and proxy vhosts into Fluxheim. The examples use one split config file per site; keep shared listeners, TLS defaults, logging, and global header policy in"
shared_server_baseline = "Shared Server Baseline"
only_for_proxies_you_actually_control_when_it_is = "only for proxies you actually control. When it is empty, Fluxheim uses the direct peer address for generated client-IP headers. When the direct peer is trusted, Fluxheim treats"
like_nginx = "like nginx"
trusted_hops_are_skipped_from_the_right_and_generated = ": trusted hops are skipped from the right and generated"
values_use_the_restored_client_address = "values use the restored client address."
cleartext_challenge_exception_and_https_redirect = "Cleartext Challenge Exception And HTTPS Redirect"
if_a_cleartext_challenge_helper_must_stay_reachable = "If a cleartext challenge helper must stay reachable while everything else moves to HTTPS, enable the global redirect and the vhost challenge helper:"
this_internally_creates_the_standard_http_01_challenge_route = "This internally creates the standard HTTP-01 challenge route at"
and_exempts_only_that_path_from_the_global_redirect = "and exempts only that path from the global redirect. Normal proxy and static routes still redirect on cleartext requests."
canonical_host_redirect = "Canonical Host Redirect"
create_a_small_vhost_for_the_secondary_host_and = "Create a small vhost for the secondary host and redirect it to the canonical name:"
app_proxy_with_upload_limit = "App Proxy With Upload Limit"
on_the_vhost_when_the_whole_site_shares_one = "on the vhost when the whole site shares one upload budget. Set it on a route only when a specific path needs a different limit."
for_common_upstream_identity_headers_prefer_the_typed_request = "For common upstream identity headers, prefer the typed request-header fields in the shared baseline:"
use_dynamic_header_templates_only_when_a_backend_needs_an = "Use dynamic header templates only when a backend needs an exact value that is not covered by a typed field. The supported variables are intentionally small:"
tls_client_certificate_variables = "TLS client-certificate variables,"
and_bounded_regex_route_captures_such_as = "and bounded regex route captures such as"
when_the_matched_route_uses = "when the matched route uses"
unknown_variables_fail_config_validation_and_rendered_values_still = "Unknown variables fail config validation, and rendered values still pass HTTP header validation before they are sent upstream."
browser_login_probe_for_wordpress = "Browser Login Probe For WordPress"
some_proxy_bugs_only_show_up_in_a_real = "Some proxy bugs only show up in a real browser. One important example is HTTP/2 clients sending multiple"
header_fields_an_http_2_to_http_1_x = "header fields; an HTTP/2-to-HTTP/1.x gateway must forward those to classic backends as one semicolon-delimited"
header_not_as_a_comma_delimited_list = "header, not as a comma-delimited list."
use_the_browser_probe_when_a_wordpress_login_works = "Use the browser probe when a WordPress login works with"
but_loops_in_chrome_or_another_browser = "but loops in Chrome or another browser:"
the_script_launches_local_chrome_or_chromium_through_the = "The script launches local Chrome or Chromium through the DevTools protocol, submits the login form, checks for the admin dashboard, and writes artifacts to"
password_values_are_redacted_from_request_logs_but_the = "Password values are redacted from request logs, but the captured HTML and cookie names are still operational debugging artifacts; keep them local and delete them after analysis."
or_another_supported_browser_name_when_more_than_one = "or another supported browser name when more than one standard Chrome/Chromium binary is installed. The accepted names are"
arbitrary_executable_paths_are_intentionally_rejected_the_probe_is = "arbitrary executable paths are intentionally rejected. The probe is intentionally not part of the normal release gate because it targets live sites and needs credentials."
websocket_or_long_lived_route = "Websocket Or Long-Lived Route"
fluxheim_preserves = "Fluxheim preserves"
request_headers_by_default_use_a_prefix_route_with_longer = "request headers by default. Use a prefix route with longer upstream timeouts and optional prefix stripping:"
if_the_backend_requires_explicit_legacy_style_upgrade_forwarding = "If the backend requires explicit legacy-style upgrade forwarding on that route, set it route-locally:"
static_alias_with_directory_listing = "Static Alias With Directory Listing"
use_a_route_local_static_action_when_only_one_path = "Use a route-local static action when only one path should expose a different filesystem root:"
directory_listings_are_off_by_default_when_enabled_index_files = "Directory listings are off by default. When enabled, index files still win, dotfiles remain hidden when"
and_symlink_entries_are_skipped_set = "and symlink entries are skipped. Set"
when_you_want_listing_modification_times_shown_with_the = "when you want listing modification times shown with the server's local UTC offset instead of GMT HTTP-date timestamps."
custom_upstream_error_page = "Custom Upstream Error Page"
attach_static_fallback_pages_to_a_proxy_policy = "Attach static fallback pages to a proxy policy:"
the_configured_error_page_is_internal_to_the_proxy_fallback = "The configured error page is internal to the proxy fallback. It is not exposed as a public route unless another"
route_serves_that_same_path = "route serves that same path."
current_1_0_boundaries = "Current 1.0 Boundaries"
dns_refresh_is_available_for_load_balancer_builds_through = "DNS refresh is available for load-balancer builds through"
and_local_sidecar_orchestrator_discovery_can_use = "and local sidecar/orchestrator discovery can use"
backend_per_line_rich_dynamic_backend_metadata_such_as = "backend per line. Rich dynamic backend metadata such as weights, aliases, backups, and drains still requires static"
in_this_release = "in this release."
per_route_proxy_static_redirect_upload_limit_timeout_header_policy = "Per-route proxy, static, redirect, upload limit, timeout, header policy, prefix stripping, and internal error-page fallback are the supported gateway building blocks."
advanced_auth_rewrite_engines_compression_waf_image_video = "Advanced auth, rewrite engines, compression, WAF, image/video filters, and WASM policy hooks are planned as optional post-"
