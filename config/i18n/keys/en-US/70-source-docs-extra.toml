[cache_encryption]
cache_encryption = "Cache Encryption"
what_gets_encrypted = "What Gets Encrypted"
cache_encryption_wraps_objects_stored_by = "Cache encryption wraps objects stored by"
it_works_with_both_disk_backends = ". It works with both disk backends:"
each_persisted_cache_object_is_encrypted_before_it_is_written_to_its_object_file = ": each persisted cache object is encrypted before it is written to its object file."
each_persisted_cache_object_is_encrypted_before_it_is_placed_into_a_bin_file = ": each persisted cache object is encrypted before it is placed into a bin file."
memory_cache_remains_plaintext_inside_the_fluxheim_process_in_a_tiered_memory_plus_disk_cache_memory = "Memory cache remains plaintext inside the Fluxheim process. In a tiered memory-plus-disk cache, memory hits stay fast and the persisted disk copy is encrypted at rest."
the_same_encryption_layer_applies_to_reverse_proxy_cache_responses_and_to_local_static_cache_respons = "The same encryption layer applies to reverse-proxy cache responses and to local/static cache responses when the selected cache policy has"
providers = "Providers"
uses_aes_256_gcm_with_a_64_character_hex_key_loaded_from_one_safe_file_or_credential_it_is_simple_an = "uses AES-256-GCM with a 64-character hex key loaded from one safe file or credential. It is simple and fast, but Fluxheim must be able to read the raw cache key at startup. Filesystem disk-cache fills use bounded encrypted chunks with this provider, preserving the streaming memory profile of plain filesystem cache writes."
sends_object_bytes_to_openbao_transit = "sends object bytes to OpenBao Transit"
endpoints_and_stores_only_the_returned = "endpoints and stores only the returned"
ciphertext_in_the_cache_backend_use_this_when_key_custody_audit_trails_and_centralized_rotation_matt = "ciphertext in the cache backend. Use this when key custody, audit trails, and centralized rotation matter more than the added call latency on disk-cache reads and writes. Because the Transit API encrypts one complete plaintext value per request, Fluxheim caps concurrent OpenBao encrypted commit heap usage and can refuse a large cache fill under pressure rather than buffering too many objects at once."
both_providers_bind_the_configured = "Both providers bind the configured"
and_the_combined_cache_key_as_authenticated_data_a_stored_encrypted_object_cannot_be_silently_moved = "and the combined cache key as authenticated data. A stored encrypted object cannot be silently moved to a different cache key."
quick_start_local_key = "Quick Start: Local Key"
this_is_the_simplest_encrypted_disk_cache_it_keeps_the_raw_aes_256_gcm_key_on_the_host_as_a_root_own = "This is the simplest encrypted disk cache. It keeps the raw AES-256-GCM key on the host as a root-owned secret and exposes it to Fluxheim through a systemd/container credential."
generate_and_install_the_key = "Generate and install the key:"
for_containers_mount_the_same_value_at = "For containers, mount the same value at:"
local_key_setup = "Local Key Setup"
prefer_credentials_over_paths_so_the_same_toml_works_with_systemd_credentials_podman_docker_secrets = "Prefer credentials over paths so the same TOML works with systemd credentials, Podman/Docker secrets, and Kubernetes secrets:"
create_the_key_as_a_root_owned_secret = "Create the key as a root-owned secret:"
for_systemd_expose_it_to_fluxheim_with_a_drop_in = "For systemd, expose it to Fluxheim with a drop-in:"
for_containers_mount_the_secret_at = "For containers, mount the secret at"
quick_start_openbao_transit = "Quick Start: OpenBao Transit"
use_openbao_transit_when_the_cache_encryption_key_should_stay_outside_the_fluxheim_host_and_when_key = "Use OpenBao Transit when the cache encryption key should stay outside the Fluxheim host and when key operations should be auditable in a central service. Fluxheim stores only Transit ciphertext such as"
create_a_minimal_transit_policy = "Create a minimal Transit policy:"
then_expose_the_openbao_token_as_a_systemd_container_credential_named = "Then expose the OpenBao token as a systemd/container credential named"
openbao_transit_setup = "OpenBao Transit Setup"
the_openbao_provider_expects_a_transit_key_and_a_token_that_can_encrypt_and_decrypt_with_that_key = "The OpenBao provider expects a Transit key and a token that can encrypt and decrypt with that key:"
a_minimal_openbao_policy_for_one_cache_key_is = "A minimal OpenBao policy for one cache key is:"
fluxheim_accepts_https_openbao_urls_plus_loopback_http_urls_for_local_testing_non_loopback_plaintext = "Fluxheim accepts HTTPS OpenBao URLs, plus loopback HTTP URLs for local testing. Non-loopback plaintext HTTP OpenBao addresses are rejected. Transit encrypt/decrypt calls do not follow HTTP redirects, and Transit JSON responses are read through a bounded buffer before parsing so a compromised or misconfigured OpenBao endpoint cannot stream unbounded response bodies into the cache process."
verifying_runtime_behavior = "Verifying Runtime Behavior"
enable_a_cache_status_header_while_validating_a_new_policy = "Enable a cache status header while validating a new policy:"
then_request_the_same_cacheable_object_twice = "Then request the same cacheable object twice:"
expected_behavior = "Expected behavior:"
disk_cache_files_or_bin_files_should_not_contain_the_plaintext_response_body = "disk cache files or bin files should not contain the plaintext response body"
for_a_storage_bin_cache_inspect_the_bin_directory = "For a storage-bin cache, inspect the bin directory:"
for_openbao_transit_persisted_objects_should_contain = "For OpenBao Transit, persisted objects should contain"
ciphertext_markers_rather_than_plaintext_response_bodies = "ciphertext markers rather than plaintext response bodies."
rotation = "Rotation"
for_local_key_encryption_changing_the_raw_key_should_also_change = "For local-key encryption, changing the raw key should also change"
and_either_purge_the_disk_cache_or_move_to_a_new = "and either purge the disk cache or move to a new"
existing_cache_objects_encrypted_with_the_old_local_key_are_intentionally_unreadable_once_fluxheim_s = ". Existing cache objects encrypted with the old local key are intentionally unreadable once Fluxheim starts with only the new key."
for_openbao_transit_the_usual_rotation_path_is_to_keep_the_same_fluxheim = "For OpenBao Transit, the usual rotation path is to keep the same Fluxheim"
then_rotate_the_transit_key_inside_openbao_openbao_can_decrypt_older = ", then rotate the Transit key inside OpenBao. OpenBao can decrypt older"
ciphertext_while_retaining_the_necessary_old_key_versions_if_you_change_fluxheim = "ciphertext while retaining the necessary old key versions. If you change Fluxheim"
treat_it_as_a_cache_namespace_cutover_and_purge_or_move_the_disk_cache = ", treat it as a cache namespace cutover and purge or move the disk cache."
local_validation = "Local Validation"
run_the_local_key_storage_bin_smoke_without_external_services = "Run the local-key storage-bin smoke without external services:"
run_the_optional_openbao_transit_smoke_with_podman = "Run the optional OpenBao Transit smoke with Podman:"
the_openbao_smoke_starts_a_disposable_openbao_dev_container_enables_transit_creates_a_cache_key_runs = "The OpenBao smoke starts a disposable OpenBao dev container, enables Transit, creates a cache key, runs Fluxheim against a local origin, verifies"
and_checks_that_the_cache_object_contains_transit_ciphertext_rather_than_the_plaintext_response_body = "and checks that the cache object contains Transit ciphertext rather than the plaintext response body."
keep_cache_encryption_opt_in_it_adds_cpu_work_for_local_key_encryption_and_network_service_dependenc = "Keep cache encryption opt-in. It adds CPU work for local-key encryption and network/service dependency for OpenBao Transit."
prefer_the_local_provider_when_large_filesystem_cache_fills_must_preserve_streaming_memory_behavior = "Prefer the local provider when large filesystem cache fills must preserve streaming memory behavior; OpenBao Transit is a whole-object external crypto boundary by design."
for_high_churn_encrypted_caches_that_would_otherwise_create_many_small_encrypted_object_files = "for high-churn encrypted caches that would otherwise create many small encrypted object files."
use_memory_plus_encrypted_disk_when_hot_objects_should_remain_fast_but_disk_persistence_must_be_prot = "Use memory plus encrypted disk when hot objects should remain fast but disk persistence must be protected."
as_cache_object_authentication_metadata_changing_it_is_a_cache_namespace_cutover = "as cache-object authentication metadata. Changing it is a cache namespace cutover."
keep_cache_status_headers_disabled_in_normal_production_responses_unless_they_are_needed_for_debuggi = "Keep cache-status headers disabled in normal production responses unless they are needed for debugging."
for_systemd = "For systemd:"
first_request = "first request:"
second_request = "second request:"
prefer_credentials_over_paths_so_the_same_toml_works_with_systemd_credentials_podman_docker_secret_2 = "<p>Prefer credentials over paths so the same TOML works with systemd credentials, Podman/Docker secrets, and Kubernetes secrets:</p>"
followed_by = "</code> followed by <code>"
prefer_storage_bin_for_high_churn_encrypted_caches_that_would_otherwise_create_many_small_encrypted = "<li>Prefer <code>storage-bin</code> for high-churn encrypted caches that would otherwise create many small encrypted object files.</li>"

[perl_cgi_support]
perl_cgi_support = "Perl CGI Support"
perl_cgi_support_is_a_future_fluxheim_application_server_milestone_for_legacy_tools_that_still_depen = "Perl CGI support is a future Fluxheim application-server milestone for legacy tools that still depend on RFC 3875 CGI execution. It must be opt-in at compile time and opt-in per vhost. It should not be part of the default runtime behavior because CGI executes local programs with request-controlled input."
current_recommendation = "Current Recommendation"
the_planned_implementation_path_is = "The planned implementation path is"
high_level_cgi_implementation_for_rust_mit = ": high-level CGI implementation for Rust, MIT."
tokio_runtime_support_for_cgi_process_execution_mit = ": Tokio runtime support for CGI process execution, MIT."
unix_resource_limits_mit = ": Unix resource limits, MIT."
optional_linux_path_based_sandboxing_mit_or_apache_2_0 = ": optional Linux path-based sandboxing, MIT OR Apache-2.0."
the_cgi_module_should_use_a_normal_external_perl_interpreter_first_for_example = "The CGI module should use a normal external Perl interpreter first, for example"
and_should_not_embed_perl_into_fluxheim_process_isolation_is_the_main_security_boundary = ", and should not embed Perl into Fluxheim. Process isolation is the main security boundary."
compile_time_features = "Compile-Time Features"
planned_feature_flags = "Planned feature flags:"
the_default_binary_should_not_enable_perl_cgi_if_a_release_profile_later_chooses_to_compile_the_modu = "The default binary should not enable Perl CGI. If a release profile later chooses to compile the module by default, runtime execution must still remain disabled unless a vhost explicitly enables it."
config_shape = "Config Shape"
initial_typed_toml_target = "Initial typed TOML target:"
the_cgi_handler_should_run_before_normal_static_fallback_for_configured_cgi_paths_if_cgi_execution_f = "The CGI handler should run before normal static fallback for configured CGI paths. If CGI execution fails, Fluxheim must return an error response and must not serve the script source."
request_flow = "Request Flow"
match_an_enabled_vhost_cgi_route = "Match an enabled vhost CGI route."
resolve_the_requested_script_under_the_configured_cgi_root = "Resolve the requested script under the configured CGI root."
canonicalize_the_root_and_script_path = "Canonicalize the root and script path."
reject_traversal_symlink_escapes_hidden_files_non_files_and_scripts_with_unsafe_permissions = "Reject traversal, symlink escapes, hidden files, non-files, and scripts with unsafe permissions."
build_a_strict_cgi_environment_from_the_request = "Build a strict CGI environment from the request."
spawn_the_perl_interpreter_with_clean_environment_configured_uid_gid_resource_limits_timeout_and_opt = "Spawn the Perl interpreter with clean environment, configured uid/gid, resource limits, timeout, and optional Landlock restrictions."
pipe_the_bounded_request_body_to_stdin = "Pipe the bounded request body to stdin."
parse_stdout_as_cgi_headers_plus_body = "Parse stdout as CGI headers plus body."
capture_stderr_separately_cap_it_sanitize_it_and_log_it = "Capture stderr separately, cap it, sanitize it, and log it."
kill_the_process_group_on_timeout_client_cancellation_or_output_limit_violation = "Kill the process group on timeout, client cancellation, or output limit violation."
security_requirements = "Security Requirements"
before_cgi_is_production_eligible = "Before CGI is production eligible:"
cgi_must_be_disabled_unless_both_compile_feature_and_vhost_config_enable_it = "CGI must be disabled unless both compile feature and vhost config enable it."
must_come_from_canonical_path_resolution_not_string_concatenation = "must come from canonical path resolution, not string concatenation."
dotfiles_and_hidden_path_segments_are_denied_by_default = "Dotfiles and hidden path segments are denied by default."
symlink_escapes_outside_the_cgi_root_are_denied = "Symlink escapes outside the CGI root are denied."
group_world_writable_scripts_are_denied = "Group/world-writable scripts are denied."
the_interpreter_path_must_be_absolute_and_validated_at_startup = "The interpreter path must be absolute and validated at startup."
the_child_environment_starts_empty_by_default = "The child environment starts empty by default."
only_strict_rfc_3875_cgi_variables_are_passed = "Only strict RFC 3875 CGI variables are passed:"
and_explicitly_configured_allow_list_variables = ", and explicitly configured allow-list variables."
incoming_request_body_limits_are_enforced_for_declared_and_streaming_bodies = "Incoming request body limits are enforced for declared and streaming bodies."
cgi_response_headers_have_a_hard_byte_limit = "CGI response headers have a hard byte limit."
malformed_cgi_headers_nul_bytes_and_header_injection_are_rejected = "Malformed CGI headers, NUL bytes, and header injection are rejected."
stdout_and_stderr_have_independent_byte_caps = "stdout and stderr have independent byte caps."
stderr_is_sanitized_before_logging_to_avoid_leaking_secrets = "stderr is sanitized before logging to avoid leaking secrets."
child_processes_are_spawned_in_their_own_process_group_where_supported = "Child processes are spawned in their own process group where supported."
timeouts_kill_the_process_group_not_only_the_direct_child = "Timeouts kill the process group, not only the direct child."
resource_limits_should_cover_cpu_address_space_file_size_open_files_and_child_processes = "Resource limits should cover CPU, address space, file size, open files, and child processes."
landlock_when_enabled_and_available_should_restrict_file_reads_writes_to_configured_paths_after_all = "Landlock, when enabled and available, should restrict file reads/writes to configured paths after all required files are opened."
deployment_model = "Deployment Model"
the_preferred_deployment_boundary_is_rootless_podman_plus_low_privilege_unix_users_inside_the_contai = "The preferred deployment boundary is rootless Podman plus low-privilege Unix users inside the container. Fluxheim should not require privileged host access for CGI."
can_be_documented_as_an_advanced_future_option_but_it_should_not_be_the_primary_security_boundary_be = "can be documented as an advanced future option, but it should not be the primary security boundary because it needs privileges and careful filesystem construction."
testing_plan = "Testing Plan"
required_tests_before_implementation_is_considered_usable = "Required tests before implementation is considered usable:"
compile_checks_with_and_without = "Compile checks with and without"
config_validation_for_unsafe_interpreter_paths_missing_roots_bad_limits_and_remote_insecure_writable = "Config validation for unsafe interpreter paths, missing roots, bad limits, and remote/insecure writable paths."
path_traversal_and_symlink_escape_tests = "Path traversal and symlink escape tests."
denied_source_fallback_tests = "Denied source fallback tests."
environment_allow_list_tests = "Environment allow-list tests."
timeout_and_process_kill_tests = "Timeout and process-kill tests."
stdout_stderr_limit_tests = "stdout/stderr limit tests."
malformed_header_tests = "malformed header tests."
request_body_limit_tests_for = "request body limit tests for"
and_streaming_bodies = "and streaming bodies."
rootless_podman_smoke_test_with_a_tiny_perl_cgi_script = "rootless Podman smoke test with a tiny Perl CGI script."
reload_and_operations = "Reload And Operations"
cgi_runtime_settings_should_be_process_upgrade_changes_until_the_process_model_is_proven_safe_for_sn = "CGI runtime settings should be process-upgrade changes until the process model is proven safe for snapshot-only reloads. Per-vhost route policy may later become snapshot-safe if it only changes immutable config and does not alter process pools, interpreter paths, uid/gid, Landlock policy, or resource limits."
metrics_should_include_cgi_request_totals_exit_statuses_timeouts_output_limit_violations_spawn_failu = "Metrics should include CGI request totals, exit statuses, timeouts, output-limit violations, spawn failures, and stderr counts by vhost."
headers_are_parsed_explicitly = "headers are parsed explicitly."
is_preserved_but_header_names_and_values_are_validated = "is preserved, but header names and values are validated."
